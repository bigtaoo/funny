// Re-export shim — this module's real source moved to @nw/engine (SLG_DESIGN §16.7).
// Kept so existing deep imports (`../game/GameState`, tests' `../src/game/GameState`) stay verbatim.
export * from '@nw/engine/GameState';
