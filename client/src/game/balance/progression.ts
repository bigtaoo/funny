// Re-export shim — real source lives in @nw/engine (SLG_DESIGN §16.7).
// Kept so deep imports (`../game/balance/progression`, tests' `../src/game/balance/progression`) stay verbatim.
export * from '@nw/engine/balance/progression';
