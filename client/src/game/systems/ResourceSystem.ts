// Re-export shim — this module's real source moved to @nw/engine (SLG_DESIGN §16.7).
// Kept so existing deep imports (`../game/systems/ResourceSystem`, tests' `../src/game/systems/ResourceSystem`) stay verbatim.
export * from '@nw/engine/systems/ResourceSystem';
