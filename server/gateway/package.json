{
  "name": "@nw/gateway",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "npm run proto:gen",
    "build": "tsc -b",
    "pretypecheck": "npm run proto:gen",
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "pretypecheck:test": "npm run proto:gen",
    "predev": "npm run proto:gen",
    "dev": "node --watch --import tsx src/index.ts",
    "start": "node dist/index.js",
    "pretest": "npm run proto:gen",
    "test": "vitest run",
    "test:coverage": "npm run pretest --if-present && vitest run --coverage",
    "proto:gen": "node scripts/gen-proto.mjs"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.12.0",
    "@nw/engine": "*",
    "@nw/shared": "*",
    "ioredis": "^5.4.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.70.0",
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "protobufjs": "^8.6.5",
    "ts-proto": "^2.11.8",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
