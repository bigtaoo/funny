// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.10
//   protoc               unknown
// source: replay.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { FrameCmds } from "./transport";

export const protobufPackage = "nw.replay";

export interface ReplayMeta {
  /** Unix ms */
  recordedAt: number;
  /** campaign level id (PvE) */
  levelId: string;
  /** 0/1; -1 = draw / unknown */
  winner: number;
}

export interface Replay {
  /** engine logic version (validated before replay; cross-version replays may diverge) */
  engineVersion: number;
  /**
   * Note: gameserver is logic-agnostic (M12) and cannot self-certify its version;
   *   server-side recordings set this field to 0 (reserved for client self-validation
   *   at replay time / future client-reported uploads).
   */
  mode: string;
  seed: number;
  /** PvE=levelId+version; PvP=rosterVer */
  configRef: string;
  /** non-empty frames only */
  frames: FrameCmds[];
  /** total frame count (empty frames are not stored; this field marks the end boundary) */
  endFrame: number;
  meta?: ReplayMeta | undefined;
}

function createBaseReplayMeta(): ReplayMeta {
  return { recordedAt: 0, levelId: "", winner: 0 };
}

export const ReplayMeta: MessageFns<ReplayMeta> = {
  encode(message: ReplayMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.recordedAt !== 0) {
      writer.uint32(8).int64(message.recordedAt);
    }
    if (message.levelId !== "") {
      writer.uint32(18).string(message.levelId);
    }
    if (message.winner !== 0) {
      writer.uint32(24).int32(message.winner);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReplayMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplayMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.recordedAt = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.levelId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.winner = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<ReplayMeta>, I>>(base?: I): ReplayMeta {
    return ReplayMeta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReplayMeta>, I>>(object: I): ReplayMeta {
    const message = createBaseReplayMeta();
    message.recordedAt = object.recordedAt ?? 0;
    message.levelId = object.levelId ?? "";
    message.winner = object.winner ?? 0;
    return message;
  },
};

function createBaseReplay(): Replay {
  return { engineVersion: 0, mode: "", seed: 0, configRef: "", frames: [], endFrame: 0, meta: undefined };
}

export const Replay: MessageFns<Replay> = {
  encode(message: Replay, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.engineVersion !== 0) {
      writer.uint32(8).uint32(message.engineVersion);
    }
    if (message.mode !== "") {
      writer.uint32(18).string(message.mode);
    }
    if (message.seed !== 0) {
      writer.uint32(24).uint64(message.seed);
    }
    if (message.configRef !== "") {
      writer.uint32(34).string(message.configRef);
    }
    for (const v of message.frames) {
      FrameCmds.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.endFrame !== 0) {
      writer.uint32(48).uint32(message.endFrame);
    }
    if (message.meta !== undefined) {
      ReplayMeta.encode(message.meta, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Replay {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReplay();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.engineVersion = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.mode = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.seed = longToNumber(reader.uint64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.configRef = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.frames.push(FrameCmds.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.endFrame = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.meta = ReplayMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<Replay>, I>>(base?: I): Replay {
    return Replay.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Replay>, I>>(object: I): Replay {
    const message = createBaseReplay();
    message.engineVersion = object.engineVersion ?? 0;
    message.mode = object.mode ?? "";
    message.seed = object.seed ?? 0;
    message.configRef = object.configRef ?? "";
    message.frames = object.frames?.map((e) => FrameCmds.fromPartial(e)) || [];
    message.endFrame = object.endFrame ?? 0;
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? ReplayMeta.fromPartial(object.meta)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
