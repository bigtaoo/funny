// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.10
//   protoc               unknown
// source: game.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "nw.game";

/** Play a card: play a card from a hand slot into an attack column */
export interface PlayCard {
  handIndex: number;
  col: number;
  /** used only for meteor spells (target row); ignored for units/buildings/speedup */
  row: number;
}

/** Upgrade the base */
export interface UpgradeBase {
}

/** Spend 10 ink to immediately refresh the full hand (redraws all slots; timers are randomly staggered, same as on initial deploy) */
export interface RefreshHand {
}

export interface PlayerCommand {
  playCard?: PlayCard | undefined;
  upgradeBase?: UpgradeBase | undefined;
  refreshHand?: RefreshHand | undefined;
}

/** The set of commands submitted by one side within a single tick (encoded into SideCmd.commands) */
export interface PlayerCommands {
  commands: PlayerCommand[];
}

function createBasePlayCard(): PlayCard {
  return { handIndex: 0, col: 0, row: 0 };
}

export const PlayCard: MessageFns<PlayCard> = {
  encode(message: PlayCard, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.handIndex !== 0) {
      writer.uint32(8).uint32(message.handIndex);
    }
    if (message.col !== 0) {
      writer.uint32(16).uint32(message.col);
    }
    if (message.row !== 0) {
      writer.uint32(24).uint32(message.row);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayCard {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayCard();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.handIndex = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.col = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.row = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PlayCard>, I>>(base?: I): PlayCard {
    return PlayCard.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayCard>, I>>(object: I): PlayCard {
    const message = createBasePlayCard();
    message.handIndex = object.handIndex ?? 0;
    message.col = object.col ?? 0;
    message.row = object.row ?? 0;
    return message;
  },
};

function createBaseUpgradeBase(): UpgradeBase {
  return {};
}

export const UpgradeBase: MessageFns<UpgradeBase> = {
  encode(_: UpgradeBase, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpgradeBase {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpgradeBase();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<UpgradeBase>, I>>(base?: I): UpgradeBase {
    return UpgradeBase.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpgradeBase>, I>>(_: I): UpgradeBase {
    const message = createBaseUpgradeBase();
    return message;
  },
};

function createBaseRefreshHand(): RefreshHand {
  return {};
}

export const RefreshHand: MessageFns<RefreshHand> = {
  encode(_: RefreshHand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshHand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshHand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<RefreshHand>, I>>(base?: I): RefreshHand {
    return RefreshHand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RefreshHand>, I>>(_: I): RefreshHand {
    const message = createBaseRefreshHand();
    return message;
  },
};

function createBasePlayerCommand(): PlayerCommand {
  return { playCard: undefined, upgradeBase: undefined, refreshHand: undefined };
}

export const PlayerCommand: MessageFns<PlayerCommand> = {
  encode(message: PlayerCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playCard !== undefined) {
      PlayCard.encode(message.playCard, writer.uint32(10).fork()).join();
    }
    if (message.upgradeBase !== undefined) {
      UpgradeBase.encode(message.upgradeBase, writer.uint32(18).fork()).join();
    }
    if (message.refreshHand !== undefined) {
      RefreshHand.encode(message.refreshHand, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playCard = PlayCard.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.upgradeBase = UpgradeBase.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.refreshHand = RefreshHand.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PlayerCommand>, I>>(base?: I): PlayerCommand {
    return PlayerCommand.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerCommand>, I>>(object: I): PlayerCommand {
    const message = createBasePlayerCommand();
    message.playCard = (object.playCard !== undefined && object.playCard !== null)
      ? PlayCard.fromPartial(object.playCard)
      : undefined;
    message.upgradeBase = (object.upgradeBase !== undefined && object.upgradeBase !== null)
      ? UpgradeBase.fromPartial(object.upgradeBase)
      : undefined;
    message.refreshHand = (object.refreshHand !== undefined && object.refreshHand !== null)
      ? RefreshHand.fromPartial(object.refreshHand)
      : undefined;
    return message;
  },
};

function createBasePlayerCommands(): PlayerCommands {
  return { commands: [] };
}

export const PlayerCommands: MessageFns<PlayerCommands> = {
  encode(message: PlayerCommands, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.commands) {
      PlayerCommand.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerCommands {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerCommands();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commands.push(PlayerCommand.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  create<I extends Exact<DeepPartial<PlayerCommands>, I>>(base?: I): PlayerCommands {
    return PlayerCommands.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerCommands>, I>>(object: I): PlayerCommands {
    const message = createBasePlayerCommands();
    message.commands = object.commands?.map((e) => PlayerCommand.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
