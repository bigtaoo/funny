{
  "name": "@nw/engine",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "test": "tsc -b && tsc -p tsconfig.test.json && node -e \"require('fs').cpSync('src/__tests__/goldenReplay/fixtures','dist/__tests__/goldenReplay/fixtures',{recursive:true})\" && node scripts/runTests.mjs",
    "test:coverage": "tsc -b && tsc -p tsconfig.test.json && node -e \"require('fs').cpSync('src/__tests__/goldenReplay/fixtures','dist/__tests__/goldenReplay/fixtures',{recursive:true})\" && node scripts/runTests.mjs --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
