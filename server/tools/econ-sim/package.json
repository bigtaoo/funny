{
  "name": "@nw/econ-sim",
  "version": "0.1.0",
  "private": true,
  "description": "SLG persistent-economy aggregation simulator (A-track of SLG_ECONOMY_CHECK). Headless, imports @nw/shared SETTLE_REWARDS/constants, never touches the DB. Counterpart of client/test/difficultySim.ts on the economy side.",
  "type": "commonjs",
  "scripts": {
    "sim": "tsx src/index.ts",
    "enhance-protect": "tsx src/enhanceProtectRun.ts",
    "stronghold-combat": "tsx src/strongholdCombatRun.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nw/engine": "*",
    "@nw/shared": "*"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.8"
  }
}
