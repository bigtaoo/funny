# 并行开发：git worktree 约定

> worktree = 一个仓库挂多个工作目录，各自钉死在不同分支，互不切换。**每条并行线一个 worktree，一个会话进一个目录。**

## 心智模型

**一条线 = 一个文件夹 = 一个分支，三位一体。**

```
.claude\worktrees\<slug>\   ← 文件夹(worktree)：工作目录的壳
        ↓ 检出在
feat/<slug>                 ← 分支：提交真正存放处
```

- **开会话靠「选文件夹」，不靠「切分支」。** 打开对应 worktree 文件夹，git 自动知道它在哪条分支。
- **「Couldn't switch branches」不是 bug**：同一分支不能被两个工作目录同时检出；解法是开那个 worktree 的文件夹。
- **生命周期**：建目录干活 → 提交到自己分支 → 干完把**分支合进当日集成分支** → 然后才 `git worktree remove` + `git branch -d`。分支已推上远端后本地删了不丢东西；否则合并落地前文件夹和分支都得留着。
- **集成走「当日分支」而非直接进 main**：feature 分支先 `--no-ff` 合进当日日期分支（命名 `DD.MM.YYYY`，如 `11.07.2026`），当日分支再开 PR 进 `main`（历史上如 PR #23/#24）。当日分支若已被 PR 合入 main，复用前先 `git branch -f <日期> origin/main` 快进到最新再合。

## 约定（规则）

1. **位置**：所有 worktree 放在 `C:\Users\TaoWang\Documents\funny\.claude\worktrees\<task-slug>\`，已在 `.gitignore` 忽略，不会污染 main。
2. **命名**：目录名 `<task-slug>` 用短横线短名；对应分支统一 `feat/<task-slug>`（目录名与分支后缀一致，避免错配）。
3. **主目录 `funny\` = 集成区**：钉在**当前当日分支**（如 `12.07.2026`），不是字面意义的 `main`；用于 review / 合并 / 跑全量。各 feature 一律在自己的 worktree 里做，不要直接在主目录改动。
4. **公共依赖先合**：改 `server/contracts` / `@nw/shared` / `@nw/engine` 的分支**最先合 main**，其余分支立刻 `git fetch && git rebase origin/main` 跟上，降冲突。
5. **干完即删**：`git worktree remove <path>`，分支合并后 `git branch -d feat/<slug>`。
6. **自管自清**：每个会话管好自己的分支和 worktree，任务结束时自行合并并清理，无需维护全局索引。

## 命令速查

```bash
# 新建一条并行线（基于主目录当前钉住的当日分支，分支不存在时一并创建；
# 把 <day-branch> 换成 `git -C funny branch --show-current` 的实际值，例如 12.07.2026）
git worktree add -b feat/<slug> .claude/worktrees/<slug> <day-branch>

# 已有分支，只挂目录
git worktree add .claude/worktrees/<slug> feat/<slug>

git worktree list                              # 看所有 worktree
git worktree remove .claude/worktrees/<slug>  # 删目录（工作树需干净）
git worktree prune                            # 清理失效记录

# 每条线保持跟 main 同步
git fetch origin && git rebase origin/main
```

## 注意

- worktree 共用同一个 `.git`，分支/历史/对象库全共享；磁盘只多一份工作文件。
- **同一分支不能被两个 worktree 同时检出**（git 会拒绝）。
- worktree 内 `npm install` 的 `node_modules` 各自独立（已 gitignore）。**但不必每次都重新 `npm install`**：先 `grep "@nw/" <pkg>/package.json` 看该目录（如 `client/`）是不是纯第三方依赖、没有本地 workspace 包（`@nw/shared` 等）——没有的话直接 PowerShell `New-Item -ItemType Junction` 整个 `node_modules` 指到主目录同名目录即可，内容不随分支变化，省下一次完整安装。`server/` 之类含 `@nw/*` 的目录仍按下面陷阱说明单独处理。
- **⚠️ 陷阱（2026-07-05 实测踩过）：图快用 Junction/符号链接整个 `node_modules` 目录会让 `@nw/*`（npm workspaces 本地包）解析回主仓库**。`server/node_modules/@nw/shared` 等条目本身就是指向主仓库 `server/shared` 的符号链接；如果为了省 `npm install` 直接把整个 `server/node_modules` 挂成 junction 指到主仓库，worktree 里 `import '@nw/shared'` 实际读到的是**主仓库未重建的 `dist/`**，跟 worktree 里改的 `.ts` 源码毫无关系——测试照样"全绿"，因为断言大多是符号引用（`SLG_MAP_W` 等），值对不对都能过，等于验证了个寂寞。正确做法：只把第三方依赖整体挂 junction（内容不随会话变化，挂哪份都一样），`@nw/*` 这几个 workspace 本地包必须单独用 `New-Item -ItemType Junction`（PowerShell；Git Bash 的 `ln -s` 对目录在无权限时会静默退化成一次性拷贝，不会跟着源码实时变，肉眼看不出区别）指向 worktree 自己的 `server/<pkg>`，且改完 `.ts` 后要 `npm run build` 生成新 `dist/`（多数包靠 `main`/`types` 指向编译产物解析，改源码不够）。验证方法：`node -e "console.log(require.resolve('@nw/shared'))"` 看落地路径，或直接 `echo probe > 目标目录/PROBE.txt` 测试链接是否实时生效。
- **⚠️ 共享主目录里 `git add`+`git commit` 分两步会被邻会话的提交卷入（2026-08-08 实测踩过）**：在主目录（非独立 worktree）里改小改动时，若先 `git add <自己的文件>` 暂存、隔了几步再 `git commit`，中间这段间隙里另一个并发会话如果也在主目录跑了 `git commit`（不带 `-a`、不带 pathspec），它提交的是**当时索引的全部内容**——包括你已暂存但还没提交的文件。结果：你的改动被悄悄塞进了别人的提交（commit message 完全不提你的改动），`git log --oneline -- <你的文件>` 能看到内容确实进了历史、diff 也完整，只是挂在一条语义不相关的 commit 上。发现方式：`git commit` 报"no changes added to commit"却之前明明 `git add` 过；`git status --short` 突然清空。**处理**：内容没丢就不用回滚/rebase 补救（历史改写在共享分支上风险更大，多个会话都可能已经基于它继续），确认 `git show --stat <那个意外提交>` 里含有你的文件+diff 正确即可，实质是提交信息属性不准，不是数据丢失。**规避**：`git add` 和 `git commit` 尽量在同一次 Bash 调用里连着执行（`git add <files> && git commit -m ...`），缩短两步之间的窗口；改动多、耗时长的任务仍然优先走独立 worktree，而不是主目录当日分支。
- **⚠️ 陷阱（2026-08-08 实测踩过）：worktree 里起的 dev server 忘了停，`git worktree remove` 后目录删不掉、越攒越多**：`git worktree remove` 只是解除 git 对该目录的挂靠（更新 `.git/worktrees/` 记录），完全不检查、也不会杀掉目录里正在跑的进程。如果收尾时忘了停掉在该 worktree 里起的 `webpack serve`（或其它 dev server），进程会一直占着端口、持有目录内文件的句柄——事后 `rm -rf`/`Remove-Item -Recurse -Force` 会报 `Device or resource busy` / `Cannot remove the item ... because it is in use`，目录残留在磁盘上；由于 git 层面已经不认识这个目录了（`git worktree list` 里不出现、`.git/worktrees/` 下也没有对应记录），残留目录会被长期忽略，堆到几十个、几百 MB 都不会被 `git worktree prune` 之类命令发现或清理。**排查**：PowerShell `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Select ProcessId,CommandLine`，看 `CommandLine` 里是否包含该 worktree 的路径（如 `...\.claude\worktrees\<slug>\client\node_modules\...\webpack.js`），找到就是它。**修法**：`Stop-Process -Id <pid> -Force` 杀掉后目录才能正常删除。**规避**：结束任务流程第④步删 worktree 之前，先确认自己在该 worktree 里起过的所有 dev server（游戏/tools 各端口）都已经停掉——不要假设 `git worktree remove` 会连带清理进程；批量体检时可以 `Get-CimInstance Win32_Process -Filter "Name='node.exe'" | ? CommandLine -match '\.claude\\\\worktrees'` 一次性扫出所有残留进程。**变体（同日晚些时候实测）：锁目录的不一定是 node 进程**——清理另一个已结束会话遗留的 worktree 时，`node.exe` 进程列表是空的，真正占着目录 cwd 的是 Claude 客户端自己给那次会话开的一个 headless conhost（`conhost.exe --headless ...`，父进程是共享的 `claude.exe`）。这类 conhost 在 `CommandLine` 里不会出现任何 worktree 路径字样，只能靠**创建时间和该目录的 mtime 相近**来对应；`claude.exe` 本体是所有会话共享的基础设施，绝对不能杀，只能精确杀掉那一个具体的 conhost PID（`Stop-Process -Id <该 conhost 的 PID> -Force`）。杀错/杀共享父进程会连累其它正在跑的会话。如果一时找不到对应的 conhost（同一时段可能有十几个同构的 headless conhost，仅凭时间戳也分不清），且目录内容已经能清空、只剩一个空文件夹本身删不掉，这种情况可以直接放着——空文件夹本身不占实质资源，通常等对应会话的残留句柄自然释放后就能删掉，不值得为了一个空目录去逐个尝试杀掉候选进程。
- **⚠️ 第三种锁：微信开发者工具（2026-09-01 实测踩过）**——在 worktree 里构建微信产物、并用 `cli.bat open --project <worktree>\client\wechatgame` 在 DevTools 里跑过之后，收尾时 `git worktree remove` 会报 `Permission denied`，逐层收窄后**只剩 `client\` 和 `client\wechatgame\` 两个空目录**删不掉。锁它的是 DevTools（十来个同名 `wechatdevtools.exe` 进程，`CommandLine` 里没有任何 worktree 路径字样，和上一条的 conhost 一样无法靠命令行反查）。**修法不是杀进程**：`cli.bat close --project <那个路径>` 关掉项目，**再等十几秒**（关闭是异步的，紧接着删还是会失败），然后 `Remove-Item -Recurse` 就成功了。**规避**：收尾清单里，「停掉 dev server」这一条要扩成「停掉所有在该 worktree 里起过的外部工具」——DevTools 不是 dev server，不会出现在任何 node 进程扫描里，但它同样持有句柄。
- **⚠️ 同一天的另一半：DevTools 打开的永远是主检出，别用「拷产物」代替「在主检出构建」**——`client/wechatgame/` 是整个 gitignore 的，`pixigame.js` 和它旁边的 `cdn/` 资源可以来自两次不同的构建，而且不会有任何东西提示你。2026-09-01 就是这么黑屏的：worktree 里构建完，收尾只把 `pixigame.js`+`.map` 拷进主检出，`cdn/` 停在两个月前，bundle 引用的 301 个资源里 244 个不在磁盘上。**修法**：收尾时在主检出跑一遍 `npm run build:wechat`（现已串联 `check:wechatpackage` 门禁，缺文件当场红）；黑屏时先单跑 `npm run check:wechatpackage`。详见 `design/game/ASSET_PACKAGING_LOG.md` §17.2。
- **补充技巧（2026-08-08 实测）：锁目录的也可能是自己起的 `npm install`（尤其 `server/` 装 `mongodb-memory-server-core` 那种带 postinstall 下载步骤的包），而且用 harness 的 `TaskStop` 停掉追踪中的 Bash 包装进程，不保证子进程（`npm install` → `postinstall.js`）真的同步退出**——`TaskStop` 后 harness 侧确认任务已不存在，但 `Get-CimInstance Win32_Process` 里对应的 `node.exe`（`npm-cli.js install` / `postinstall.js`）可能还挂着一段时间，此时贸然 `Stop-Process` 有误杀"其实是另一个并发会话自己的 `npm install`"的风险（多个 worktree 各自装 `server/` 依赖时，`CommandLine` 长得一模一样，Win32 API 也拿不到进程的当前工作目录，无法反查到底是哪个 worktree 的）。找不到能安全下手的 PID 时，**别在整个目录上死磕 `Remove-Item -Recurse`，改成逐层收窄**：`Get-ChildItem $root -Force | % { try { Remove-Item $_.FullName -Recurse -Force } catch { "kept: $($_.Name)" } }`，先删掉顶层能删的（`client`/`tools`/`wrangler`/`design`/`art`…），剩下 `server/`；再对 `server/` 下同样跑一遍，剩下 `node_modules/`；再对 `node_modules/` 下每个包目录跑一遍——最终能把残留体积从几十 MB 收窄到只剩一个真正被锁住的**空**子目录（如 `server/node_modules/mongodb-memory-server`）。这时就落回上面 07-25 那条陷阱结尾的结论：空文件夹本身不占实质资源，不值得为了它去猜/杀候选进程，放着等句柄自然释放即可——但先做这轮收窄，能把"放着不管"的代价从几十 MB 降到几十 KB，性价比远高于直接放弃整个目录。
- **⚠️⚠️ 更狠的陷阱（2026-07-25 实测踩过，会连累主目录）：上面这条"补救"手法本身有个隐藏前提——只有当 `server/node_modules` 是**真实目录**（`npm install` 装出来的）时，事后单独重挂 `@nw/*` 才是安全的。如果图快先把整个 `server/node_modules` 当一个单位 junction 到主仓库（如上一条陷阱描述的反面操作），那 worktree 里的 `server/node_modules` 跟主仓库的 `server/node_modules` 其实是**同一个物理目录**，只是路径不同——这时候在 worktree 里 `rmdir` + 重新 `New-Item -ItemType Junction` 单独修 `@nw/shared`/`@nw/engine`/`@nw/metaserver`/`@nw/commercial` 这几个条目，改的是那个唯一的物理目录，**主仓库那边看到的也是改过的结果**。任务做完 `git worktree remove` 把 worktree 删掉后，主仓库的 `server/node_modules/@nw/*` 就变成指向一个已经不存在的路径的悬空链接——`@nw/shared` 等包在主仓库（以及当时所有其它会话）里直接解析失败，`tsc`/测试全灭，而且现象是"主目录莫名其妙坏了"，不会直接联想到某个已经收尾、已经删除的 worktree。**正确顺序**：`server/node_modules` 只要含 `@nw/*` 就不要整体 junction 到主仓库——**直接在 worktree 里 `cd server && npm install` 走真实、独立的安装**（npm workspaces 会自动把 `@nw/*` 正确 symlink 到 worktree 自己的包目录），几十秒到一分钟，比排查"主仓库为什么坏了"便宜得多。`client/` 这类没有 `@nw/*` 依赖的目录，整体 junction 到主仓库仍然安全（单向只读引用，事后不会再修改这几个条目）。收尾前建议顺手 `git worktree list` 确认待删的路径只在自己这条线里出现，删除后 `node -e "console.log(require.resolve('@nw/shared'))"` 在主目录复测一次确认没被连累。
- **⚠️ 陷阱（2026-08-20 实测踩过）：worktree 里 `npm install <新包>` 会把 `node_modules` junction **换成一份真实目录**，于是「省下一次安装」的前提当场失效，而且**主检出并没有拿到那个新包****——这轮要给 `tools/animator` 加一个 devDependency（`fake-indexeddb`），worktree 的 `tools/animator/node_modules` 事先 junction 到了主检出同名目录。原以为 `npm install` 会写穿链接（那样主检出会顺带装上，无害），实际是 npm 直接把链接**替换成一个 121MB 的真实目录**：worktree 里有 `fake-indexeddb`，主检出里**没有**。后果不是「污染了主检出」而是反过来——合并进当日分支后，主检出的 `package.json` 已经声明了这个依赖，但 `node_modules` 里没有它，主检出跑 `npm test` 会直接挂在 `Cannot find module`。**处理**：合并后在主检出 `cd tools/<tool> && npm install` 补一次（`package-lock.json` 已随提交进来，几秒钟），然后在主检出把 typecheck/test/build 重跑一遍确认——**合并完就得在主检出验一次，别只信 worktree 里的绿**。**顺带的好消息**：正因为 junction 被换成了真实目录，收尾时它不是链接，上一条陷阱那套「先 `cmd /c rmdir` 解链」的顺序对它不适用（`Get-Item -Force` 的 `LinkType` 为空就是它已经不是链接了，直接 `Remove-Item -Recurse` 是安全的）；但**其它没被 `npm install` 碰过的 junction 仍然是链接**，仍然要先解链——所以收尾时对每一个挂过的目录都得先看一眼 `LinkType`，而不是凭记忆分类。
- **⚠️ 陷阱（2026-08-20 实测踩过）：`client/node_modules` 那个 junction 会让 `git worktree remove` "成功"却留下残留目录**——上面两条把残留归因于"没停 dev server"或"进程占着句柄"，但还有第三种、更安静的成因：`git worktree remove` **报告成功、`git worktree list` 里也确实不再出现该路径**，磁盘上却剩下一个 `<worktree>\client\` 目录，里面只有那个指向 `D:\funny\client\node_modules` 的 junction。git 拒绝（或者说不去）删穿这个链接，于是把包含它的那一层整个留了下来，而且**没有任何报错**——所以只看命令输出会以为清理干净了。**排查**：第④步之后额外跑一次 `Get-ChildItem .claude\worktrees -Directory`，跟 `git worktree list` 的结果比对，差集就是残留（本次还顺带扫出 4 个更早会话留下的孤儿目录）。**修法有顺序要求，不能图快**：先用 `cmd /c rmdir "<worktree>\client\node_modules"` **只删链接本身**（`cmd` 的 `rmdir` 对 junction 只解链、不动目标），确认 `Test-Path` 为假、并且**数一遍主目录 `client\node_modules` 的条目数没变**（本次 651 → 651），再 `Remove-Item -Recurse -Force <worktree>`。**千万不要**直接对 worktree 根跑 `Remove-Item -Recurse -Force`：PowerShell 5.1 在这条路径上会穿过 junction 去删**目标**的内容，也就是主目录的 `client\node_modules`，正好踩中上一条陷阱说的"连累主目录"（只是介质从 `server/@nw/*` 换成了 `client/` 整包）。**规避**：挂 junction 时就记住它是收尾时的一个待办；`Remove-Item -Recurse` 之前永远先解掉工作树里所有的链接。
- **⚠️ 想在共享主目录里临时对比"改动前/改动后"行为，图快用了 `git stash`，结果 stash 的是别的并发会话的未提交 WIP（2026-08-25 实测踩过，有惊无险）**：在独立 worktree 里做完一轮图标重构后，想验证某几个 UI 测试失败是不是自己改动引入的，图省事直接在共享主目录（`D:\funny`，钉在当日分支）跑了 `git stash -u` 想拿到一份"干净"基线对比——但共享主目录本来就可能（且经常）挂着其他并发会话的未提交改动（当时主目录上有另一会话正在改 `client/src/render/BuildingView.ts` 等文件）。`git stash -u` 不认改动是谁做的，会把当前工作区**全部**未提交内容（包括别的会话的 WIP）一并搬进 stash——如果这之后再对主目录做其它写操作（哪怕只是切分支、被动触发 checkout），别的会话的改动就可能在毫无提示的情况下从磁盘"消失"，而那个会话对此一无所知，还在假设自己的文件好好待在原地。**正确做法**：需要一份干净基线做对比时，只应该在**自己独立的 worktree 里**`git stash`（本地隔离，不影响任何人）；共享主目录任何时候都不要为了"临时看一眼"而 `git stash`/`git checkout -- <path>`/`git reset` 之类会改动他人未提交内容的命令——这类操作只允许针对**自己刚改的、已确认路径**的文件。**处理**：发现手滑后立即 `git stash pop`（同一会话内、没有中间任何其它操作时通常能完整找回，本次确认 pop 后 `git status --short` 与操作前完全一致，未丢东西），但这只是运气好；能不能安全 pop 回去取决于期间有没有人也在改同一批文件、有没有人也在操作 stash 栈——这些都不受自己控制。**规避**：把"需要基线对比"当成一个信号——去开一个新 worktree（或者干脆到自己已有的独立 worktree 里执行 `git stash`），而不是对共享主目录动手；共享主目录只用来 `git status`/`git log`/`git diff`/`git merge`/`git commit`（提交自己那几个文件）这类只读或范围精确的操作。
- **⚠️ 变异测试（mutation testing）的「改坏源码→跑用例→还原」循环，用 `git checkout -- <file>` 还原会把这轮**自己**未提交的改动一起冲掉（2026-08-27 实测踩过）**：新写了用例想逐条验证「改坏源码用例会不会挂」，脚本写成 `sed -i '<改坏>' f.ts; 跑用例; git checkout -- f.ts`。前两个循环看着都对（用例确实挂了），但 `git checkout -- f.ts` 还原的是 **HEAD 版本**，不是「我改坏之前的工作区版本」——于是第一次执行就把这个文件上本轮所有未提交的改动（正是被测的那个特性本身）一起还原没了。后续循环全部在「特性根本不存在」的代码上跑，`sed` 匹配不到、静默不改，用例照挂，**看起来和「变异被抓住了」一模一样**。发现方式：循环跑完 `grep` 一下特性关键字，文件里已经没有了。**修法**：还原用**文件副本**而不是 git——循环开始前 `cp f.ts /tmp/f.bak`，每轮结束 `cp /tmp/f.bak f.ts`。**规避**：worktree 里任何「临时改坏再还原」的操作都别走 git（`checkout`/`restore`/`stash` 的粒度都是「相对 HEAD」，而你要的是「相对我 30 秒前的工作区」）；另外别在这种脚本里用 `set -e` + 管道到 `grep`，`grep` 无匹配返回 1 会让脚本在还原那步之前就退出，问题会更隐蔽。

- **⚠️ 派给子 agent 的任务如果自己跑了 `git checkout <path>` 来"撤销手滑改的文件"，会连带清空同一个文件上你自己还没提交的改动（2026-08-09 实测踩过）**：主目录里同时有"我"在改设计文档、又派了一个子 agent 去改测试文件。子agent 误改了它任务范围之外的 `design/DECISIONS.md`/`design/game/SLG_DESIGN.md`（大概是顺手做了不该做的文档同步），发现后自己跑了 `git checkout -- design/DECISIONS.md design/game/SLG_DESIGN.md` 想撤销——但 `git checkout <path>` 撤的是**该路径在工作区里的全部未提交改动**，不区分"哪几行是子agent 刚加的、哪几行是我更早加的还没提交的"，于是把我自己那部分也一起冲掉了，且没有任何报错或提示——直到我后续 `git status --short -- design/` 发现该路径完全没有 diff 才察觉。**处理**：好在改动内容还留在这轮会话的上下文里，重新 Edit 一遍即可，没有真正丢失（只是多花一轮）。**规避**：①派子agent 任务时如果涉及可能touch 到"我这轮也在改的文件"，在任务描述里明确列出"仅改 X/Y/Z 路径，其它路径一律不要动，哪怕看到过时的表述"；②子agent（或自己）想撤销一处误改时，优先用 `git diff`/编辑器手动删掉那几行，而不是对整个文件 `git checkout`——后者的撤销粒度是"整个文件相对 HEAD"，在共享检出里几乎总是比你想要的粒度粗；③改完关键文档后，紧跟着就 `git add`+`git commit`（同一次调用），别让它以"未提交"状态在工作区里存活太久，存活时间越长，被另一个并发操作（无论是别的会话，还是自己派的子agent）连带清空的窗口就越大。
- **设计文档里 `§10.x` 这类顺序编号的变更日志，两条并行 worktree 各自在同一个父提交上追加新一节，编号会撞车（2026-08-09 实测踩过）**：两个会话都从 `ca24af65` 开出 worktree，都往 `RETENTION_DESIGN.md` 追加了"下一节"，都手写成了 `### 10.13`（各自当时看到的"最后一节"确实是 §10.12，编号推算完全合理，只是两边同时这么推算）。先合并的一支顺利落地；后合并那支 `git rebase` 到最新日分支时在这个文件上冲突——不是内容冲突，是"两段完全不相关的新增内容各自声称自己是同一个编号"。**处理**：冲突时不能简单"两边都要"地拼接（会留下两个 §10.13），需要手动把其中一段（通常是后 rebase 的那段）改成下一个可用编号（如 §10.14），同时检查文件顶部的状态摘要行（`> 状态：...`）有没有同样引用了旧编号，一并改掉。**规避**：给设计文档追加编号小节前，先 `git log --oneline -- design/xxx.md` 或直接读文件确认"最后一节的编号"是不是自己 rebase/fetch 之后的最新值，而不是开 worktree那一刻的旧值——但即使这样也无法完全避免（对方可能在你确认之后、你提交之前也追加了一节），rebase 时留意这类"看似冲突但其实是编号顺序问题"的 diff，不要用 `git checkout --theirs`/`--ours` 简单二选一。
- **⚠️ 环境信息报的 worktree 路径没有当参数用，手滑用了不带 worktree 前缀的路径，改动全落进了共享主目录（2026-08-14 实测踩过）**：环境提示里明明写了"Primary working directory: `.claude/worktrees/<slug>`"，但一连串 Read/Edit 调用图省事直接传了 `D:\funny\server\...`（没有 `.claude\worktrees\<slug>\` 前缀）——这两个路径都真实存在（worktree 和主目录是两份独立工作文件），工具不会报错，只会老老实实改到你给的那个路径上。改完一路 `tsc`/`vitest` 全绿（因为验证也在同一个错误目录里跑的），直到收尾阶段习惯性 `cd` 到 worktree 路径去 `git status` 才发现 worktree 干干净净、改动其实全在主目录（当时签出在共享的当日分支）里。**处理**：改动还没提交，先 `git diff --stat` 确认改的就是这几个文件、没混进别的会话的 WIP，`git checkout -- <these files>` 撤回主目录，再在真正的 worktree 路径下把同样的 Edit 重放一遍。**规避**：一个会话开头看到"你在 worktree 里"的环境提示后，第一次 Read/Edit 前先 `ls <worktree路径>` 或直接把完整 worktree 路径拼进第一次调用，建立"这次任务的文件都在这个前缀下"的路径习惯，而不是凭记忆敲仓库根路径。
- **⚠️ 派下来的 worktree 分支本身是"陈旧"的——从 main 开出、没跟上当日分支的最新提交，任务要改的文件在这条线上根本不存在（2026-08-14 实测踩过，紧接上一条陷阱之后发现）**：撤回主目录的误改、切回 worktree 路径后，发现 worktree 分支 `claude/<slug>` 的 HEAD 正好是它与 `origin/main` 的 merge-base——也就是这条线从 main 开出后再没同步过，而当日分支 `DD.MM.YYYY` 已经比这个 merge-base 多了几十个提交（含前一天新增的、这次任务恰好要改的测试文件）。**判断是否能安全同步**：`git rev-list --count HEAD..<日分支>` 看落后多少、`git status --short` 确认 worktree 干净、`git merge-base --is-ancestor HEAD <日分支>` 确认自己的 HEAD 确实是日分支的祖先（没有自己独有的、日分支上没有的提交）——三者都满足时，直接 `git merge --ff-only <日分支>` 把 worktree 分支快进到日分支最新，比 `rebase` 更直接（没有自己的提交需要重放）。**同步后别忘了**：`server/` 下 `@nw/*` workspace 包要按上面 07-25 那条陷阱的正确顺序真实 `npm install`（不要整体 junction），且新同步进来的代码如果涉及 `@nw/shared`/`@nw/engine` 等本地包，这些包自己也要 `npm run build` 出 `dist/`（否则 `tsc`/vitest 会报 `Cannot find module '@nw/shared'`）；如果测试里有依赖 `../dist/xxx.js`（走编译产物而非 `src/`）的 e2e 用例，metaserver 自己（以及它依赖的 `socialsvc` 等同级服务）也要 `npm run build` 一遍，否则这批 e2e 会报 `Failed to load url ../dist/xxx.js`。**规避**：接到 worktree 任务时，如果任务描述提到的文件路径在 `ls`/`Read` 时报"不存在"，先怀疑是不是分支陈旧（`git log --oneline -1` 对比日分支的 `git log --oneline -1`），而不是假设文件被删除或路径写错了。
- **⚠️ `git worktree remove` 报成功，目录却还在——剩下的正是你自己挂的 `node_modules` junction（2026-08-17 实测踩过）**：按上面"只把第三方依赖整体挂 junction"的省时做法给 `client/node_modules` 挂了 junction 后，收尾 `git worktree remove` **会成功返回**（git 只管它跟踪的文件，未跟踪的 reparse point 不归它管），`git worktree list` 里也确实消失了——但磁盘上留下 `<slug>/client/node_modules` 这个链接和它的两层父目录。跟上面 08-08 那条"dev server 占着目录删不掉"表现相似（都是孤儿残留），但**成因完全不同**：这里没有任何进程占用，纯粹是 git 不删未跟踪的链接，所以按那条去查 `node.exe`/conhost 会一无所获。**排查**：收尾后 `ls .claude/worktrees/` 对一遍 `git worktree list`，两者对不上的就是这类残留（本次同时发现了另一个已结束会话留下的同类目录）。**修法（顺序不能错）**：先单独删 junction 本体，**再**删父目录——`[System.IO.Directory]::Delete($link, $false)` 只摘掉 reparse point、绝不跟进目标；**不要**对着父目录直接 `Remove-Item -Recurse -Force`，Windows PowerShell 5.1 对 junction 的递归删除历史上会删到**目标**的内容里去，而这里的目标是共享主检出的 `D:\funny\client\node_modules`，删了就是 07-25 那条"连累主目录"的翻版。删完立刻验一次：`(Get-ChildItem D:\funny\client\node_modules -Force).Count` 与删除前一致（本次 651 → 651），再 `node -e "console.log(require.resolve('pixi.js-legacy'))"` 确认主检出仍能解析。**规避**：收尾第④步删 worktree 之前，先把自己挂的 junction 按上述方式摘掉，再 `git worktree remove`。
