# client/ 关键约束与核心模块

## 关键约束

- **渲染**：`pixi.js-legacy`，兼容微信小游戏 WebGL
- **游戏逻辑**：纯 TS，固定点数（`@nw/engine/math/fixed`），与渲染解耦
- **平台适配**：Web / 微信小游戏 / CrazyGames，多入口 webpack 构建
- **骨骼动画 Runtime**：`StickmanRuntime`（`src/render/stickman/`），加载 `.tao` ZIP，驱动 PIXI Sprite
- **确定性约束**：游戏逻辑（`@nw/engine` = `server/engine/src/`）内严禁 `Math.random()`，必须用 `Prng`（`@nw/engine/math/prng`）
- **多语言（i18n）**：`zh.ts` 为键唯一来源（`TranslationKey`），`en`/`de` 声明为 `Record<TranslationKey, string>` 漏翻报错；`t(key, params?)` 取词，支持 `{param}` 插值
- **网络协议 codegen**：`transport.proto`/`game.proto` → ts-proto via buf → `src/net/proto/`；`openapi.yml` → openapi-typescript → `src/net/openapi.ts`；改契约须重跑 `npm run proto:gen` / `npm run rest:gen`
- **统一输入管线（M13）**：`InputSource` 接口，`LocalInputSource`（单机）/ `NetInputSource`（联机锁步）/ `RecordingInputSource`（录制）/ `ReplayInputSource`（回放）
- **⚠️ 单文件 500 行收敛 + 拆分形态优先级（2026-08-11 起，照抄 `claudedocs/server.md` 同名章节，规则原文见那边，这里只记 client 特有的落点）**：`client/scripts/checkFileLength.mjs` + `file-length-baseline.json`（排除生成产物 `net/openapi*.ts`/`net/proto/*`/`i18n/locales/*`，接入 CI）与服务端同一套 baseline-drift 语义。**拆分形态按①独立函数模块→②独立类+组合→③线性继承链的顺序择优，继承链是兜底不是默认**——**`XMixin<TBase>(Base)` + 接口声明合并这套写法本质就是③（一条隐式继承链，只是没写 `extends`），不再是首选写法**：写新场景/服务时按①/②设计，不要新增 `XMixin` 工厂或往现有链上加一环。**进度（2026-08-12）**：`net/ApiClient`、`net/WorldApiClient`、`scenes/worldmap/WorldMapPanels`、`scenes/{GachaScene,CityScene,DefenseEditorScene,ShopScene,SectScene,FriendsScene,AuctionScene,CardScene,EquipmentScene,FamilyScene,LobbyScene}`、`render/GameRenderer`、`scenes/worldmap/WorldMapRenderer` 16 条链**已完成**改判为独立类+组合（构造器接 `XxxCore`，原 `base.ts` 的 `render()`/`update()` 派发上移到装配壳），下表对应行已更新措辞、baseline 里已摘除随之瘦身到 500 行以下的文件；`test/ui/composition-wiring.ui.ts` 对这 16 条逐条断言"每个兄弟类持有装配壳同一个 `core`/被依赖兄弟实例"（同服务端 `composition-wiring.test.ts` 手法，identity check 不是行为测试）。至此原计划批 4-6 的全部 7 条 mixin 链（`GameRenderer`/`AuctionScene`/`CardScene`/`EquipmentScene`/`FamilyScene`/`LobbyScene`/`WorldMapRenderer`）**已全部改判完毕**。`scenes/worldmap/WorldMapRenderer`（7 层 mixin 应用，这批里最深的一条）的两处真双向依赖都以 `pool.ts` 为枢纽——① `pool` 调 `city.refreshCityLayer()` ↔ `city` 调 `pool.isBaseAnchor()`；② `pool` 调 `fog.renderOverlay()` ↔ `fog.renderMap()`（`invalidatePool()` 的纯转发别名）调回 `pool.invalidatePool()`——**没有各打一次性 lazy hook**，而是判定 `pool.ts` 本身越界代持了"数据变了、重绘全图"的编排职责（`buildPool()`/`invalidatePool()`/`refreshPool()` 原本就是 `build.ts`/`viewport.ts`/十几个外部调用点唯一的"刷新入口"），把触发点整体上移到装配壳：`pool` 不再引用 `city`/`fog`（`city` 仍单向依赖 `pool.isBaseAnchor()`，不成环），装配壳新增 `invalidatePool()`/`refreshPool()` 显式依次调 `pool.*`+`city.refreshCityLayer()`+`fog.renderOverlay()`；`build.ts`/`viewport.ts`/`lifecycle.ts` 中途需要这套组合调用，以闭包 `refreshMap: () => void` 注入（同样安全：这几个方法都只在装配壳整体构造完成后才会被外部调用）。重新 grep 全量调用图确认 `build→pool`/`viewport→pool`/`lifecycle→{vignette,fog,build}` 均仍是单向，未发现审计摘要之外的漏报。`fog.ts` 转换前已超 500 行（613），二次拆分挖出 `tokens.ts`（form①，march/occupy/stationed 三个 token 同步循环——只读写 `core.ctx`、不调其它 Fog 方法，天然自由函数）后回落（332 行）；`shieldFx.ts` 确认不在链上，原样保留。`scenes/LobbyScene`（原 807 行、这批里最大的单文件）的 `build.ts`↔`badges.ts` 是一对真双向依赖（`build()` 在建好每个 badge 图层后调 `badges.ts` 的 `drawSocialBadge`/`drawAchievementBadge`/`drawShopBadge`/`drawWorldOfflineBadge`/`drawSideStripBadges`；`badges.ts` 的 `applyEventsAvailable` 反过来调自己的 `rebuild()`，`rebuild()` 又调 `build()`）——**没有合并成一个类**：`rebuild()`（teardown 干净的 Core 状态后重新触发整个场景重排）本质是一个跟"badges"无关的全场景级操作，只是恰好第一个需要它的调用点（events 条目显隐）长在 `badges.ts` 里，判定依据正是 FamilyScene 那次总结出的"`build()` 若属于更大范围的关注点、不该被一个窄域类拥有"这条——**修法是把 `rebuild()` 整个搬到 `core.ts`**（用惰性钩子 `buildHook` 补上 Core 对尚未构造的 `BuildPanel.build()` 的引用，同 `allianceHooks`/`reopenCreateForm` 手法），之后 `badges.ts` 只需单向调 `this.core.rebuild()`，`build.ts→badges.ts` 那条边保持单向（画 badge 图层）不再有回边。`overlays.ts` 重新 grep 全量调用图后确认仍是纯叶子（只被 `build.ts`/装配壳调用，自己不回调任何兄弟）。`build.ts` 的二次拆分没有走 `types.ts`/共享 chrome 这条老路，而是按视觉分区切成 5 个纯 form① 兄弟模块——`header.ts`（logo/title/头像/账号-金币-段位徽章）、`mainContent.ts`（主按钮+英雄剪影+图鉴/世界地图双卡+右侧 daily/mail/events 竖条，依赖 `badges` 画竖条红点）、`bottomNav.ts`（底部五格 tab+社交/成就/商店/世界离线四个徽章层，同样依赖 `badges`）、`vsOverlay.ts`（对局揭示层）、`matchState.ts`（本地 AI 对局的 `onStartPressed`/`matchFound` 状态机，零本地状态、纯操作 `core` 字段，故不建类）——`build.ts` 本身瘦身到只剩 `build()` 编排 + `handleDown()` 分发，188 行。**唯一一次跳出"Core 拥有 update()/destroy()"惯例**：原 mixin 链的 `update()` 直接按名字调两个不同兄弟的方法（`BuildMixin` 的 `matchFound()`、`OverlaysMixin` 的 `clearToast()`），不是单一注入回调能覆盖的形状，故改判后 `update()`/`destroy()`/首次 `build()` 调用/`input.onDown` 订阅全部留在装配壳 `LobbyScene.ts`（`core.ts` 不再实现 Scene 接口的这三个方法）——装配壳因此需要自己的 `unsubs` 数组承接 `input.onDown`（而不是塞进 `core.unsubs`），否则 `test/input-subscription-cleanup.test.ts` 的同文件订阅/回收静态扫描会报"只见 push 不见 drain"的假阳性。`scenes/FamilyScene` 是这批里唯一一次"预审计双向依赖数完全准确、没有漏报"的转换：原 `actions.ts`（`doSendMsg` 里没草稿时调 `input.ts` 的 `openSendInput`）↔`input.ts`（Enter 键回调调 `actions.ts` 的 `submitMessage`）确认是唯一一对真双向依赖，重新 grep 全量调用图未发现其它漏报——修法选了"合并成一个类"而非惰性钩子：`doSendMsg`/`submitMessage` 连同原本就在 `input.ts` 的 `openSendInput`/`openInputFor` 一起挪进新的 `InputPanel`（因为"没草稿先弹键盘、有草稿直接发"是同一个发送框交互的两种状态，不是两个域），`ActionsPanel` 因此完全不再依赖 `Input`，`Render` 仍同时依赖 `Actions`+`Input`（不变）。`core.ts`（560→469 行）挖出 `header.ts`（`drawHeaderTitle`，纯 form①，无 Core 委托）后回落 500 行以下；`render.ts`（547→328 行，先减去合并进 `InputPanel` 的发送逻辑，再挖出 `lists.ts`）把 `renderMembers`/`renderChannel` 挖成 `lists.ts`（同 `SectScene` 的 `lists.ts` 手法，显式 `(core, actions|input, ...)` 传参，`truncateToWidth` 工具函数也跟着搬过去、`render.ts` 反向 import 回来，避免 render.ts↔lists.ts 循环 import）后回落。`scenes/EquipmentScene` 一次性踩出两对真双向依赖（此前审计只报出一对，属于"审计会漏"的实例，改判前务必自己重新 grep 全量调用图，不要照抄审计摘要）：①`inventory.ts`（renderInstanceCell 调 `detail.instanceActions`/`openDetail`）↔`detail.ts`（doEnhance 调 `inventory.refreshInstanceCell`，一个单格增量重绘的性能优化，2026-07-28 修复引入，改走全量 `render()` 会打回那个 flicker 老问题，不能简化掉）；②`detail.ts`（instanceActions 调 `assign.beginAssign`/`ownerCardId`）↔`assign.ts`（doEquipTo 调 `detail.doEquip`）——都用惰性钩子切断（`core.refreshInstanceCellHook`/`core.doEquipHook`，同 `doFuse`/`reopenCreateForm` 手法），装配顺序 `reforge → assign →`（挂 `cancelAssignHook`）` → detail →`（挂 `doEquipHook`）` → inventory →`（挂 `refreshInstanceCellHook`）` → craft`。`core.ts`（835→525 行）挖出两个纯 form① 模块后仍压线超限，判定为与 `CardScene/core.ts`（519 行）同类的"无自然切分点"例外，baseline JSON 里记了理由；`inventory.ts`（589→415 行）把两个最大的格子渲染函数（`renderInstanceCell`/`renderLoadout`）挖成 `cells.ts`（显式 `(core, detail, ...)` 传参，无 Core 委托方法）后回落 500 行以下，不需要例外。`render/GameRenderer` 是这批里唯一一个 events→input 单向依赖（`EventsPanel.handleEvent` 的 `card_played`/`card_expired`/`game_over`/`game_draw` 分支要调 `InputPanel.cancelDrag`/`cancelTapSelect` 并读 `drag`/`tapSelect`）——没有走窄接口，直接经 `core.input` 转一手，因为 EventsPanel 本来就要拿 Core 的大半个面（boardView/unitView/vfxSystem/buildingView/tutorial/hudView/…），窄接口不会减少耦合面。`scenes/AuctionScene` 的 `PickerMixin`/`CreateFormMixin` 是一对真双向依赖（picker→`openCreateForm`，createForm→`selectedItemLabel`/`openItemPicker`）——**没有合并成一个大类**，而是把"返回创建表单"这一半调用改经 Core 的 `reopenCreateForm` 惰性钩子（同 `allianceHooks` 手法），双向依赖因此完全消失；消失之后 picker 那一侧不再需要 `this`/兄弟引用，于是它从"该合并的类"直接降级成了 `itemPickerRender.ts` 里的一组纯函数（form①），比合并成一个类更瘦——**双向依赖的资治标准修法多一条**：先看能不能用惰性钩子切断其中一个方向，切断后往往发现另一侧根本不需要类了，不要一见双向就默认合并。`scenes/CardScene` 的 `feed.ts`→`actions.ts`（confirm 按钮调 `doFuse`）与 `actions.ts`→`feed.ts`（`doFuse` 成功后调 `playFusionAnim`）是一对真双向依赖——但两者严重程度不对称：`actions.ts` 只需 `feed` 的一个方法，窄接口 `PlayFusionAnimSource` 直接引用即可；`feed.ts` 需要的 `doFuse` 却出在装配顺序更晚构造的 `ActionsPanel` 上（`feed` 先于 `actions` 建，因为 `actions` 反过来要拿 `feed`），构造时序上不可能互相直接持有——用 `core.doFuse` 惰性钩子（默认 no-op，装配壳建完 `ActionsPanel` 后立刻覆盖，同 `reopenCreateForm`/`allianceHooks` 手法）切断这一个方向，双向依赖消失，无需合并成一个类。`feed.ts`（741 行）二次拆分与 `CityScene`/`FriendsScene` 走的是不同分支——它没有可提炼的"多面板共享 chrome"，而是有一段与本类局部状态（`slotIds`/`artHooked`/`currentTarget` 等 `openFuseSelect` 内闭包变量）零耦合的纯动画代码块（`playFusionAnim`）——整段搬进 `feedAnim.ts`（`playFusionAnimImpl(core, geom)`，`geom` 快照替代闭包捕获）；剩余仍超限的部分再照抄 `chrome.ts` 手法，把候选行渲染（逐行绘制，参数显式传入而非闭包捕获）挖成 `feedList.ts` 的 `drawFuseCandidateRow(...)`。**16 条链全部改判完毕后，重新审计了剩余 baseline 条目里符合①形态的候选**：`scenes/worldmap/tileGraphics.ts`（零类零 `this` 的自由函数集合，按视觉/几何职责拆成 `tileGraphics/{tiles,resources,primitives}.ts`，原文件降为 barrel）、`app/nav/game.ts`（`createGameNav(ctx)` 拆时发现 campaign 与 roster 两组间有一对真双向调用——`goCampaignMap.onOpenEquipment` 调 `goEquipment`/`goCardRoster`，`goEquipment` 的默认 `back` 又指回 `goCampaignMap`——判定不值得为此专门开惰性钩子，两组合并进 `game/campaignRoster.ts` 一个文件，仍在 500 行以内；career 组因不跨调，按原计划单独拆成 `game/career.ts`）、`app/nav/shop.ts`（按建议三分：`shop/badges.ts` 纯函数、`shop/iap.ts` 充值轮询、`shop/nav.ts` 六个跳转，彼此无跨组调用）均已按①拆分，原文件均降为薄转发壳/barrel，外部 import 路径不变。`scenes/CampaignMapScene.ts`/`scenes/BattlePassScene.ts`/`scenes/ResultScene.ts` 三个"类内部分方法零/近零 `this` 依赖"的部分抽取候选也已完成：分别挖出 `CampaignMapScene/drawing.ts`（618→450 行）、`BattlePassScene/cell.ts`（623→482 行）、`ResultScene/builders.ts`（786→455 行，`ResultScene` 那组预审计漏报了实际 `this.` 引用数，但复核后发现全是 `this.container`/`this.w`/`this.h` 等平凡字段读取，照样能显式传参抽出，不影响结论）。**另将 `src/app.ts`（`PixiAppViews`，~35 个近一行的 `showX` 转发方法）与 `src/game/meta/SaveManager.ts`（单一云同步共享状态根）记为两条新的 baseline 例外**（同 `ApiClient`/`CityScene/core.ts` 一类理由，写在 `file-length-baseline.json` 的 `_readme_2026-08-11-followup` 字段），未做代码拆分。至此原 38 文件审计的全部候选（含批 0-3 的 9 条链、批 4-6 的 7 条链、6 个①形态候选、2 条新记的例外）**已全部处理完毕**。
**PR #98 CI 撞出的一批 GREW/NEW 违规（2026-08-12）**：`checkFileLength.mjs` 在这条 PR 的 CI 上一次性拦下 6 处（5 GREW + 1 NEW），逐一核对 diff 后判定全部是"小且嵌在既有方法内、没有可提炼切分点"的例外，记进 baseline 而非拆分（理由详细版见 `file-length-baseline.json` 的 `_readme_2026-08-12-pr98` 字段）：`src/app.ts` 607→614（既有转发方法 `showReplay` 加了 3 个可选参数，ADR-065 攻城回放持久化——跟 2026-08-11 那条"薄转发壳"例外同款，拆转发列表不会降低复杂度）；`src/render/HUDView.ts` 586→587、`src/render/TutorialDirector.ts` 503→504（各一行 ADR-065 fp 字段改名，无可挖内容）；`src/scenes/CityScene/core.ts` 753→771（SLG 开局引导链 `GuideOverlay` 字段 + 构造器/`update`/`destroy` 三处生命周期调用，嵌进既有共享状态根——跟已记录的 752/753 例外同一种"无自然切分点"形状）；`src/scenes/worldmap/WorldMapNet.ts` 740→745（两处各 2 行的引导步骤 flag 判断，嵌在既有网络响应处理方法内）；`src/scenes/FriendsScene/core.ts`（NEW，505，约战互斥修复给 `applyDuelCancelled` 的既有三元链加了一个 `reason:'busy'` 分支 + 1 行状态复位，同样是既有 switch 里的一个 case，没有切分点）。

**仍待处理**：原计划批 4-6 的全部 mixin 链已清零，只剩一批未成链但单文件超 500 行、只需 form① 拆分（无跨文件 `this.*` 调用）的独立文件（详见 baseline JSON 剩余条目）。改判前的判断法（数跨 mixin `this.*` 调用、双向依赖=边界画错了要合并或抽共同底层）与服务端完全一致，不要重新发明。**这 16 条实践中新增的三个具体手法**（原服务端文档没有，因为服务端没有这三类问题）：
  - **同模块内部调用无法被 `vi.spyOn` 拦截**：拆成 form① 自由函数后，若同一模块内的另一函数直接裸调用它（如 `grid.ts` 的 `renderGrid` 内部调 `drawArtFit`），这是词法自引用，不经过模块导出对象的可变绑定——测试里 `vi.spyOn(gridModule, 'drawArtFit')` **拦不住**这种内部调用（实测验证过，不是猜测；跨模块调用不受影响，只有"同一文件内部调自己文件的另一个导出"这一种情况会踩）。**修法**：给该文件加一个自引用 `import * as selfModule from './thisFile'`，内部调用点改走 `selfModule.drawArtFit(...)` 而不是裸 `drawArtFit(...)`——牺牲一点可读性换回测试可拦截性，仅在确实有测试要 spy 该调用点时才加，不要预防性到处加。
  - **跨域调用目标尚未构造完成的两阶段构造**：`XxxCore` 构造函数常需要在自己内部（`build()`/`renderHeader()` 等同步路径）或直接暴露给 `InputManager` 的订阅回调里调用尚不存在的兄弟域方法（如 `SectSceneCore` 的表头联盟按钮要调 `ActionsPanel.openManageAllies`，`DefenseEditorSceneCore` 要把 `input.onDown` 接到还没建的 `InputPanel.handleDown`）。**两种修法按情形选**：①**惰性闭包参数**——装配壳把 `(x,y) => this.input.handleDown(x,y)` 这类闭包（真正调用发生在后面、闭包创建时 `this.input` 还没赋值也没关系）作为构造器参数传给 Core，Core 内部把闭包接到 `InputManager`（`DefenseEditorScene` 的 `handlers` 参数）；②**默认空实现的可写字段**——Core 声明一个默认指向 no-op 的字段（如 `net: NetworkHandlers = NOOP_NETWORK`/`allianceHooks = {openManageAllies: async()=>{}, ...}`），装配壳在真正的兄弟实例构造完毕后**立即**（早于任何真实渲染/交互）用真实实例整体覆盖该字段。两者本质相同（都是"先占位、后填充"），选哪个看调用点数量——只有一两个方法用①（参数化最直接），一整个接口的多个方法用②（省得给每个方法都开一个闭包参数）。
  - **Core 本身仍超 500 行时的二次拆分**：把 `render()` 派发移出 Core 后仍超限（`SectScene`/`FriendsScene` 都踩到），继续拆成两类纯 form①：**`types.ts`**（纯类型/接口声明，零逻辑，从 `core.ts` 里挖出来单独占一个文件，`core.ts` 里 `export type {...} from './types'` 保持既有 import 路径不变）+ **一个"chrome/header/lists"自由函数模块**（多个兄弟面板都会调的共享渲染原语，如 `FriendsScene/chrome.ts` 的 `addButton`/`scrollRegion`/`openHiddenInput`、`SectScene/header.ts` 的表头绘制）——**这类模块不在 Core 上留任何委托方法**，所有调用方（包括 Core 自己剩下的方法、以及每个兄弟面板）改成直接 `import { addButton } from './chrome'` 后 `addButton(core, ...)` 显式传 `core`，因为这些函数本来就没有"只有 Core 自己会调"的调用点需要保留兼容委托（跟 `DefenseEditorScene/grid.ts` 那种"Core 域内其它方法还在调它，需要留一层委托别名"的情形不同）。**服务端经验在 client 不成立的两点**：①`tsconfig.json` 是 `target: ES2020`（`useDefineForClassFields` 关闭），继承→组合转换踩 field-initializer 读 `this.core` 的坑不会被 `tsc` 报错拦下（服务端 ES2022 会报 TS2729），只会静默变 `undefined`——转换后必须手工复核有没有 `= ...this.xxx...` 形式的字段初始化器；②改链会牵连测试接线：`test/` 下有测试直接 `XMixin(FakeBase)` 或 `Object.create(XxxBase.prototype)` 构造被测对象，链改判后要跟着改成 `new XxxDomain(core)`（往往还需要把测试里 `vi.spyOn(scene, 'methodName')` 改成 `vi.spyOn(scene.core, ...)`/`vi.spyOn(scene.xxxPanel, ...)`，直接 cast `scene as unknown as {...}` 的字段路径也要跟着改，同一份 `internals(scene)` helper 模式贯穿多个测试文件时改一次即可），且 `client-modules.md` 第 18 条记录过的坑同样适用——挪文件后必须 `grep -rn "vi.mock" client/test` 复核路径字符串不会跟着 import 自动改。
- **⚠️ 2026-08-13 CI 门禁复查：11 个普通大文件从未被纳入过审计（不是 mixin 链，也不在①候选清单里），baseline 里躺了 2+ 天没有任何理由**：`SettingsScene.ts`(852)/`BoardView.ts`(850)/`UnitView.ts`(843)/`DailyScene.ts`(711)/`RoomScene.ts`(569)/`ApiClient` 等已判例外之外的 `StickmanRuntime.ts`(535)/`LoginScene.ts`(531)/`HUDView.ts`(587)/`HandView.ts`(511)/`TutorialDirector.ts`(504)/`worldmap/WorldMapNet.ts`(745) ——这批文件的 baseline 条目此前只是一个裸数字，`file-length-baseline.json` 的 `_readme` 说明里从未单独提到它们，"仍待处理"这条也只是笼统指向"baseline JSON 剩余条目"，没有逐条核实。**根因修复**：baseline schema 改为 `{"path": {"lines": N, "reason": "..."}}`，`checkFileLength.mjs`（client/server/tools 三份合并为仓库根 `scripts/checkFileLength.mjs` 一份，各自留薄 wrapper）新增两条硬规则——条目必须有非空 `reason`，`lines` 超过 800 行硬顶直接拒绝进 baseline（不允许"例外"这个选项，只能拆）。这 11 个文件本轮直接按 form①/②拆分处理（见下方各自条目），未被拆的只保留 7 条真正有书面理由的例外（`app.ts`/`SaveManager.ts`/`ApiClient.ts`/`WorldApiClient.ts`/`CardScene/core.ts`/`EquipmentScene/core.ts`/`FriendsScene/core.ts`）——**其中 `FriendsScene/core.ts` 已于 2026-08-20 摘除**：社交页签卡顿修复本来会把它推到 650 行，改为挖出 `repaint.ts`（增量重绘状态+方法）与 `input.ts`（指针分发）两个接缝后回落到 499 行，baseline 条目随之删除（现存 6 条例外）。同时 `CityScene/core.ts`(771) 复议后判定"无自然切分点"不成立（5 个 async 网络动作 + 5 个纯 helper 函数其实都有先例可抄），一并从例外名单摘除、重新拆分。
  - **收尾（同日）**：上述 11 个文件 + `CityScene/core.ts` 全部拆完——`DailyScene`/`SettingsScene`/`RoomScene`/`StickmanRuntime`/`LoginScene`/`HandView`/`TutorialDirector` 7 个走 form①（各自挖出 `panels.ts`/`views.ts`/`forms.ts`/`cellDraw.ts` 等纯函数兄弟模块）；`HUDView`/`UnitView`/`BoardView` 3 个同样按 form①（`hpBar.ts`/`assets.ts`+`effects.ts`+`gear.ts`/`bases.ts`+`highlights.ts`+`effects.ts`）；`worldmap/WorldMapNet.ts`（745→99）挖出 `worldmap/net/{errors,loaders,march,structures,push}.ts`，`pendingTeamIds` 这个真正的实例级守卫状态留在 `WorldMapNet` 上、显式传参给 `march.ts`；`CityScene/core.ts`（771→481）挖出 `actions.ts`（5 个 async 网络动作）/`data.ts`（初始数据 fan-out）/`helpers.ts`/`icons.ts`——过程中抓到一个新坑：`artHost()`/`data.load()` 的 host 若把 `destroyed` 当**构造时快照的布尔值**塞进去，而消费方是在纹理 `'loaded'` 回调或 Promise `.then()`/`.finally()` 里异步读它，就会读到永远为 `false` 的旧值（场景已销毁但仍 `render()`）——必须写成 `get destroyed() { return core.destroyed; }` 活读；改完后 `grep -rn "destroyed: this\.destroyed\b" src/` 复核了本轮所有 host builder，确认无第二处。伴随做了三项收尾：①删除确认零引用的死代码（`game/balance/unitCards.ts` + 5 个标注"Legacy module — dead code, not compiled by webpack"的 `assetsManager/*.ts`/`inputSystem/uiElement.ts` 存根）；②比照服务端"撞线未超限也提前拆"的先例，抢先拆了 4 个 486-497 区间的文件——`net/anomaly.ts`(492→35，四个既有小节按原有分节直接搬进 `net/anomaly/{reporter,anrContext,crashSentinel,watchers}.ts`，全是模块级状态、不需要 host，`net/anomaly.ts` 退化成纯 re-export 聚合层，`longTaskObserverActive` 靠 ES module 的 live binding 特性直接读，不需要 getter)、`BattlePassScene.ts`(492→458，挖出 `nav.ts` 的 `drawSidebar`/`contentBounds`)、`CardScene/feed.ts`(492→457，`openFuseSelect` 内部全是互相捕获的闭包局部状态、大部分挖不动，只挖出唯一真正纯函数的 `findAutoTarget` 排序逻辑到 `feedAutoTarget.ts`)、`GameRenderer/input.ts`(489→422，`updatePlacementHighlights`/`meteorTargetUnits`/`columnTargetUnits` 只碰 `this.core` 不碰 `InputPanel` 自身状态，搬进 `placementHighlights.ts` 时直接传 `core: GameRendererCore` 整个对象、不建窄 host，同 `CardScene/feedList.ts` 的 `drawFuseCandidateRow(core, ...)` 先例)；③`tools/` 首次接入检查后发现的 `animator/src/io/IOController.ts`(771) 按四个既有小节拆成 `io/{fileIO,clipSerialization,editorProject,taoExport}.ts`（详见 `animator.md`）。全程 `npm test`(164/1310)+`npm run test:ui`(176/1588)+`npm run build:web` 反复回归，White-box 测试（`(x as any).privateMethod()`/`vi.spyOn(instance, 'method')`）遇到两处需要改法：`worldMapNetActions.ui.ts`/`worldMapSiegeResultToast.ui.ts` 的 `vi.spyOn(net, 'loadMapViewport'/...)` 因为方法变薄委托、被同模块内其它函数直接调用（绕过实例）而失效，改成 `vi.mock('.../net/loaders', ...)` 模块级 mock；`laneHighlightMirror.ui.ts` 的 `(bv as any).laneRect(col)` 改成直接 `import { laneRect } from '../../src/render/BoardView/highlights'`。
- **2026-08-18 `CardScene/feed.ts` 二次拆分（融合面板目标意图重设计，[ADR-068](../design/DECISIONS_ADR-041-069.md)）**：上一条里"只挖出 `findAutoTarget` 到 `feedAutoTarget.ts`"已作废——**`feedAutoTarget.ts` 已删除**。融合面板的状态机重写后（缺口态 / 备料下钻 / 推荐条，规格见 `design/game/CHARACTER_CARDS_DESIGN.md §3.2`）`feed.ts` 一度涨到 607 行，按 form① 拆成四个兄弟：`feedPlan.ts`（纯规划/排序：`listFusableTargets` 承接原 `findAutoTarget` 的排序、`planPrep`/`pickFeeder`/`countPrepRounds`/`autoFillMaterials`）、`feedGap.ts`（面包屑 / 缺口通知 / 批量按钮 / 推荐条 / Fuse-Cancel 页脚）、`feedRing.ts`（环形本体 + 槽位命中）、以及既有的 `feedAnim.ts`/`feedList.ts`，`feed.ts` 回落到 489 行。**`core.ts` 同批从 519 降到 503**：新增 `doPrepBatch` 惰性钩子（同 `doFuse` 手法）会让它逆着 baseline 长，所以把两个与实例状态无关的纯函数 `sortCards`/`injuryCountdown` 外移到 `cardSort.ts` 并原地 `export {...} from`，导入方零改动——baseline 里"`core.ts` 无自然切分点"那条理由说的是**那个内聚状态对象**，不含这两个搭便车的自由函数。
- **2026-08-25 `CardScene/core.ts` 三次拆分 + `app/nav/game/campaignRoster.ts` 二次拆分（装备页改 overlay，[ADR-072](../design/DECISIONS_ADR-070-onward.md)）**：两个文件改前都卡在 490–500，加上 overlay 需要的 `pause()`/`resume()` 就双双越线。`core.ts`（558→476）挖出 **`CardScene/input.ts`**：`subscribeInput`/`unsubscribeInput` + `handleDown`/`handleMove`/`handleUp`/`inRect`，form① 自由函数吃 `core`（同 `EquipmentScene/cells.ts`、`FriendsScene/input.ts` 手法）——**状态一律留在 Core**（`gesture`/`modalSliders`/`activeModalSlider`/`scrollDirty`/`feedScroll*` 随之从 `private` 提升为公开字段，符合 core.ts 头注释"所有实例字段公开"的既定约定），只搬处理函数。`campaignRoster.ts`（537→461）挖出 **`app/nav/game/equipmentActions.ts`**：`craft`/`enhance`/`salvage`/`equip`/`reforge` 五个回调 + `equipErrKey`，判据是"这块从来不碰 nav 闭包"——只用 `client`+`saveManager`+`analytics`，留在原处的才是真·导航（挂哪个场景、带哪些 peer 页签、`back` 去哪）。
  - **`Scene.pause()` 的通用坑：别把所有订阅塞一个数组**。CardScene 原本 input 四条 + `cb.onSaveChanged` 共用 `unsubs`，pause 时一把梭会让 overlay 里的每次 equip 都刷不进底下的场景。拆成 `inputUnsubs`（pause/resume 收放）与 `unsubs`（只在 destroy 收）。**任何要加 `pause()` 的场景都要先看这一眼**——WorldMapScene 当初没踩到只是因为它的 SLG 推送不走 InputManager。
  - **被盖住时不渲染**：装配壳 `render()` 在 `core.destroyed` 之后加一道 `core.paused` 门（置 `pendingRender` 返回），`resume()` 补渲一次。overlay 的每个动作都 ping `onSaveChanged`，不拦就是每件装备在不可见的场景上重建一次整页。
  - **`ui/scrollTapGesture.ts` 新增 `cancel()`**：pause 掉指针订阅后，本该结束手势的 `up` 永远不会到，脏 pending tap 会在 pop 之后的下一次无关抬手上误触发。
  - **`test/input-subscription-cleanup.test.ts` 的正则放宽为 `[\w.]*[Uu]nsubs`**：它原先要求字面量 `unsubs.push(`，`inputUnsubs.push(` 直接判红。守的是"unsub 存下来且在某处排空"，不是某个标识符名。
- **2026-08-24 `CardScene` 三次拆分（卡背包渲染性能，见 `design/game/CHARACTER_CARDS_DESIGN_IMPL.md §10.5`）**：拖动列表每帧全量 `render()`、每帧重建 105 个 `PIXI.Text` 的性能修复顺带把三个文件推过 500 行，全部按 form① 拆掉，**`core.ts` 的 baseline 例外条目随之删除**（现存 3 条）：①`list.ts`(566→348) 挖出 `rosterCell.ts`（`renderCardCell` + `cellSignature` + `statRow`）——**这两半必须放在同一个文件里读**，因为增量网格「签名不变就不重绘」的契约要求"格子新画了什么就得同步进签名"，分开放等于把契约的两端拆到两个文件；②`detail.ts`(528→485) 挖出 `detailPortrait.ts`（`drawDetailFace`/`flipDetailPortrait`，都只操作调用方交给它的一个 container，不碰弹窗布局和命中矩形）；③`core.ts`(550→462) 挖出 `types.ts`（`CardCallbacks`/`Card*Result`/`Rect`/`Hit`/`DoFuseFn` 等类型 + `MODAL_DIM`/`CARD_CELL_H` 等常量，抄 `EquipmentScene/types.ts` 的先例，`core.ts` 原地 `export ... from './types'` 所以导入方零改动）——baseline 里"`core.ts` 无自然切分点"那条理由说的是**那个内聚状态对象**，一百行纯声明不在其内，同 08-18 挪走 `sortCards`/`injuryCountdown` 的判断。
- **`render/fastText.ts`（2026-08-24 新增，全局可复用的文字快路径）**：`cachedTxt()`（有界字符串集 → 每个 (串,字号,颜色,粗细) 只光栅化一次，LRU 320）+ `numTxt()`（数字/ASCII → 预烘字形图集拼 sprite，零光栅化且与面板边框合批）。**两个必须知道的坑**（细节和实测数据见设计文档 §10.5）：`PIXI.Text.destroy()` 即使传 `texture:false` 也会把自己的 canvas 宽高清零，所以"取走 texture 再销毁 Text"会渲染成纯黑块——缓存必须持有整个 `Text`；字形图集**必须按最终颜色**烘，白色图集 + tint 会因为 Chrome 按填充色做字形 gamma 校正而明显偏重（实测墨量 +11%）。无 bake renderer（headless 测试）时两者都退化成普通 `txt()`，所以 `*.ui.ts` 里遍历 `PIXI.Text` 的断言不受影响。
- **2026-08-24 `EquipmentScene/core.ts` 挖出 `headerRow.ts`（页头货币簇 + 材料带，form①）**：起因是页头标题/金币重叠修复（见 `design/game/LOBBY_IA_REDESIGN_LOG.md §26`）新增了一个 `headerCurrencySpec()` 方法把 core.ts 推到 549 行、超过 baseline。`renderHeaderCurrency`/`renderMaterialsBand`/`headerCurrencySpec` 三者归在同一个文件而不是各自成模块，因为它们共享同一个前提——**材料标签放不下这一行，所以才另开一条带**；`headerCurrencySpec` 又是 `core.build()` 量预留和 `renderHeaderCurrency` 画簇**唯一共读的那一份**，拆开就是让预留和实际宽度悄悄漂移。**没有留转发方法**（同 `FamilyScene/header.ts` 先例），4 个调用点改为直接 import 自由函数；core.ts 回落 489 行，**baseline 例外条目随之删除**（现存 3 条：`app.ts`/`SaveManager.ts`/`ApiClient.ts` 一族）。
- **⚠️ 2026-08-17：`src/app.ts` 的"薄转发壳"例外作废，改判为拆分**：commit `132a83e9`（首屏加载门禁：preload hints / L0 分层 / L1 idle prefetch）往 `startApp()` 里加了 boot 资源门禁接线，把 `app.ts` 顶到 623 行、在 `npm run check:filelength` 上撞出 GREW（baseline 614）。复议后判定 2026-08-11/08-12 记的那条例外**只对了一半**：「再拆转发列表只会碎片化」成立，但真正的切分点根本不在转发列表*里面*，而在它**和 `startApp()` 之间**——`PixiAppViews`（~35 个 `showX` 转发）与 `startApp()`（PIXI runtime + MemoryMonitor/PerfMonitor/anomaly 三个看门狗 + L0 资源门禁 + GlobalToast/Appeal/Feedback 三个 stage 级弹窗接线）除了构造器那几个运行时句柄（`platform`/`app`/`scaling`/`manager`/`input`/`layout`）之外**没有任何共享状态**，是标准 form② 抽取。类整体搬到 `src/app/PixiAppViews.ts`（403 行，与 `AppViews.ts`/`createAppCore.ts`/`battleGate.ts` 同目录）并改为 `export`，`app.ts` 只剩 boot 序列（239 行），baseline 里随之摘除该条目（现存 6 条例外）。`src/entries/*` 的 `import { startApp } from '../app'` **路径不变**（`app.ts` 优先于 `app/` 目录解析），零调用点改动。**教训**：baseline 的 `reason` 只证明"当时那个切法不划算"，不等于"这个文件没有切分点"——GREW 撞线时先重新找切分点，别照着旧 `reason` 续命把 `lines` 往上抬。**同时补了 `test/ui/pixiAppViews.ui.ts`（13 例）**：这个类此前**零覆盖，而且不可能有覆盖**——它是 `app.ts` 里一个没有 `export` 的局部类，只有跑完整个 `startApp()` 才够得着；拆出来才第一次可测。断言的都是这个类自己拥有的接线（不碰场景内部，5 个用到的场景类 + `battleAssets` 全部 mock，真构造由各自的 `*.ui.ts` 冒烟）：①大厅 `resize` 监听的生老病死（`showLobby` 订阅、任何非大厅屏 `leaveLobby` 退订、overlay 挂载**不**退订）；②resize 驱动的重建即使调用方要求 `fade` 也必须瞬切（`!this.resizing &&` 那半个条件）；③`mountSlg` 的 `pushOverlay` vs `goto` 分支 + `hideOverlay`；④`timedBuild` 往 `recordConstructSample` 报名字；⑤`showGameNet` 给 joiner 建 `Side.Top` 翻转 layout、且资源门禁未开时到达的服务器推送会被 `DeferredSceneCalls` 缓冲后按序回放。**逐条做过变异验证**（7 处人为改坏源码：删 `leaveLobby`、去掉 `!this.resizing`、`mountSlg` 恒 `goto`、`netLayout`→`this.layout`、世界地图丢 `fade`、`recordConstructSample` 变哑、`deferred.resolve` 不传 scene）——**7/7 全部被测出**，不是只跑绿的摆设。测试里 `.at(-1)` 用不了（本项目 `lib` 是 ES2020），用文件内 `last()` helper 代替。
- **⚠️ 渲染层销毁契约（防内存泄漏）**：每个战斗视图（`GameRenderer` 及子视图 `BoardView`/`UnitView`/`BuildingView`/`HandView`）**必须**实现 `destroy()`：①注销所有挂在 `PIXI.Ticker.shared` 上的一次性特效 tick（`Ticker.shared` 是 GC 根，漏一个就钉住整局场景图+纹理）；②`pool.drain()` 销毁池内已 `removeFromParent` 的游离对象；③`container.destroy({children:true})`。**共享纹理（spritesheet/`bake()`/`Texture.from`）只解引用不销毁。** 详见 **[`client-memory-leak.md`](client-memory-leak.md)**（事故复盘 + 完整契约 + heap snapshot 验证法）—— 2026-06 一次因六个视图全无 `destroy()` 导致每局退场泄漏整张场景图、2 小时涨到 16GB 的事故。
- **📁 目录组织（2026-08-02 整理，改文件位置前先读这条）**：
  - **`client/src/game/` 只放客户端独有逻辑**（`meta/`、`replay/`、`campaign/levels` 数据、`balance` 定义、`NetInputSource`、`index.ts` 公共出口）。确定性引擎在 **`@nw/engine`**（= `server/engine/src/`），**直接 import**——原先 30 个 `export * from '@nw/engine/*'` 的 3 行转发壳（`Board`/`GameEngine`/`config`/`types`/`systems/*`/`math/*` 等）已于 2026-08-02 删除，因为它让 `client/src/game/` 与 `server/engine/src/` 目录树长得一模一样、读者分不清哪个是真源码。`@nw/engine` 在 `tsconfig.json`、`webpack.config.js`、`vitest*.config.ts` 三处都配了同名 alias，加 import 不需要额外接线。
  - **`render/` = 渲染本身**：战斗视图（`BoardView`/`UnitView`/`HUDView`…）、主题/字号/文本度量、`sketch*` 手绘原语，以及子目录 `atlas/`（**全部** `*Atlas`/`*AtlasLoader`，含 `spriteAtlas`/`iconsAtlas`/`worldAtlas`/`decorMergedAtlas` 四张 L0 底图）、`icons/`、`vfx/`、`stickman/`、`GameRenderer/`。
  - **`ui/` = 交互组件**：`dialogs/`（`confirmDialog`/`AppealDialog`/`ConsentDialog`/`ReconnectPromptDialog`/`ProfilePopup` 等模态浮层）、`widgets/`（`SceneHeader`/`HubTabs`/`CareerTabs`/`socialTabRail`/`chatRow`/`hudButton`/`ScrollIndicator`/`uiCache`…）、根目录放全局浮层与输入工具（`GlobalToast`/`LoadingOverlay`/`busyTracker`/`wheelScroll`/`scrollTapGesture`/`inputDisplay`）。**判断标准是职责不是历史**：新写的对话框/可复用控件进 `ui/`，别再往 `render/` 根目录堆。
  - **⚠️ 移动文件时 `vi.mock()` 的路径不会被"改 import"顺手带到**（2026-08-02 实测踩过）：`vi.mock('../../src/render/xxx')` 里的模块路径既不是 import 语句、也不参与类型检查，`tsc --noEmit` 全绿但 `npm run test:ui` 当场挂 5 个文件。移动 `render/`/`ui/` 下的模块后，**必须**额外 `grep -rn "vi.mock" client/test` 核一遍。
- **⚠️ 高频 `render()` 场景的文本纹理销毁（2026-06-27 事故）**：画布场景用「`container.removeChildren()` + 全量重画」模式重渲。`removeChildren()` **不销毁**子对象——而 `sketchUi.txt()` 每次 `new PIXI.Text`，**每个 Text 自带一张 GPU 纹理**，孤儿纹理要等 PIXI 纹理 GC（默认 ~60s）才回收。`LoginScene` 尤其致命：**每次按键** + 光标闪烁 **每 0.5s** 都全量 render，连打字时纹理瞬间堆满 → iPad Safari WebGL 内存预算极小 → 上下文丢失 → Safari 刷新页面（用户反馈「注册输昵称一直崩溃」）。**修复契约**：用 `sketchUi.tearDownChildren(container)`（共享单一实现）代替裸 `removeChildren()`——遍历返回的子对象，`PIXI.Text` 走 `destroy({texture:true,baseTexture:true})` 释放自有纹理，其余（Graphics/包 `bake()` 共享纹理的 Sprite）走 `destroy({children:true})`（缺省 `texture:false`，**不碰共享烘焙纹理**）。
  - **关键依赖（已被测试钉死）**：`destroy({children:true})` 会递归销毁子树，故**嵌套**的 Text（如聊天气泡 `container→layer→node→body`）也会被命中——`PIXI.Text.destroy()` 会与它自带的 `defaultDestroyOptions(texture:true,baseTexture:true)` 合并，即便经 `{children:true}` 递归也会释放自有纹理；而嵌套的非 Text Sprite 因 `texture` 缺省 false 保住共享纹理。PIXI 升级若改动该默认值会悄悄复发泄漏。
  - **审计结论（2026-06-27）**：命中并已套用契约 = `LoginScene`（逐键+光标 0.5s）/ `ChatScene`（逐键+光标 0.5s，含嵌套气泡）/ `ShopScene`（充值码逐键+光标 0.5s+按钮补间逐帧）/ `SettingsScene`（改名逐键+光标 0.5s）/ `SectScene`·`FamilyScene`（创建表单逐键→`bodyLayer`）/ `WorldMapScene`（HUD 每 ~5s 行军轮询 + 行军面板每 1s 倒计时）。**豁免** = `AuctionScene`：input 仅存字符串不触发 render，`update()` 仅 toast 计时，render 全是事件驱动（切页/滚动/拉取），无逐键/逐帧/定时 Text 重建。`WorldMapScene` 的 `renderMapL3`/`renderOverlay` 用单个 Graphics + `g.clear()`、无 Text，不在此列。
  - **回归测试**：`client/test/ui/textTeardown.ui.ts`（headless PIXI，`npm run test:ui`）—— 锁定顶层/嵌套 Text 释放、共享 bake 纹理保留、裸 `removeChildren()` 确实泄漏、ChatScene 反复重渲不跨代堆积。
- **⚠️ 菜单场景生命周期契约（2026-07-07 事故：切换 UI 偶发 `transform is null` + 整页卡死，只能刷新）**：根因是 PIXI7 `Ticker._tick` 在任一监听器抛异常时**中止 update 循环且不再 `requestAnimationFrame`** → 整个 canvas 永久冻结。三条防线：
  - **`SceneManager.onTick` 用 try/catch 包 `scene.update()`**（每场景只记一次错，避免刷屏），`goto()` 亦 try/catch 包 `prev.destroy()`——一次抛出不再能杀死主 ticker / 阻断切场景。
  - **`InputManager` 分发用快照 + 逐 handler try/catch**：旧 `list.forEach(f=>f())` 里一个 handler 抛异常会连坐其后所有 handler（含当前场景的）→ 表现为「点啥都没反应」。
  - **每个 `Scene.destroy()` 必须 `container.destroy({children:true})`**（此前约 20 个菜单场景只 `unsubs.forEach()`，泄漏每个子对象的 `Ticker.shared` 闭包——boil 标题/建筑/单位特效——跨导航累积拖垮帧率）；**异步重绘（`fetch`/纹理 `once('loaded')` 回调）须以 `destroyed` 标志兜底**（`render()` 首行 `if(this.destroyed)return`，是覆盖所有重绘入口的唯一节流点）。`StatePlayerScene` 另需显式 `boardView/unitView/buildingView.destroy()`（各自注销 `Ticker.shared` 特效 tick）。
  - **⚠️ 复发（2026-08-15，Playwright 打 web-e2e 构建实测）：`IntroScene`/`IllustratedInterludeScene` 的插画 `baseTexture.once('loaded', fitIllustration)` 漏了这层兜底**——冷缓存下快速点穿开场白，纹理解码晚于场景销毁，回调对已 destroy 的 Sprite 取 `.scale` 抛 `TypeError: Cannot read properties of null (reading 'scale')`，从 PIXI Runner 冒进 ticker → 整页永久冻结（同 07-07 事故的表现）。修法是**两道**：回调首行 `if (this.destroyed) return;`（两个场景新增 `destroyed` 字段，`destroy()` 里置位），且把 `base.off('loaded', fitIllustration)` 塞进 `unsubs` 以便 `destroy()` 直接摘钩。**顺带全量 grep `baseTexture.once('loaded'`（19 处）逐个核对**，另补两处同类缺口：`CardScene/feed.ts` 的 `drawFusePanel`（既是 `core.feedRedraw` 又是融合环卡面的 loaded 回调，`destroy()` 不会把 `feedRedraw` 置 null）首行加 `if (core.destroyed) return;`；`FriendsScene.ts` 的 `render()` 首行加 `if (core.dead) return;`（此前只有 `endRender` 自守 `dead`，`beginRender` 会对已销毁容器 `removeChild`/`tearDownChildren`）。其余 15 处均已经由各自 `render()` 的 `destroyed` 首行守卫覆盖；`render/HandView/cellDraw.ts` 的回调只写普通字段（`lastSyncKey`/`slotContentKey`），不碰 PIXI 对象，不会抛。
  - **回归测试**：`client/test/ui/sceneManager.ui.ts`（假 ticker 步进：update 抛错被吞、destroy 抛错不阻断切场景）+ `client/test/inputManager.test.ts`（一个 handler 抛错不连坐其余）+ `scenes.ui.ts` 的 `exercise()` 现断言 **每个场景 `destroy()` 后 `container.destroyed===true`**——任何将来忘记拆容器的场景会当场挂测；`client/test/ui/storySceneLateTextureLoad.ui.ts`（2026-08-15 新增）则针对上面这条复发：`destroy()` 之后手动 `base.emit('loaded')` 断言不抛、且 `listenerCount('loaded')===0`（并先断言 `base.valid===false`，避免将来资源桩把纹理变成同步 valid、测试静默失效）。
  - **静态守门（2026-08-16 新增，`client/test/textureLoadedGuardCallSites.test.ts`）**：上面那些都是「一处 bug 一个测试」，这个文件改成钉形状，兄弟测试是 `input-subscription-cleanup.test.ts`。两组断言：①扫全 `src/` 的 `.once('loaded', …)` 调用点，每个必须落进「只 resolve promise / 回调体内有 `if (…destroyed|dead…)` / 委托 `core.render()`|`this.render()` / 在文件内 allowlist 里带人工复核理由」四类之一，新增调用点不复核就挂；②`src/scenes/**` 里**每个** `render(): void` 定义头 8 行必须有守卫，**无 allowlist**（契约原话「唯一节流点」的直接编码，且覆盖纹理扫描看不见的 fetch/await/onSaveChanged/timer 重绘入口）。②落地时当场揪出两个漏网场景 `SectScene`/`TitlesScene`（都还没有异步重绘入口，属于测试跑在 bug 前面）已一并补上。
    - **⚠️ 写这类源码扫描测试的坑**：本检出是 **CRLF**，而 JS 的 `.` 不匹配 `\r`，所以 `split('\n')` + 带 `$` 锚点的正则会**每一行都匹配失败**——扫描结果恒为空，`expect(offenders).toEqual([])` 永远绿，等于测了个寂寞。必须 `split(/\r?\n/)`，并且**给每组扫描配一条「扫到的数量 > N」的 canary 断言**（本文件里就是它第一个报红抓出了这个问题）。既有的 `input-subscription-cleanup.test.ts`/`no-debug-hooks-in-src.test.ts` 因为正则里没有 `$` 锚点而侥幸没踩到，不代表这个坑不存在。
  - **运行时兜底（2026-08-16，`client/test/ui/deferredRedrawAfterDestroy.ui.ts`）**：静态扫描只证明守卫「写了」，这个文件证明它「拦住了」——destroy 之后真的去调 `CardScene` 的 `core.feedRedraw`（即 `drawFusePanel`，`destroy()` 从不置 null）和 `FriendsScene` 的 `core.render()`。**断言的是「重绘没产出任何东西」，不是「没抛」**：删掉守卫两条都不抛，写成 `not.toThrow()` 的话整个文件就是空转（第一版正是这么写的，删了守卫照样绿）。改成断言容器子对象数仍为 0 后，删守卫会立刻报 23 / 16 个子对象被塞回已销毁的容器。
    - **⚠️ 这条测量结论值得单记，别凭直觉**：PIXI7 `Container.destroy()` 是把 `children` **清空**而不是置 null，所以对已销毁容器做 `addChild`/`removeChild`/`tearDownChildren` **都不抛**；真正抛的是读已销毁 display object 的 transform 派生属性（`sprite.scale`/`.x`/`.rotation`，getter 走 `this.transform.scale` 而 `transform` 已是 null）。所以 25 个 `once('loaded')` 调用点里只有两个 story 场景是「能冻 canvas 的」；`FriendsScene.render()`/`feed.ts drawFusePanel`/`DefenseEditorScene.render()` 这几处缺守卫只是**白跑一次完整重绘 + 搅动纹理**，值得修但严重性不同级，别混为一谈。
  - **`IntroScene` 行为覆盖（2026-08-16，`client/test/ui/introScene.ui.ts`，12 条）**：此前 `IntroScene` 只有 `scenes.ui.ts` 的启动冒烟，而 `illustratedInterludeScene.ui.ts` 开头就声明自己「只测相对 IntroScene 新增的部分」——两边都以为对方覆盖了，实际是空的。现补齐：逐行 fade/自动前进/堆叠、skip 按钮 `skipped=true` vs 读完 `skipped=false`、插画 alpha 跟 `story.line.3` 同步且停在 0.6（不是像 interlude 那样到全不透明）。**最要紧的一条是「最后一行永不自动前进」**——它下游接的是 consent/隐私门（`auth.ts` 的 `gateConsent`），自动前进会让结尾一闪而过；已通过移除 `isLastLine` 例外验证该断言会红。
- **⚠️ 场景淡入淡出切换 + 输入冻结契约（2026-07-11 事故：大厅「Store」进商城→点「Shop」页却跳去「Career」；2026-07-12 收窄：淡出淡入不是所有场景切换都需要，子标签/普通返回不该有动画）**：`SceneManager.goto(scene, opts?)` **默认同帧 instant 切换**（不传 `opts` 或不传 `fade`）——覆盖绝大多数导航（大厅各 tab、返回按钮、二级页面）。仅显式传 `{fade:true}` 才走 cross-fade（纸色调 `0xfaf6ee`、峰值透明度 0.92，非早期版本的纯黑全遮罩；fade-out 90ms → swap → fade-in 180ms，`easeInOutQuad`）。目前只有 4 处调用点传 `{fade:true}`：`app.ts` 的 `showGame`/`showGameNet`/`showWorldMap`（进入对局/进入 SLG），以及 `nav.goLobby({fade:true})` 的调用方——`game.ts`（对局放弃/教程完成跳过）、`result.ts`（净斗放弃/结算页返回或再来一局默认落地大厅）、`world.ts`（SLG 地图退出）。新增「进入/退出对局或 SLG」以外的转场**不要**加 `{fade:true}`。淡入期间**新场景在 `new XxxScene()` 时即已构造并订阅输入**，旧场景要等 fade-out 结束的 swap 才 `destroy()`——故约 270ms 内新旧两套 hit-rect **同时在线**。关键坑：**输入不走 PIXI**（`WebAdapter` 直接把 DOM `pointer*` 喂进 `InputManager`），所以纸色遮罩的 `eventMode='static'` 挡不住点击——淡入期一次点击会落到旧场景仍在线的 hit-rect（大厅底栏 Store 隔壁就是 Career）→ 误导航；**instant 切换完全不冻结输入**，只有显式 `{fade:true}` 才会启用下面这套门控。
  - **修复**：`SceneManager` 持有 `InputManager`（`InputGate` 接口），`startTransition` 时 `suppress(true)`、fade 完全结束（或 instant swap / 容错兜底）时 `suppress(false)`——`InputManager.dispatch` 前置 `if(suppressed)return` 丢弃期间所有指针事件。
  - **首击中止 fade**（避免 270ms 死区）：`suppress` 期间的 pointer-**down** 触发 `onSuppressedInput` 钩子 → `SceneManager.skipTransition()` 立即跳到目标场景并解冻；该 down 被吞不下发，且 `swallowNextUp()` 一次性吃掉配对的 pointer-**up**，使这次「中止点击」不会误点到刚挂载的新场景。
  - **回归测试**：`inputManager.test.ts`（suppress 丢弃三通道 / suppressed-down 触发钩子且不下发 / `swallowNextUp` 一次性 / suppress 期间 arm 的 swallow 跨解冻仍生效）+ `sceneManager.ui.ts`（默认 instant 同帧、`{fade:true}` 延迟 swap、instant 取消进行中的 fade、fade-out 中途 retarget、冻结跨 fade 后解冻、首击中止 + 真 `InputManager` 集成：中止后配对 up 被吞、下一击正常送达）。
- **⚠️ 输入订阅遗漏（永久泄漏，非上条的瞬时竞态）契约（2026-07-13 事故：社交页点返回偶发跳到 Career）**：与上条 2026-07-11 的 fade 竞态是**同一症状、不同根因**——`TitlesScene` 构造时直接 `input.onDown(...)`，**丢弃了返回的取消订阅函数**，`destroy()` 也只 `tearDownChildren(container)`，是全代码库唯一没走 `this.unsubs.push(...)` + `destroy()` 里 `unsubs.forEach(u=>u())` 标准模式的场景。后果：每进一次「称号」页就永久多挂一个绑定 Career 侧边栏 `[Stats|Titles|Achievements]` hit-rect 的 down 监听——`InputManager.dispatch()` 广播给**所有**订阅者而非只当前场景，所以之后任何一次点击只要坐标落在那个失效 rect 里（哪怕当前显示的是别的场景的返回按钮），都会额外触发 `onOpenStats()`，与真正点击的场景导航同帧抢跑，最终谁的 `goto()` 后调用谁赢。**排查手法**：在 `_emitDown`/`_emitUp` 里打印 `downs.length`/`ups.length`（正常应恒为 1），`>1` 直接实锤泄漏，比逐场景 code review 快得多。**修复**：给 `TitlesScene` 补齐标准 `unsubs` 数组 + `destroy()` 清理。**教训**：第 21 条只保证了 `container.destroy({children:true})`，没单独强调「每个 `input.onX()` 的返回值都必须存起来并在 `destroy()` 里调用」——两者是独立契约，容器销毁不会自动带走 `InputManager` 的订阅，新增场景时两条都要抄。**审计（2026-07-13）**：逐一核对全代码库约 28 处 `onDown`/`onMove`/`onUp` 调用点，除 `TitlesScene` 外全部规范，未发现同类泄漏。**回归测试**：`client/test/input-subscription-cleanup.test.ts` 静态扫描 `client/src` 全部源文件——任何 `.onDown(`/`.onMove(`/`.onUp(` 调用若不是 `unsubs.push(...)` 包裹的形式即判失败（用原始泄漏那行代码验证过确实会挂测），比逐场景 review 更快锁死这条契约。
- **⚠️ Career 侧边栏 tab 切换的 back 语义（2026-07-13 事故：Career 页点 Titles 标签再点返回，落到 Career/Stats 标签页而非大厅）**：`Stats/Titles/Achievements` 是同一 Career hub 的对等 tab（`ui/widgets/CareerTabs.ts`），不是导航栈——但 `app/nav/game/career.ts`（2026-08-12 从 `app/nav/game.ts` 拆出，见本文件「单文件 500 行收敛」条）里 `goStats`/`goAchievements`/`goTitles` 互相切 tab 时曾各自硬编码 `goTitles(goStats)`/`goTitles(back)`（未传）等写法，把 back 目标写死成"返回 Career 根（Stats）"，导致从任一非根 tab 返回都要多点一次才能出 hub。**修复**：三者统一改为把调用方传入的 `back` 闭包原样透传给对等 tab（`goStats(back)`/`goTitles(back)`/`goAchievements(back)`），只有独立入口（大厅直接进 Achievements、无参 `goTitles()`）才默认落到 `goLobby`/`goStats`。**回归测试**：`client/test/careerNav-backNavigation.test.ts`（Stats→Titles/Achievements 切 tab 后 back 直达大厅、Achievements→Titles 两跳仍透传同一 origin、无参 `goTitles()` 仍保留退到 Stats 的默认行为）。
- **⚠️ StatsScene 竖屏单列布局没有滚动，超屏内容不可达（2026-08-11 用户反馈：Career 页竖屏下拉不动）**：`StatsScene`（大厅底栏「生涯」入口，Career hub 的 Stats 根 tab）render() 里 landscape/portrait 只是排布分支——横屏两列各自较矮，天然不超一屏；竖屏把 ranked/campaign/collection/match-history 四段纵向堆叠，构造函数却只订阅了 `input.onDown`，`render()` 也从未加 mask，超出屏幕高度的内容（尤其排最后的 match history，最多 10 行）既不裁切也摸不到——与 `battlePassScroll.ui.ts` 头部注释记录的 BattlePassScene 那次是同一类漏洞（"只接 onDown、没接 onMove/onUp/onWheel + mask"）。**修复**：portrait 分支改为 TitlesScene/CardCodexScene 同款——`body`+`bodyMask` 容器 + `scrollY`/`scrollMax`（渲染前用 `sectionHeight()`/`historyHeight()` 两个纯高度计算函数量出总内容高度，视口高度按 `bottomNavH(h)` 扣掉底部 Career 导航条）+ `drawScrollIndicator`；`handleDown/Move/Up/Wheel` 按 `this.landscape` 分岔——**横屏保持原有 onDown 立即派发不变**（本就不需要滚动，改成 onUp 延迟派发只会加时延），竖屏改用「按下记录起点→挪动超过 6px 才算拖拽/滚动→松手时未拖拽才派发点击」的标准手势（同 TitlesScene）。`drawSection`/`drawHistorySection` 新增可选 `parent` 形参（默认 `this.container`），竖屏传 `this.body`，横屏不传保持原样直画 `this.container`——两条布局分支除了这一处传参外互不影响。**回归测试**：`client/test/ui/statsSceneScroll.ui.ts`（竖屏内容超一屏时 `scrollMax>0`、横屏两列布局 `scrollMax` 恒为 0、竖屏拖拽改变 `scrollY` 且夹在 `[0,scrollMax]`、竖屏小幅抖动仍判定为点击而非滚动、横屏点击保留旧的「按下即触发」时延）。**2026-08-12 补充**：这次滚动修复把 `drawSection`/`drawHistorySection`/`sectionHeight`/`historyHeight`（连同它们唯一的私有 helper `truncate`）从 `StatsScene` 挪出，独立成 `scenes/StatsScene/panels.ts`（form①，同 `ResultScene/builders.ts`/`CampaignMapScene/drawing.ts` 先例）——四者本就不读 `this.cb`/`this.history` 以外的 `StatsScene` 状态，`parent`/`h`/`hits` 全部改成显式形参（`hits: Hit[]` 由调用方传入、就地 `push`），`StatsScene.ts` 因此从 510 行（跨过 500 行基线，`checkFileLength.mjs` 判定为 NEW 违规，见本文件「单文件 500 行收敛」条）回落到 343 行，未进 baseline（从未落地过，随手就改判过关）。
- **⚠️ 战令/聊天页面手机端整页崩溃重载（2026-08-12 用户反馈：手机打开战令页整个网页刷新）**：详细复盘见 [`claudedocs/client-memory-leak.md`](client-memory-leak.md) §9——`BattlePassScene`/`ChatScene` 的可滚动列表在 `render()` 里无条件全量构建所有列表项的真实 PIXI 显示对象（无视口裁剪），单帧内创建的 GPU 对象/纹理数超出移动端 WebView 预算，被浏览器判定为内存峰值直接杀掉整个标签页重载——与 `LeaderboardScene` 早前修复过的问题同源。**拆分产出两个新 form① 模块**：`BattlePassScene/rows.ts`（`RewardRowVirtualizer` 类，固定行高场景走 `builtRows: Map<number, Container>` 增量构建/销毁，同 `LeaderboardScene.updateVisibleRows()` 手法）——`BattlePassScene.ts` 借此从 557 行（跨过 500 行基线）回落到 492 行；`ChatScene/thread.ts`（`measureRows()`+`buildBubble()`，可变行高场景先用 `PIXI.TextMetrics.measureText()` 测量几何、不建纹理，再对视口内的行才真正调 `buildBubble()`）——`ChatScene.ts` 从 443 行降到 396 行（本就没跨线，纯粹顺手拆干净，避免这次改动后逼近基线）。**回归测试**：`client/test/ui/battlePassScroll.ui.ts`（`rowViz.size` 恒 < `BATTLEPASS_MAX_LEVEL`、Text 总数远低于未虚拟化上限、滚到底部首行销毁/末行构建、拖动全程不触发 `render()`）、`client/test/ui/chatThreadVirtualization.ui.ts`（200/600 条消息 Text 总数仍 <60、初始只见最新消息滚到顶才见最早消息、composer 光标闪烁的周期性 `render()` 同样不重建全量历史）。同批顺手排查了其余可滚动列表场景，`CardCodexScene`/`DeckBuilderScene`/`CityScene/render.ts` 有同款反模式但数据量当前被内容常量硬顶在 10~20 以内，风险等级起初判为中/低、本次未处理——**同日晚些时候用户要求补齐**，三处均已按同款裁剪修完，详见 [`claudedocs/client-memory-leak.md`](client-memory-leak.md) §9.6。`CardCodexScene` 那次顺手又拆出一个新 form① 模块：`CardCodexScene/tile.ts`（`drawCardTile`/`drawTileFace`/`drawArtFit`/`storyText`/`cardStats`/`drawStatChips`，art-load 去重集合和重渲染回调改成显式参数而不是读 `this`）——`CardCodexScene.ts` 改造中一度冲到 576 行（跨过 500 行基线），靠这次拆分回落到 385 行。
- **⚠️ 跨界面数据共享审计后修复（2026-07-29）**：客户端数据共享全面分析（详见 `design/game/META_DESIGN.md` §3.3「变更通知」段）发现两处具体问题并修：①`WorldMapContext`（`scenes/worldmap/WorldMapContext.ts`）的世界频道「已读」标记（`worldChatSeenKey`/`getWorldChatSeenTs`/`markWorldChatSeen`）此前直接调用全局 `localStorage`，跳过了 `IPlatform.storage` 平台抽象——微信小游戏运行时没有 `localStorage`，该功能在微信端实际不生效；改为通过 `WorldMapCallbacks.storage: IStorage`（`nav/world.ts` 注入 `platform.storage`）读写，两平台一致。②`SaveManager` 新增 `subscribe(listener): unsub` 发布订阅（每次 `persist()`/`reconcile()` 后同步通知），解决 `WorldMapScene` 与其 overlay（City/Friends/Sect/Auction）同时挂载时一方花钱、另一方余额显示不自动刷新的问题；`nav/*.ts` 把 `(fn) => saveManager.subscribe(fn)` 作为 `onSaveChanged` 注入到所有展示钱包/存档的界面，各自构造函数里 `unsubs.push(cb.onSaveChanged(() => this.render()))`，退订走标准 `unsubs`/`destroy()` 契约（同第 27 条的订阅泄漏契约，新场景照抄）。`WorldMapScene` 本身未接入（其 `getCoins` 唯一消费点是打开时才画一次的世界商城子 Tab，pull 式面板，接入 push 通知的改造收益不成比例）。**回归测试**：`client/test/save-manager.test.ts` 的 `SaveManager.subscribe` 描述块——多监听者独立通知、退订生效、`update`/`patchLocal`/`reconcile`/`setFlag`/`optimisticEquip`（`equipTitle`/`equipAvatar`）/`spendStaminaForLevel` 各写入点均触发；`client/test/ui/worldChatSeenStorage.ui.ts` 锁定「已读」标记走注入的 `storage` 而非全局 `localStorage`（headless 环境本就没有全局 `localStorage`，旧 bug 复发会直接抛 `ReferenceError`）+ 按 `worldId`/`accountId` 隔离；`client/test/ui/saveManagerAutoRerender.ui.ts` 端到端复现「两个同时挂载的场景共享一个 `SaveManager`」场景（`GachaScene`+`BattlePassScene`，仿 `nav/shop.ts` 的真实接线）：一个场景外部改了钱包，另一个场景**不用自己调 `render()`** 就能看到新值；并验证 `destroy()` 后监听器确实被移除、后续写入不报错。
- **⚠️ 资源管理审计后修复（2026-07-29，同日第二轮）**：客户端资源加载/存储/缓存/文件系统全面审计（见记忆 `client-resource-mgmt-audit-2026-07-29`），修复六项：①`net/anomaly.ts` 崩溃哨兵（`lsGet`/`lsSet`）与 `readPublicId()` 此前直接碰全局 `localStorage`（与上条 `WorldMapContext` 同类问题，微信端静默失效无报错）——新增 `setAnomalyStorage(platform.storage)`，`app.ts` 在 `initCrashSentinel()`/`installAnomalyWatchers()` 之前调用；未注入时回退到 `globalThis.localStorage`（保持既有测试/Web 行为不变）。②`SaveManager` 新增 `resetForLogout()`：`doLogout()`（`nav/auth.ts`）此前只 `clearSyncedLocalSections()`（仅 equipped/flags/pvpDeck），wallet/progress/cardInv/离线队列全部原样留存——纯离线切账号会读到上一账号数据，更严重的是离线队列（`pending`/`pendingStamina`）可能在下一账号联网后被其 token 结算，造成跨账号奖励串号；`resetForLogout()` 先（若仍在线，即 `api.setToken(null)` 之前）尽力 flush 待结算队列，再清空队列 + `store.clearLocal()` + 重新 `loadLocal()` 得到全新存档，`doLogout()` 用 `.finally(() => api?.setToken(null))` 延后失效 token 确保 flush 请求仍带着有效凭证发出。③`assets/WechatAssetIO.ts` 的 `USER_DATA_PATH/nwassets` 缓存目录原来只增不减；新增 `.index.json` 记录按文件名的最后访问时间，构造时若目录超 30MB 软预算即按最久未访问（含修复前从未被追踪的孤儿文件，视为最旧）优先清理；`saveFileSync` 失败/GC 异常均上报 `reportAnomaly`（此前完全静默）。④`platform/wechat/WechatPlatform.ts` 的 `WechatStorage.setItem` 写入失败（含容量超限）原来静默吞掉；现附带 `wx.getStorageInfoSync()` 诊断信息上报 `reportAnomaly`。⑤`render/atlas/spriteAtlas.ts`（`createAtlasLoader`）/`render/atlas/coinIconAtlas.ts`/`render/stickman/StickmanRuntime.ts`（`loadAsset`）三处的按 URL 缓存原先失败后把**已 reject 的 Promise 永久缓存**，弱网抖动会让某资源在整个会话生命周期内卡死占位/降级、网络恢复也不会自愈；现在 `.catch` 里把缓存槽重置为 `null`，下一次调用真正重试。⑥`game/meta/migrate.ts` v3→v4 步骤此前只 `d.version=4`，废弃字段 `unitLevels`/`cardInventory`/`gear` 靠 `fillDefaults` 的「保留多余 key」兜底原样留在存档里永不清理；现改为显式 `delete`。**回归测试**：`test/anomaly-chain.test.ts`（沿用既有 stub，向后兼容）、`test/save-manager.test.ts` 新增 `resetForLogout` 描述块、`test/WechatAssetIO.test.ts`/`test/WechatPlatform.test.ts`（新文件）、`test/render/atlasNegativeCacheRetry.test.ts`（新文件，三处负缓存一并覆盖）、`test/migrate.test.ts` 新增 v3→v4 描述块。另外**新增 Web/App 资源分级机制**（`webpack.config.js` 的 `.hires` 兄弟文件约定，`mobile` target 专属，见 `design/game/ASSET_PACKAGING.md` §9）：`assets/logo.png` 由 512px/497KB 降为 256px/129KB 压缩默认版（Web/微信/CrazyGames 共用），新增 `assets/logo.hires.png`（1024px/1.9MB，仅 mobile 构建自动选用）。
- **⚠️ 数据共享复查修复（2026-08-03，全客户端场景切换专项审计）**：对第 34 条「跨界面数据共享」契约做了一次复查，找到该契约的一处遗漏（不是回归——2026-07-29 那次审计当时就没覆盖到）：`DefenseEditorScene`（`scenes/DefenseEditorScene/`，以 overlay 挂在 `WorldMapScene` 上，攻击模式下经 `cb.getSave?.().cardInv/equipmentInv` 展示存档卡组/装备库存）从未声明/接入 `onSaveChanged`，是文档所说「所有展示钱包/存档的界面」里唯一漏掉这个订阅的。后果：编辑布防阵容期间若存档被后台改动（邮件自动领取、云同步 reconcile），面板会显示陈旧的可用卡牌，直到玩家任意一次点击间接触发 `render()` 才刷新——UI 展示层陈旧读，非资金/存档损坏。修复：`DefenseEditorCallbacks` 新增 `onSaveChanged?(listener): unsub`，`base.ts` 构造函数里 `if (cb.onSaveChanged) this.unsubs.push(cb.onSaveChanged(() => { if (!this.destroyed) this.render(); }))`；`nav/world.ts` 里唯一传了 `getSave`（因而唯一会读到陈旧数据）的调用点——`openCity()` 的 `onEditTeam`（CityScene 编队跳转攻击阵型编辑）——同步接入 `onSaveChanged: (listener) => saveManager.subscribe(listener)`。`goDefenseEditor()`（防守模式，从地图直接点瓦片进入）本就不传 `getSave`，不读存档数据，故不需要接入。
- **⚠️ 全 client 代码审查修复批次（2026-08-03，9 路并行子代理全量扫描）**：对 `client/src/` 全目录（333 文件）做了一次系统性代码审查，发现并修复 30 项问题（不含此前已记录的内存/生命周期类审计）。关键几条会影响未来改动，单列说明：
  - **局末结算竞态**：`GameRenderer`（`events.ts`/`base.ts`，2026-08-11 `base.ts`→`core.ts` 改判为组合，见上方拆分优先级条目）game_over/game_draw/教程胜利后延迟 1.5~2s 才调 `onGameEnd`，但投降按钮此前未按 `gameEnded` 门控——玩家可在这段窗口内投降退出，计时器仍会在之后触发，对已离开的场景重复调用 `onGameEnd`（重复上报战绩/重复发奖励）。修复：`input.ts` 的 `handleDown` 顶部新增 `if (this.gameEnded) return`；新增 `GameRendererCore.scheduleGameEnd()`（原 `GameRendererBase`）统一记录计时器句柄，`destroy()` 里 `clearTimeout` + 置空 `onGameEnd`。
  - **`NetState` 新增 `'disconnected'`**（`net/NetClient.ts`）：与原有 `'closed'` 区分——`'closed'` 仅表示主动/优雅断开（`disconnect()`），`'disconnected'` 表示服务端永久拒绝（致命关闭码，如 4409、或选配的 4401）。`GameScene`/`RoomScene`/`NetStatusView` 均已按新值分支处理；新增消费方必须显式处理这个值，不能与 `'closed'` 混为一谈。
  - **`NetSession.freshToken()`**：此前仅在“完全没有缓存 token”时才重新鉴权，网关握手被拒（4401）后会拿着同一个过期 token 无限重试。现改为：网关明确回报鉴权被拒（`onAuthRejected`）且本机无密码登录会话（`TOKEN_KEY` 未持久化）时才重新铸造匿名凭证 token；若是真实密码登录账号则不能这样做（会静默切换成匿名身份），改为 toast 一次提示玩家需要重新登录。
  - **装备重铸材料锁定校验缺口**：`EquipmentScene/reforge.ts`/`detail.ts` 与 `server/metaserver/src/equipment.ts` 的 `reforgeEquipment` 此前都只检查了目标物品的 `locked`，未检查素材物品的 `locked`——锁定物品可被当素材消耗销毁。已补齐两侧校验（同 salvage/escrow 已有的锁定检查模式）。
  - **`CAMPAIGN_LEVEL_ORDER` 移除 `ch_stress`**（`game/campaign/levels.ts`）：压测关卡混进了正式关卡顺序（`CH0_TUTORIAL` 早已排除，`ch_stress` 当时漏排除），导致玩家 100% 通关后 `currentChapter()` 扫描到这个永远无法通关的条目、回退到第 1 章。`ch_stress` 仍注册在 `CAMPAIGN_LEVELS`（可直接按 id 加载），只是不进入可玩顺序。
  - **`CardScene` 新增 `fuseRingOpen` 标记**（`base.ts`）：范围比原有 `fuseInProgress`（仅覆盖网络请求期间）更广，覆盖整个融合环展示期间（含确认前选材料阶段）。`render()` 的弹窗分发、`list.ts` 的 `applyCardState()`、`onSaveChanged` 监听器均已改为在此标记为真时跳过 `detailId`/弹窗状态改动，避免外部重渲染把还开着的融合环悄悄换成普通详情弹窗。在 `closeModal()`（唯一收口点）里复位，不要在各调用点分别复位。
  - **`skinDisplayName(id)` 补齐使用面**（`game/meta/skinDefs.ts`）：`CardScene` 的换装 Tab 与详情弹窗换肤选择器此前直接显示原始皮肤 id（如 `skin_e1`），已改为统一走这个函数（与商城/抽卡页一致）。`SKIN_TARGET_UNIT` 保证每个 `UnitType` 至多对应一款皮肤，故单角色的换肤选择器最多只有 2 行（默认+该皮肤）。
  - **验证**：`tsc --noEmit` 全绿、client 单测 954/954 + UI 冒烟 1041/1041 + 渲染泄漏 34/34（新增 1 条 `baseUpgradeAtlasLoader` 负缓存回归）+ webpack 生产构建通过；server `metaserver` 单测 772/772（新增 1 条 reforge 锁定素材回归）。**踩坑记录**：`metaserver/test/*.e2e.test.ts` 是从 `dist/` 导入而非 `src/`（文件头注释已写明），改了 `server/metaserver/src/*.ts` 后必须先 `npm run build --workspace @nw/shared && npm run build --workspace @nw/metaserver`（即 `tsc -b`）重新编译，否则 e2e 测试仍跑旧逻辑，之前 `equipment-enhance-protect-rebalance-2026-08-02` 那次也踩过同一个坑。
- **内存看护（MemoryMonitor）**：`app.ts` 启动时 `new MemoryMonitor().install(app.ticker)`，跨场景常驻。每 5 秒采样 `performance.memory.usedJSHeapSize`，超阈值（默认 400MB，`localStorage 'nw_mem_warn_mb'` 可调）即 `console.warn`（`netLog('mem')`）并 dump 各对象池的空闲对象数 + 粗估占用，30 秒内不重复刷屏；微信侧另接 `wx.onMemoryWarning`（移动端真预算闸门，微信运行时常无 `performance.memory`）立即同样 dump。各池经 `cache/poolRegistry.ts` 登记/注销：4 个 `ObjectPool`（unit.circle/building/hand.slot/fx.meteor）由 `ObjectPool` 构造自注册、`drain()` 注销；3 个手工池（unit.stickman/fx.vfx/projectile）在各拥有者构造登记、`destroy()` 注销。**注意：`usedJSHeapSize` 只含 JS 堆、不含 GPU 纹理显存**——故"堆高但池都小"多为泄漏的场景图/闭包（同上次事故），某池 idle 只涨不回落才是池侧泄漏，两类靠这份 dump 区分。**池化范围**：仅战斗内高频生灭对象（stickman 是关键热路径）池化；HP 条/护卫 sprite、HUD 等常驻或低频对象按需 new/destroy，不入池。

## 核心模块

| 文件 | 职责 |
|---|---|
| `app/createAppCore.ts` + `app/appCtx.ts` + `app/appConstants.ts` + `app/nav/*` | **无渲染编排核心**（同一份代码跑 PixiAppViews 真机 + HeadlessAppViews E2E）。`createAppCore` 现只做装配：建 `ApiClient`/`SaveManager`/`ReplayStore`/`FeatureFlags`/`NetSession` + 会话可变态 + 叶子 helper（session/gateway/profile/deck/replay/shard）+ `start`/`onResized`。**约 50 个 `go*` 屏幕跳转按域拆入 `app/nav/{auth,lobby,room,social,world,shop,game,result}.ts`**，各导出 `createXxxNav(ctx)`。`AppCtx`（`appCtx.ts`）= 依赖 + `state`（可变态，按引用共享）+ `nav`（跳转注册表）；`nav` 装配时 `Object.assign` 各模块产物，故跨模块互调走 `ctx.nav.goX()` 无 import 环。存储键/logger/`clientPlatformName` 在 `appConstants.ts`。**新增跳转须同步 `Nav` 接口 + 对应 nav 模块 + `AppViews`/HeadlessAppViews 补桩** |
| `@nw/engine/GameEngine` | 主循环、系统编排、命令处理；每 tick 从 `InputSource` 消费指令；tick 顺序：resource→production→**trait**→combat→escort→hazard→movement→spell。**联机追帧**：`tick()` 按 `confirmedLead` 积压选倍速（>30s→5×/>10s→3×/>1s→2×/否则1×）缩短 `stepDt`，让暂停/最小化（rAF 停摆）落后的客户端加速排帧追上水位线；只重定时 step 不改帧序，确定性不变 |
| `@nw/engine/net/InputSource` | 统一输入管线接口 + `LocalInputSource` |
| `@nw/engine/net/ReplayInputSource` | `RecordingInputSource`（捕获确认帧，`snapshot()→Replay`）+ `ReplayInputSource`（喂 Replay，永不停步） |
| `game/net/NetInputSource.ts` | 联机锁步：`submit`→opaque `cmd_submit`；`frame_batch`→解码→`take(frame)`；未确认返 `null` 停步；`confirmedLead(frame)` 报告播放头之后的已确认积压帧数（供引擎追帧倍速） |
| `game/meta/ReplayStore.ts` | 本地录像：key `nw_replays_v1`，最近 12 局 ring |
| `game/GameState.ts` | 纯数据状态，持有 Board / Player / PRNG |
| `@nw/engine/systems/AISystem` | AI 决策（威胁驱动三段式；注入 Prng + 难度档） |
| `@nw/engine/systems/TraitSystem` | 被动 trait tick：regen / aura_heal（fp 累加→整数 HP）；slow 倒计时到期 resetSpeed；summonOnTimer 倒计时到期出兵；在 combat 前执行 |
| `@nw/engine/math/prng` | LCG 确定性随机数生成器 |
| `@nw/engine/math/fixed` | 定点数运算（`TICK_RATE = 30`） |
| `i18n/index.ts` | `t()` 取词 + 插值；`initI18n`/`setLocale`/`onLocaleChange` |
| `game/meta/SaveData.ts` | 元系统单一权威根；`makeNewSave`/`SyncPatch`/`SAVE_VERSION` |
| `game/meta/migrate.ts` | `migrate(raw)→SaveData`：顺序升级 + fillDefaults；改字段必加迁移步 |
| `game/meta/SaveManager.ts` | 云同步：离线优先→bootstrap→防抖 push→409 reconcile；PvE 通关/升级走 `/pve/*` API。后台 push 连续失败达阈值（3 次）触发一次 `onSyncError`（接 `showToastMessage`，提示进度可能未上云），一次成功上行即复位，不刷屏 |
| `net/rateGate.ts` | **全局出站请求节流**（ADR-058）：令牌桶，5 次/秒，`globalRequestGate.acquire()` 排队 FIFO；metaserver REST/worldsvc REST/WS 业务消息三层共用同一个桶。WS 侧 `ping`/`submitCmd`（对战延迟敏感）豁免，见 `NetClient.sendClient` |
| `net/ApiClient.ts` + `net/ApiClient/{core,auth,pve,equipment,shop,gacha,social,mail,achievements,misc}.ts` | metaserver REST 客户端（fetch + ApiResp 包络）；`fetchRaw` 过 `rateGate` + 10s `AbortController` 超时（ADR-058 之前完全无超时）。**2026-08-11 由 `XMixin(Base)` 继承链改判为组合**：`core.ts`=`ApiClientCore`（transport+token，全字段 public）；9 个域文件各自是独立类（`AuthService`/`PveService`/…），构造器接 `core`；`ApiClient.ts`=瘦转发壳，持 `core`+9 个域实例，~66 个端点方法逐条一行转发（无跨域调用，故不用组合规则第②档"窄接口"，直接持有 core 即可） |
| `net/NetClient.ts` | WS 连接/重连（退避+代次）/ts-proto 编解码；`sendClient` 拆成限速分发 + `doSend`，`submitCmd`/`ping` 走 `{rateLimited:false}` 直发 |
| `ui/busyTracker.ts` | `BusyTracker`（`busy`/`start()`/`stop()`/`tick(dt)`）+ `withTimeout(promise, ms=10000)`/`TimeoutError`：请求进行中锁定 UI（不重复发）+ 超时兜底的标准范式，`Shop/Sect/Family/Auction` 等场景统一用 |
| `net/NetSession.ts` | 联机会话：gateway(控制面 `/gw`) + game(数据面 `?ticket=`) 双连接；跨场景存活；含社交 + **SLG 实时 push** 路由（`onMarchUpdate/onTileUpdate/onUnderAttack/onSiegeResult/onFamilyMsg`，worldsvc→gateway 下发） |
| `net/WorldApiClient.ts` + `net/WorldApiClient/{core,types,world,defenseTeams,siege,nationsSeason,shop,family,auction,sect,worldChannel,cityOps}.ts` | **SLG worldsvc REST 客户端**（第四公网面，独立 base URL）；DTO 由 `server/contracts/openapi-world.yml` → `npm run rest:gen` → `net/openapi-world.ts` 生成（勿手改）；覆盖 world/march/troops/siege/defense/nations/season/shop/family/auction 全端点。**⚠️ `checkHealth()` 网络失败→true（inconclusive）**：dev 环境 `/health` 常缺 CORS 头而 fetch 抛错，但实际 feature 路由（`/world*`）完全正常——返回 false 会误标大世界离线。故所有 catch（连接被拒/超时/CORS）均视为"不确定"返 true，只有 HTTP 4xx/5xx 才返 false。单测断言需对应 true。**2026-08-11 由继承链改判为组合**：原单文件 712 行拆成 `core.ts`（`WorldApiCore`）+ `types.ts`（纯 DTO 类型）+ 10 个域服务类（`family.ts` 里 `listFamilies()`→`getMyFamily()` 是唯一真正的跨方法调用，留在同一文件里，没有拆两个类再互相依赖）；`WorldApiClient.ts` 瘦转发壳（712→534 行，~76 端点方法逐条转发），`file-length-baseline.json` 里仍标着这条转发壳的行数并注明原因（每行都对应真实调用点，拆表不会降低复杂度）。 |
| `scenes/WorldMapScene.ts` + `scenes/worldmap/` | **SLG 大世界地图**（视口裁剪+拖拽平移；瓦片类型对齐服务端 8 类型；敌蓝我红；首府星标；行军连线；地图尺寸从 `getSeason` 动态取）；HUD「练兵」面板（训练队列倒计时+招募预设+金币加速，C4，**2026-07-14 起弹窗按 2x 尺寸渲染**——`renderTrainPanel` 内 `S=2` 缩放系数，面板/字号/按钮/图标统一放大，宽高仍按屏幕裁剪封顶）+ 右上「世界」面板（国家/赛季/商城三 Tab，C5）。**已按 MVC 拆分**：`WorldMapScene.ts`=瘦编排壳（仅构造/生命周期/推送委派）；`worldmap/WorldMapContext.ts`=共享状态+类型出口（`WorldMapCallbacks`/`WorldMapView`/`DeployKind`）；四协作类持 ctx——`WorldMapRenderer`(地图/瓦片渲染+视图变换；**2026-08-02 按 mixin 拆分，2026-08-12 改判为组合**——`WorldMapRenderer.ts`=瘦装配壳（`new WorldMapRenderer(ctx)`，7 个域类）+ `worldmap/WorldMapRenderer/{core,build,viewport,pool,city,fog,vignette,lifecycle,tokens,shieldFx}.ts`，`core.ts`=`WorldMapRendererCore`（持 `ctx` + 记忆化的 NPC 城市节点表）+ 7 个独立域类，构造器接 `core`（+ 依 confirm 后的调用图接兄弟引用）；原链有两处真双向依赖、都以 `pool.ts` 为枢纽——① `pool` 调 `city.refreshCityLayer()`、`city` 调 `pool.isBaseAnchor()`；② `pool` 调 `fog.renderOverlay()`、`fog.renderMap()`（`invalidatePool()` 的纯转发别名）调回 `pool.invalidatePool()`——两处均未各自打一次性 lazy hook 补丁，而是判定 `pool.ts` 本身越界代持了"数据变了、重绘全图"的编排职责，把该触发点整体上移到装配壳：`pool` 不再引用 `city`/`fog`（`city` 仍单向依赖 `pool.isBaseAnchor()`，未成环），装配壳的 `invalidatePool()`/`refreshPool()` 显式依次调 `pool.*`+`city.refreshCityLayer()`+`fog.renderOverlay()`；`build.ts`/`viewport.ts`/`lifecycle.ts` 中途需要这套"刷新全图"组合调用（`build()`/`setZoom()`/`bootstrap()`），以闭包 `refreshMap: () => void` 注入（安全，因为这几个方法都只在 `new WorldMapRenderer(ctx)` 整体返回之后才会被外部调用，届时所有兄弟字段早已就绪）。`fog.ts` 转换前已超 500 行（613），拆完仍超——二次拆分挖出 `tokens.ts`（form① 自由函数模块，march/occupy/stationed 三个 token 同步循环 + 配套 helper，原本就只读写 `core.ctx` 不调其它 Fog 方法，天然适合自由函数）；`shieldFx.ts` 本就不在链上，原样保留）、`WorldMapPanels`(HUD/弹窗/toast/练兵+世界面板等 chrome UI；**2026-08-02 按 mixin 拆分，2026-08-11 改判为组合**——`WorldMapPanels.ts`=瘦转发壳（`new WorldMapPanels(ctx)`，零跨域 `this.*` 调用，纯文件拆分）+ `worldmap/WorldMapPanels/{core,hud,shop,territory,replay}.ts`，`core.ts`=`WorldMapPanelsCore`（持 `ctx` + 弹窗/toast/出兵原语 + 面板 chrome helper）+ 4 个独立域类，构造器接 `core`；`territory.ts` 因 `this.core.` 加前缀后超 500 行，`renderWorldTabBody` 拆成 form① 自由函数 `territoryWorldTab.ts`；**2026-08-12 补充**：`hud.ts` 之后随 SLG portrait HUD legibility 修复（`renderHeaderHud` 内新增资源簇 shrink-to-fit 逻辑 + 队伍/领地卡片重排，commit `5002c7b2`）涨到 513 行，跨过 500 行基线（`checkFileLength.mjs` 判定为 NEW 违规，未进 baseline）——`renderHeaderHud`（产量读数 + 商城/拍卖行按钮，只读写 `ctx` 不调 `HudPanel` 其它方法，天然 form①）挖成独立函数模块 `headerHud.ts`，`hud.ts` 保留 `renderHud()` 编排（底部聊天条/缩放列/右侧状态卡+buff+行军/回放徽章）回落到 380 行)、`WorldMapNet`(worldsvc API+行军动作+实时 push)、`WorldMapInput`(拖拽+瓦片点击派发；**2026-08-12 补充**：新增「回家」按钮——header 里紧贴商城按钮左侧，点击调 `ctx.view.centerAt` 回到 `me.mainBaseTile`（无 base 时按钮不渲染，`homeBtnRect` 留零矩形）；`handleDown` 里 Zoom/资源簇/Back/商城/回家/拍卖行/行军徽章/回放徽章/聊天条这一串 header/HUD 按钮命中测试挖成 form① 自由函数 `WorldMapInput/headerButtons.ts`（跨过 510 行基线前的顺手拆分，之后回落到 457 行，已从 `file-length-baseline.json` 摘除），`headerHud.ts` 同步加了 `renderHeaderHud` 里的按钮绘制块)；纯 helper `constants.ts`/`tileStyle.ts`/`zoom.ts`/`tileGraphics.ts`(无状态绘制原语；**2026-08-12 起为瘦转发壳**——原 724 行零类零 `this` 的自由函数集合，按①拆成 `worldmap/tileGraphics/{tiles,resources,primitives}.ts`，`tileGraphics.ts` 本身降为 `export * from './tileGraphics/{tiles,resources,primitives}'` 的 barrel，外部 import 路径不变) |
| `scenes/FamilyScene.ts` + `scenes/FamilyScene/{core,types,header,data,actions,input,pointer,modals,render,lists,repaint}.ts` | SLG 家族场景（S8-4，noFamily→create/join 分支；myFamily→成员+频道两栏，横屏常驻双栏/竖屏 Tab 切换）。**2026-08-11 由继承链改判为组合**（原 `base.ts` 560 行）：`FamilyScene.ts`=瘦装配壳，owns `render()` 派发（`beginRender`/`drawRail` 在 `core.ts` 上）+ 首次 `render()`/`data.loadData()`；`core.ts`（560→469 行）挖出 `header.ts`（`drawHeaderTitle`，纯 form①，无 Core 委托）后回落 500 行以下；`data.ts`=独立类，全场景唯一的干净叶子（零跨域调用）；`actions.ts`/`input.ts`=独立类，均只依赖 `data`；`render.ts`（547→328 行）依赖 `actions`+`input`，把最大的两段（`renderMembers`/`renderChannel`）挖成 `lists.ts`（同 `SectScene` 的 `lists.ts` 手法，显式 `(core, actions|input, ...)` 传参）。**唯一一对真双向依赖**——原 `actions.ts`（`doSendMsg` 无草稿时调 `input.ts` 的 `openSendInput`）↔`input.ts`（回车键回调调 `actions.ts` 的 `submitMessage`）——重新 grep 全量调用图确认无其它漏报（这批里第一次审计完全准确的转换），修法是**合并成一个类**而非惰性钩子：`doSendMsg`/`submitMessage` 并入 `InputPanel`（与原本就在那里的 `openSendInput`/`openInputFor` 是同一个发送框交互的两种状态），`ActionsPanel` 因此完全不依赖 `Input`。**2026-08-25 增量重绘（详见 `design/game/SOCIAL_DESIGN.md` 变更表同日第二行）**：新增 `repaint.ts`=`FamilyRepaint`（form②，`core.repaint`），与 `SectScene/repaint.ts` 同构（按列 band 存滚动状态 + `caretText`/`setFieldValue` 只改一个 `Text`），列名为 `'members' | 'channel'`；本场景两个朝向都是名册→`scrollY`、频道→`scrollYChannel`，故 `scrollKeyFor(col)` 不看朝向（宗门那条要看）。**与宗门唯一的结构差异**：名册的行原本直接画在 `bodyLayer` 上、**没有遮罩**，这轮给它加了自己的遮罩层才能平移——顺带修掉「滚到底那行溢出视口、压住底部导航条」的老错位。行内 4 处 hit rect 打 `scroll: 'members'` 标记，`handleDown` 用 `repaint.appliedDelta(col)` 换算 + 视口裁剪（同 FriendsScene 那条 `appliedScrollDelta` 警告）。同样删掉了 `bt.tick()` 驱动的整树重建（本场景不画 dots 遮罩），并补上 `preloadTabIconTextures().then(() => this.render())`。`core.ts` 因此超限（525），挖出 `types.ts`（纯声明，`export type {...} from './types'` 保持旧 import 路径，同 `FriendsScene/types.ts`）后回落 494。**2026-08-25 弹窗列表统一**：两个弹窗（浏览/加入选择器、入帮申请审批）从 `actions.ts` 整体挖进新的 `modals.ts`（form①自由函数 `drawFamilyPickModal`/`drawJoinRequestsModal`，显式接 `onPick`/`onRespond` 回调——做成类会与 `ActionsPanel` 形成真双向依赖），并都改成真正的滚动区（列名 `'modal'`，重建走 `core.modalRedraw`）：选择器原本 `families.slice(0, 6)` 只给 6 个且无提示，审批表原本把所有行画到面板外面去。`keepScroll` 参数让「审批中途置灰按钮」的那次重绘不再跳回列表顶部。同时挖出 `pointer.ts`（同宗门，见上一行）。 |
| `scenes/AuctionScene.ts` | SLG 拍卖行场景 |
| `scenes/LobbyScene.ts` + `scenes/LobbyScene/{core,build,badges,overlays,header,mainContent,bottomNav,vsOverlay,matchState,format}.ts` | 主菜单/大厅（S2）。**2026-08-12 由继承链改判为组合**（原 `base.ts`/`build.ts`/`badges.ts`/`overlays.ts` 四文件链，`build.ts` 807 行是这批 15 条里最大的单文件）：`LobbyScene.ts`=瘦装配壳，**是这批里唯一一次连 `update()`/`destroy()` 也没留在 Core 上**的转换——原 mixin 链的 `update()` 直接按名字调两个不同兄弟的方法（旧 `BuildMixin.matchFound()`、旧 `OverlaysMixin.clearToast()`），不是单一注入回调能覆盖的形状，故 `update()`/`destroy()`/首次 `build()` 调用/`input.onDown` 订阅全部留在装配壳里，`core.ts` 不再实现 Scene 接口；装配壳因此自带一份 `unsubs` 数组承接 `input.onDown`（没有塞进 `core.unsubs`），否则 `test/input-subscription-cleanup.test.ts` 的同文件订阅/回收静态扫描会报假阳性。`core.ts`=`LobbySceneCore`，全字段 public；`build.ts`=`BuildPanel`（`build()`编排 + `handleDown()`分发，构造器接 `core`+`badges`+`overlays`）；`badges.ts`=`BadgesPanel`（红点徽章，只依赖 `core`）；`overlays.ts`=`OverlaysPanel`（首次引导/赛季结算/成就-info toast，只依赖 `core`，全场景唯一干净叶子）。**唯一一对真双向依赖**——旧 `build.ts`（`build()` 建好每个徽章图层后调 `badges.ts` 的 `drawSocialBadge`/`drawAchievementBadge`/`drawShopBadge`/`drawWorldOfflineBadge`/`drawSideStripBadges`）↔旧 `badges.ts`（`applyEventsAvailable` 调自己的 `rebuild()`，`rebuild()` 又调 `build()`）——判定为 `rebuild()`（teardown 干净的 Core 状态后重新触发整个场景重排）本质是与"badges"无关的全场景级操作，只是恰好第一个调用点（events 条目显隐）长在 `badges.ts` 里，**没有合并成一个类**，而是把 `rebuild()` 整个搬到 `core.ts`（配一个惰性钩子 `buildHook`，默认 no-op，装配壳建完 `BuildPanel` 后立刻覆盖，同 `allianceHooks`/`reopenCreateForm` 手法），之后 `badges.ts` 只需单向调 `this.core.rebuild()`，`build.ts→badges.ts` 只剩画徽章那一条单向边。`build.ts` 的二次拆分按视觉分区切成 5 个纯 form① 兄弟模块而非走 `types.ts`/共享 chrome 老路——`header.ts`（logo/title/头像/账号-金币-段位徽章）、`mainContent.ts`（主按钮+英雄剪影+图鉴/世界地图双卡+右侧 daily/mail/events 竖条，依赖 `badges` 画竖条红点）、`bottomNav.ts`（底部五格 tab+社交/成就/商店/世界离线四个徽章层，同样依赖 `badges`）、`vsOverlay.ts`（对局揭示层）、`matchState.ts`（本地 AI 对局 `onStartPressed`/`matchFound` 状态机，零本地状态、纯操作 `core` 字段，故不建类）；`build.ts` 本身瘦身到 188 行。`format.ts`（纯 `fmtCoins`/`headerMetrics`）不是链的一部分，未改动。 |
| `scenes/GachaScene.ts` + `scenes/GachaScene/{core,page,reveal,odds,trail}.ts` | 抽卡（单抽/十连 + 保底 + 揭示动画）。**2026-08-02 按 mixin 模式拆分，2026-08-11 改判为组合**（原单文件 1034 行）：`GachaScene.ts`=瘦装配壳，持 `core`+`page`+`odds`+`reveal`，owns `render()` 派发（原 mixin 链靠接口声明合并从 base 反向调，现装配壳按原顺序显式调各面板方法）；`core.ts`=`GachaSceneCore`，全字段 public，构造器接 `render: () => void` 回调（不再自触发首帧 render/`loadPools`，改由装配壳在全部面板建好后调）；`page.ts`/`odds.ts`=独立类，构造器接 `core`；`reveal.ts`=独立类，构造器接 `core` + 窄接口 `entryPictures: EntryPictureSource`（只含 `drawEntryPicture` 一个方法，不接整个 `OddsPanel`——组合规则第②档的典型用法）。**`trail.ts` 仍不是 mixin**——传说卡描边拖尾是无状态的圆角矩形周长几何 + 虹彩色相运算，`reveal.ts`（建拖尾）和 `core.ts` 的 `update()`（每帧推进）都要用，故独立成纯函数模块 |
| `scenes/CityScene.ts` + `scenes/CityScene/{core,render,modals,teamRow,trainModal}.ts` | 家门内政（资源/建造队列/建筑格子/五队伍位）+练兵。**2026-07-26 按 mixin 模式拆分，2026-08-11 改判为组合**（原单文件 1266 行）：`CityScene.ts`=瘦装配壳，持 `core`+`renderPanel`+`modals`，owns `render()` 派发；`core.ts`=`CitySceneCore`（752→753 行，`file-length-baseline.json` 标了这条为"合理例外"——单一共享状态+业务逻辑根，`render.ts`/`modals.ts` 都依赖它，没有天然的再拆点）；`render.ts`/`modals.ts`=独立类，构造器接 `core`；两者因 `this.core.` 加前缀后都超 500 行，各拆出一个 form① 自由函数兄弟文件（`teamRow.ts`=队伍行卡片，`trainModal.ts`=练兵弹窗），调用方用别名 import（如 `renderTeamsRowImpl`）避免与类自身同名委托方法混淆 |
| `scenes/DefenseEditorScene.ts` + `scenes/DefenseEditorScene/{core,data,render,input,grid,roster}.ts` | SLG 通用半场布防编辑器（防守布防 / 出征阵型两种 mode，见文件头注释）。**2026-07-26 按 mixin 模式拆分，2026-08-11 改判为组合**（原单文件 1267 行）：`DefenseEditorScene.ts`=瘦装配壳，owns `render()` 派发 + 把 `onDown/onMove/onUp` 接到 `InputManager`（`InputPanel` 此时还没建好，故传给 `core` 构造器一份惰性闭包 `handlers: {onDown,onMove,onUp}`，真正调用发生在闭包已捕获好 `this.input` 之后——两阶段构造的典型写法，`onWheel` 因 body 只碰 `core` 自身字段、无需委托，直接在 `core.ts` 里实现）；`data.ts`=独立类；`buildArmy()` 从 `data.ts` 挪到了 `core.ts`（`teamCapacity`/`effectiveLeaderId` 两个 Core 方法与 `data.ts` 都要调它，本是双向依赖，按规则挪到共享底层而不是发明单向接口）；`render.ts`因 `this.core.` 前缀超 500 行，拆出 `grid.ts`（格子/单位/建筑绘制）+ `roster.ts`（出征卡池），`grid.ts` 内部一处同模块自调用（`renderGrid`→`drawArtFit`）为了让测试 `vi.spyOn` 拦得住，改走自引用 `import * as gridSelf from './grid'`；`input.ts`=独立类 |
| `net/proto/{transport,game}.ts` | ts-proto 生成（勿手改） |
| `scenes/RoomScene.ts` | 好友房 UI：idle→codeEntry→connecting→inRoom |
| `scenes/FriendsScene.ts` + `scenes/FriendsScene/{core,types,chrome,input,repaint,network,friendsList,search,orgForm,orgBrowse,worldChat,mail}.ts` | 社交五 Tab（好友/家族/帮会/世界频道/邮件）。**删除好友二次确认 + 切磋邀请**（2026-07-25）：`modalLayer`/`modalHits`/`modalOpen`/`showConfirm`/`closeModal`（照抄 `FamilyScene` 那套通用确认弹窗管线），✕ 删除按钮改走 `confirmRemove()`。切磋："切磋"按钮直接发 `NetSession.duelInvite(publicId, deck)`（控制面新消息，不走 `RoomScene` 的手动房间码流程），matchsvc 端接受后直接复用既有 `startMatch()`（详见 `design/game/MATCHSVC_DESIGN.md §10`）；client 侧无需处理"已接受"——沿用既有 `onMatchStart`。matchsvc 同一时刻只认一条在途邀请（`pendingDuelByAccount`），故客户端也让**所有**好友行的切磋按钮一起置灰（`sendingDuelTo`），不只是被邀请的那一行；收到邀请显示在列表顶部的横幅（`incomingDuelInvite`，本地 60 秒倒计时仅做显示，权威超时在服务端）。**2026-08-11 由继承链改判为组合**（`base.ts`757行 是这批里最大的一条，`service.ts`→`network.ts`）：`FriendsScene.ts`=瘦装配壳，owns `render()` 派发 + 首次 `render()`/`refresh()`/`triggerTabLoads`；`core.ts`=`FriendsSceneCore`——`switchTab`/`triggerTabLoads`/7 个 `applyXxx` 推送回调都要调 `NetworkPanel`（`refresh`/`loadSLGStatus`/`loadWorldMessages`），装配壳建完 `NetworkPanel` 后立刻整体覆盖 `core.net` 字段（默认 no-op，同 SectScene 的 `allianceHooks` 手法）；`core.ts`本身仍超 500 行，先挖出 `types.ts`（纯 `SLGSocialStatus`/`FriendsSceneCallbacks`/`Tab`/`Hit` 类型声明，`core.ts` 里 `export type {...} from './types'` 保持旧 import 路径），再挖出 `chrome.ts`（`beginRender`/`endRender`/`drawTabBar`/`drawHeader`/`addButton`/`scrollRegion`/`openHiddenInput` 等每个 Tab 面板都调的共享渲染原语，form① 自由函数，Core 上不留委托方法）；`network.ts`=`NetworkPanel`（独立类，被全部 5 个 Tab 面板依赖，自己不依赖任何 Tab）；`friendsList.ts`/`search.ts`/`orgForm.ts`/`worldChat.ts`/`mail.ts`=5 个 Tab 各自的独立类，构造器接 `core`+`network`（`friendsList.ts`额外接窄接口 `search: SearchEntry` 只含 `openSearch` 一个方法，跳转到搜索 Tab 不需要整个 `SearchPanel`）。**2026-08-20 页签切换卡顿修复（详见 `design/game/SOCIAL_DESIGN.md` 变更表同日行）新增/移动的三个文件**：`repaint.ts`=`RepaintState`（form②，Core 持有 `core.repaint`）——**这个场景的三条增量重绘路径都在这里**：滚动（行按 `builtScrollY` 布好、上下各多建一屏 `overscan`，拖拽只 `layer.y = -delta`，拖出带外才回落 `render()`，照抄 `CardCodexScene` 先例）、光标闪烁（`caretField`，只换一个 `Text` 的字符串）、切磋倒计时（`duelBannerLabel`，同理）；每条都在目标对象没了/已 destroy 时回落整树 `render()`，所以漏注册只会退化成旧行为而不是留下脏屏；`beginRender` 里 `core.repaint.reset()` 负责在 `tearDownChildren` 之后丢掉全部句柄。`input.ts`=指针/滚轮分发（`onPointerDown`/`Move`/`Up`/`onWheel`，form① 自由函数，从 `core.ts` 挖出以回到 500 行以下，同 `FamilyScene/input.ts` 的接缝）——**⚠️ 滚动层的命中判定必须用 `repaint.appliedScrollDelta`（=`-layer.y`，屏幕上实际的位移），不能用 `pendingScrollDelta`（=`scrollY - builtScrollY`，逻辑意图）**：`onWheel`/`onPointerMove` 同步改 `scrollY`，图层要等下一帧 `update()` 排空 `scrollDirty` 才平移，这一帧窗口里两者不一致，用后者会让点击整整错一行（2026-08-20 实测踩过，详见 `claudedocs/client-testing.md` 同日那节）——**注意这是测试接线的破坏性改动**：`test/ui/socialTabRail.ui.ts` 原先直接 `scene.core.onPointerDown(...)`，改成 `friendsInput.onPointerDown(scene.core, ...)`。`orgBrowse.ts`=`OrgBrowsePanel`（form②，`OrgFormPanel` 持有并委派）——家族**浏览/加入**流程（搜索框、可加入列表、预览弹窗、申请按钮），`orgForm.ts` 只留两个页签外壳 + 两个「自建组织」表单。另：`chrome.ts` 新增 `caretText(core, {...})` 统一构造「带闪烁光标的输入框文字」并顺手注册到 `repaint.caretField`——**新增输入框一律走它**，否则光标闪烁会回落成整树重建。 |
| `render/sketch.ts` | `SketchPen`：确定性 Prng 抖动的手绘笔触 |
| `render/sketchUi.ts` | 共享手绘 UI 原语（纸底/手绘按钮/面板/色板单一来源）。**`sketchPanel` 返回 `PIXI.Container` 而不是 `Graphics`**（2026-08-20 起）——边框由 `render/panelFrame.ts` 的图集 sprite 拼出来；要往面板上再画墨用 `inkLayer(panel)`，不要假设它是 `Graphics` |
| `render/panelFrame.ts` | 面板边框图集：启动时把 3 档线宽 × 3 档抖动幅度的长边条 + 圆转角块烘成**一张** RenderTexture，`sketchPanel` 用它的 sprite 切片拼每个面板（长条按原生尺寸顺序平铺、`tint` 上色、`seedFor` 派生窗口偏移）。动机是性能：旧的 `SketchPen.rect` 在世界地图 HUD 那 13 个面板上每次重建要 8.6 ms / 132,300 顶点且 13/13 不可合批（`renderHud()` 每秒重建一次），现在 0.47 ms / 704 顶点 / 1 次。面板短边小于阈值（24/30/36）时退回实时 `SketchPen`。⚠️ 切片位置必须整数，否则窗口交界处出现亚像素错位台阶。设计文档：`design/product/panel-frame-art-prompts.md` |
| `ui/wheelScroll.ts` | **PC 鼠标滚轮滚动的共享判定**（2026-07-23）：`wheelScrollY(regionTop, regionBottom, y, deltaY, scrollY, maxScroll)`——纯函数，命中视口 y 范围内且有滚动余量才返回新 `scrollY`，否则 `null`（场景据此决定要不要重绘）。`InputManager.onWheel` 只在浏览器/PC 平台派发（微信小游戏无 wheel 事件，二者天然不冲突，场景侧无需平台判断）；此前仅 `WorldMapScene` 接了滚轮，现已铺到全部可滚动场景（Chat/Friends/Family/Sect/Shop/Equipment/Card/CardCodex/Gacha/Auction/City/Leaderboard/BattlePass/Titles/DeckBuilder/DefenseEditor/Recharge/Settings/Stats）——多 Tab/多列共享一份 `scrollY` 的场景（如 FriendsScene 五个 Tab、Sect/Family 的成员列+频道列双栏）一份 `input.onWheel` 订阅按当前激活的列/Tab 路由即可，双栏场景新增了 `xxxRegionTop/Bottom`+`xxxMax` 字段（触屏拖拽本就不需要提前知道视口边界，滚轮判定必须要）。 |
| `ui/widgets/scrollRegionLayer.ts` | **遮罩滚动区的那四行**（2026-08-25）：`scrollRegionLayer(parent, view)` → `{ layer, clip }`，clip 是 layer 的**兄弟**（不是子节点，否则遮罩跟着内容一起动、等于没裁），内容按绝对坐标画进 `layer`、平移 `layer` 即滚动。只负责这对显示对象，不管滚动状态/剔除/滚动条（那些各调用点不同）。目前 7 个调用点：Family/Sect 的名册列 + 频道列 + 3 个列表弹窗。 |
| `ui/widgets/ScrollIndicator.ts` | **所有可滚动页面共用的滚动位置指示条**（2026-07-14）：`drawScrollIndicator(parent, view, scrollY, scrollMax, opts?)`——在视口 `view`（=内容 mask 的矩形）右缘画一条墨黑细圆角轨道 + 位置滑块（滑块长≈视口/内容比、位置≈滚动进度）；`scrollMax<=0` 或视口退化时返回 `null` 不画，故可无条件调用。**只是指示器、不吃指针**（各场景自管拖拽/滚轮）。用法：在场景 `render()`（内容+mask 加完）末尾调一行，把 bar 画进**不随滚动位移的**容器（Pattern A 容器位移型如 CardCodex/Leaderboard/DeckBuilder 用 `this.container`；Pattern B 无 mask 剔除重绘型如 Equipment/Card/Sect/Family/Auction 用 `bodyLayer`，传 `listY/listH` 局部量作视口）。有拖拽快速路径（不走完整 render）的 BattlePass/CardCodex 需在快速路径里也重画一次（存 `scrollView` rect + `scrollbar` 句柄，先 `destroy()` 旧的）。FriendsScene 四个滚动面板（好友/邮件/世界频道/搜索）共用 `chrome.ts` 的 `scrollRegion()`+`endRender()` 里那一次 `core.repaint.drawScrollbar()`（render 顶部先 `maxScroll=0`，仅滚动面板置回 >0）——2026-08-20 起句柄由 `RepaintState` 持有，因为它的拖拽快速路径（只平移 layer、不重建）必须自己重画一次滚动条；WorldMap 世界信息面板画在 `beginScrollList()` 里。纯几何 `scrollThumbGeometry()` 已拆出便于单测。回归测试 `test/ui/scrollIndicator.ui.ts` + `battlePassScroll.ui.ts` |
| `ui/widgets/SceneHeader.ts` | **场景标题栏单一来源**：`drawSceneHeader(container,w,h,title,opts)` 画返回胶囊 + 标题（+ `opts.icon` 的标题图标，2026-08-17 起）。标题图标走 `buildTitleIcon()`——它固定要 AI 光栅图的 `content` 墨（纸面正文墨色），**不要**在调用点自己 `buildIcon(kind,size,C.dark)`，那会拿到页签非激活态刻意压暗的灰。自绘标题的场景（`CampaignMapScene`/`FamilyScene`/`SectScene`，传 `title:null`）也必须调 `buildTitleIcon` 而不是自己推导墨色。新场景一律用 `opts.icon`，不要在标题旁边手摆 Sprite。返回胶囊 = AI 手绘箭头（`BACK_ARROW_ART`，2026-08-19 起，i18n 值里**没有**字面 `←`）+ `common.back` 文案；箭头由 `addBackArrow()` 画在**缓存 chrome 之外**（光栅异步解码，烘进去会把无箭头的胶囊永久缓存），宽度取常量 `BACK_ARROW_ASPECT` 而不是纹理尺寸。要知道「返回按钮右边缘在哪」用导出的 `backPillRightEdge(h)`，别自己抄 chip 宽度公式。曲纹底走 `SceneHeader/guilloche.ts` |
| `render/icons.ts` + `render/icons/tabIconRaster.ts` | **UI 小图标单一来源**：`buildIcon(kind,size,color,opts?)` 一个入口分派两种画法——程序化 SketchPen glyph（`DrawableIconKind` → `DRAW` 表 → `icons/{currency,motifs,equipment,slg,ui,titles}.ts`）与 AI 光栅页签图（`RasterIconKind` → `TAB_ICON_RASTER` 的 46 kind × 3 墨 = 138 张 PNG）。**2026-08-18 拆分**：光栅那半（138 个静态 import + url 表 + `tabIconVariant`/`buildRasterTabIcon`/`preloadTabIconTextures`）挪进 `icons/tabIconRaster.ts`，因为 batch 6 让 `icons.ts` 涨到 517 行、越过 500 行收敛线，而那部分占了全文约三分之二、与 `DRAW` 分派除 `IconKind` 外毫无耦合。调用点**不用改**：新模块的导出全部由 `render/icons.ts` 再导出，它仍是唯一公开入口。顺带把 `DrawableIconKind` 从 `Exclude<IconKind, RasterIconKind>` 反转成显式联合，`IconKind = DrawableIconKind | RasterIconKind`（原来光栅那些名字在 `IconKind` 和 `RasterIconKind` 里各写一遍） |
| `render/atlas/coinIconAtlas.ts` | **金币图标单一来源**：`buildCoinIcon(kind,size,color)`——AI 位图图集（`assets/shop/coins.{png,json}`）加载后返回贴图 Sprite，未加载/无对应 kind 时退回 `render/icons.ts` 的程序绘制 `buildIcon`。`loadCoinIconAtlas()` 幂等、模块级单例缓存，跨场景共享。所有「金币余额」展示（大厅头部 `LobbyScene/header.ts`、`ui/widgets/SceneHeader.ts` 的 `drawHeaderCurrency`、ShopScene 商城/充值页）**必须**走这个函数，不要直接 `buildIcon('coin',...)`——2026-07-06 修过一次大厅头部金币图标与商城不一致的问题（LobbyScene 首屏渲染时图集常未加载完，需在 `LobbyScene/core.ts` 构造函数里 `loadCoinIconAtlas().then(()=>this.rebuild())` 补一次重绘；2026-08-12 组合化后 `rebuild()` 挪到了 `LobbySceneCore` 自己身上，调用点不变） |
| `scenes/ShopScene.ts` + `scenes/ShopScene/{core,card,actions,shop,coins}.ts` | 商城（订阅卡/年卡/礌装包+材料/皮肤+充值+促销码）。**2026-08-11 由继承链改判为组合**：`ShopScene.ts`=瘦装配壳，owns `render()`；`core.ts`=`ShopSceneCore`（Core 自行接 `InputManager` 的四个订阅——`handleDown` 等只碰自身 `hits` 字段，不用委托，无两阶段构造问题）；`actions.ts`=`ActionsPanel`（网络动作，唯一被 `shop.ts`/`coins.ts` 都依赖的域，二者接同一个 `actions` 实例）；`shop.ts`/`coins.ts`=两个 Tab 各自的独立类。`drawCard()`/`drawButton()` 因两个 Tab 共用又让 `core.ts` 超 500 行，拆成 form① 自由函数 `card.ts`（无 Core 委托，调用方直接 `import { drawCard } from './card'`）。`drawCard()` 商品卡片统一布局：**价格信息（badge/coinAmount/yuanPrice）固定在卡片右上角、右对齐、不换行**；标题在左侧，超长时用 `txt()` 的 `wordWrapWidth` 参数换行（不再靠 `fitText` 缩小字号），换行后 icon/状态行按标题实际高度下移——2026-07-06 改的，此前价格挤在 icon 右侧同一行导致长标题/长价格互相挤压 |
| `scenes/SectScene.ts` + `scenes/SectScene/{core,header,data,modals,actions,input,pointer,render,lists,repaint}.ts` | SLG 帮会（家族的家族）：成员家族列表+频道聊天+联盟。**2026-08-11 由继承链改判为组合**：`SectScene.ts`=瘦装配壳，owns `render()` 派发 + 首次 `loadData()`；`core.ts`=`SectSceneCore`（Core 自行接 `InputManager` 四个订阅，同 ShopScene 无两阶段构造问题）；表头联盟按钮渲染发生在 Core 自己的构造路径（`build()`→`renderHeader()`），但此时 `ActionsPanel` 还不存在——`core.ts` 声明 `allianceHooks`（默认 no-op），装配壳建完 `ActionsPanel` 后立刻整体覆盖；`data.ts`/`modals.ts`=独立类，互相无依赖；`actions.ts`=依赖 `data`+`modals`（两者都不反向依赖它，单向）；`input.ts`（创建表单/发消息的隐藏 input）依赖 `actions`；`render.ts`依赖 `actions`+`input`。`core.ts`/`render.ts` 各自超 500 行，`core.ts`拆出 `header.ts`（表头联盟按钮绘制），`render.ts`拆出 `lists.ts`（成员列表+频道消息列）——均为 form① 自由函数，无 Core 委托。**2026-08-25 弹窗列表统一**：`modals.ts` 的选择器（浏览/结盟/管理盟友）从 `sects.slice(0, maxRows)` （静默截断、无遮罩、不可滚动）改成**真正的滚动区**——与页面两列同一套 band 机制（第三个列名 `'modal'`），但重建走 `core.modalRedraw`（`modalLayer` 不在 `render()` 的树里）；行 hit 打 `scroll: 'modal'` 标记，弹窗点击因此也改成**在 pointer-up 触发**并按 `appliedDelta` 换算（拖列表不再误触行）。`pointer.ts`（form①）是这轮从 `core.ts` 挖出的指针/滚轮分发（`onPointerDown/Move/Up/Wheel` + 列路由），Core 只留 4 个一行转发（20 多处测试按名字调 `core.handleXxx`）。**2026-08-25 增量重绘（详见 `design/game/SOCIAL_DESIGN.md` 变更表同日行）**：新增 `repaint.ts`=`SectRepaint`（form②，`core.repaint`），照抄 `FriendsScene/repaint.ts` 但**按列（band）存状态**——本场景横屏是家族列+频道列两栏独立滚动，所以 `register(col, {...})`/`applyScroll(col)`/`appliedDelta(col)` 都带列参数，`core.scrollDirtyCol`（原 `scrollDirty` 布尔）记住是哪一栏动了，拖一栏绝不平移另一栏；`scrollColAt(x)`（横屏按 `chatColX` 分栏、竖屏按 `activeTab`）+ `scrollKeyFor(col)`（横屏频道列用 `scrollYChannel`，其余用 `scrollY`，与 `renderTabbedView`/`renderSplitView` 传给 lists.ts 的 `scrollKey` 一致）两个 getter 是唯一的路由真源，`handleDown`/`handleMove`/`handleWheel` 都只经它们。滚动层里的 hit rect（家族行的「投票罢免」按钮）记在 build 空间并打 `scroll: 'families'` 标记，`handleDown` 用 `repaint.appliedDelta(col)` 换算 + 视口内裁剪（同 FriendsScene 那条 `appliedScrollDelta` 警告，overscan 带里的行有 rect 但不在屏上，点它必须落空）。输入框文字一律走 `repaint.ts` 导出的 `caretText(core, {...})`（含值相关颜色 `color: (v) => ...`，频道输入框空态是灰的），光标闪烁与**每次按键**都只改这一个 `Text`（`setFieldValue`）。**顺带删掉 `bt.tick()` 驱动的整树重建**：本场景从不画 busy 的 dots/loading 遮罩（只用 `bt.busy` 置灰按钮，而 start/stop 各自都 render 过），原来那 2.5 次/秒的重建纯属白烧——**若以后要加 dots 遮罩，必须走 `repaint` 而不是 render()**。另：装配壳新增 `preloadTabIconTextures().then(() => this.render())`（同 CardScene/EquipmentScene），因为 `buildRasterTabIcon` 在纹理未解码时**画的是空容器**，而这个页面以前是靠上面那些多余重绘意外兜住的。 |
| `scenes/CardScene/detail.ts` + `EquipmentScene/{detail,reforge}.ts` | **弹窗按 80% 屏幕轴等比放大**（2026-07-14）：卡片/装备详情、装备重铸弹窗不再用固定像素宽高，而是先按原有公式算出「自然内容尺寸」`mw0×mh0`（内部所有 padding/字号仍按这份自然尺寸手工排版，不变），再取 `scale = landscape ? h*0.8/mh0 : w*0.8/mw0`——横屏顶满 80% 屏高、竖屏顶满 80% 屏宽，宽高比不变。整个面板内容画到 `this.modalPanelRoot`（新 Container，`position=screenX,screenY` + `scale.set(scale)`），不再直接画到 `modalLayer`；`modalHits` 里凡是画在 `modalPanelRoot` 上的元素，命中矩形必须过 `this.toModalScreen(localRect)` 转真实屏幕坐标（`CardSceneCore`/`EquipmentSceneBase` 共享方法，用 `modalScale`/`modalOriginX/Y`，`closeModal()` 里重置为恒等）——**全屏 dim 遮罩和最外层「点击空白关闭」的 `{0,0,w,h}` 矩形本来就是真实屏幕坐标，不要再套 `toModalScreen`**，否则会被二次错误缩放。同批修了 `CardScene`/`EquipmentScene` 的 `handleDown`：之前弹窗打开时 header 的「← Back」完全交给 `modalHits`，命中的是弹窗自己的空白关闭矩形，点了只关弹窗不退出场景；现在 `modalOpen` 分支里先单独判一次 `this.backRect`，命中就直接执行退出场景（或 assign 模式下取消），弹窗打开时 Back 按钮也保持一次点击生效。**注意 `CardScene/feed.ts`（选喂养材料弹窗）不走这套 `modalPanelRoot` 机制**——它早于本次重构、仍直接画到 `modalLayer`；2026-07-14 单独给它加了局部 `S=3` 缩放因子（所有尺寸/字号/间距/按钮 ×3，`rowH` 已 ×S，勿再整体 ×S 造成重复缩放）。**2026-07-15 分页修复**：素材多于 6 张时（同门派材料卡较多的常见情况）原先面板会把行无限往下画，撑破自己算出的 `mh`，把 Confirm/Cancel 挤到屏幕外点不到；且早期修法仍用 `h - 60` 夹紧面板高度、没扣掉 `this.headerH`，header 较高时面板底部依旧会溢出屏幕（被 `test/ui/cardFeedPaging.ui.ts` 的截断断言抓到）。现改为：面板高度夹在 `[this.headerH+4, h-8]` 之间（`topLimit`/`bottomLimit`/`availH`），最多同屏展示 6 行。**2026-07-18 重做素材列表**：①相同卡（同 `defId`+同等级）折叠为一行，右侧数量步进器 `[−] n / 总数 [+]`（`counts: Map<groupKey,number>`，行体点击 +1 循环到 0），Confirm 计数=各组数量之和、`selectedIds()` 从每组取前 n 个 id 交给 `doFeed`；②列表改为**像素拖动滚动**（不再翻页箭头）——行画进带 mask 的 `listC` 容器，超出时只留一条 `drawScrollIndicator`；滚动状态在实例字段 `this.feedScrollPx`/`feedScrollMax`（`CardSceneCore`），`handleDown` 用 `feedScrollPx` 作为手势基准、`handleMove` 在 `modalOpen` 分支把拖动量写回 `feedScrollPx` 并置 `feedScrollDirty`、`update()` 每帧最多重绘一次（调 `this.feedRedraw`，即 `drawFeedPanel` 闭包；`closeModal()` 里清空这些字段）。部分可见的行用 `clip()` 把命中矩形裁到视口内，避免点到被 mask 遮掉的部分。**2026-07-20 弹窗文字糊字修复**：`modalPanelRoot`/局部 `panelRoot` 上的 `PIXI.Text` 在自然尺寸排版后被 `scale.set(scale)`（常见 ×3~4）整体拉伸，位图字形随之被拉糊（矢量 `Graphics` 描边不受影响，故边框清晰而文字糊——曾在 `CityScene.ts` Desk 详情弹窗上肉眼可见）。修法：`render/sketchUi.ts` 新增 `scaledTxt(scale)` 工厂，按 `scale` 提高该 `Text` 自身 `resolution` 抵消拉伸；`CardSceneCore`/`EquipmentSceneBase` 各包一层 `protected stxt(...)`（挂在 `toModalScreen` 旁，天然复用 `modalScale`），`CardScene/detail.ts`+`EquipmentScene/{detail,reforge}.ts` 里的 `txt()` 全部换成 `this.stxt()`。`CityScene.ts` 的 `renderDetailModal` 没有共享基类，改用局部 `const st = scaledTxt(scale)`（2026-07-26 起 `renderDetailModal` 随 `CityScene.ts` 拆分搬到了 `CityScene/modals.ts`，写法不变）。新弹窗只要走 `modalPanelRoot`/自建 `panelRoot.scale.set(scale)` 这套，就必须用 `stxt`/`scaledTxt`，不能直接 `txt()`。**2026-08-11**：`CardScene` 由 `XMixin(Base)` 继承链改判为组合（`base.ts`→`core.ts`=`CardSceneCore`，见上方拆分优先级条目），本行所有 `CardSceneCore` 方法/字段引用现指该 Core 类，机制本身不变；`feed.ts` 早于本次重构，仍保持局部 `S` 缩放不走 `modalPanelRoot`。 |
| `ui/GlobalToast.ts` | **全局兜底 toast**：浮在所有场景之上（挂 `app.stage` 屏幕坐标，不受 Contain 缩放/场景切换影响，跟随 resize），手绘风格梯形淡入淡出。专供漏网错误兜底，各场景仍用自身 `showToast`——「有提示则跳过、漏了才兜底」 |
| `net/apiErrorMessage.ts` | `uncaughtErrorMessage(reason)`：未捕获 reason→玩家可读文案。duck-type（`err.name`+`err.code`，避免与 ApiClient 循环依赖）识别 `ApiError`/`WorldApiError`（按 code 映射 `common.err.*`，未知码→`common.actionFailed`）/ `TypeError`（fetch 网络失败→`common.networkError`）/ 超时；普通 JS 异常→`null`（不弹，不拿 bug 吓玩家） |
| `net/log.ts` | 网络日志 + **全局错误兜底中枢**：`installGlobalErrorHandlers` 在 window `error`/`unhandledrejection` 把漏网错误经 `apiErrorMessage` 归类后弹 toast；`setToastSink`（app.ts 注入 `GlobalToast.show`）/ `showToastMessage`（定点本地化提示出口，供 SaveManager 云同步失败复用） |
| `game/campaign/levels.ts` + `levelSchema.ts` | 关卡注册（`CAMPAIGN_LEVELS`/`CAMPAIGN_LEVEL_ORDER`，61 关 JSON 单一来源）+ `parseLevelDefinition` 运行时校验，加载即 fail-fast。⚠️ 出兵/`activeLanes` 的 col 必为 `ATTACK_LANES=[0,1,2,3,4,7,8,9,10,11]`（5/6 是中央基地列，非攻击道——棋盘 6→12 列迁移后的历史坑） |
| `game/campaign/maps/` | 章节地图（CAMPAIGN_DESIGN §12）：`ChapterMap` 类型 + `parseChapterMap` 校验（节点 `levelId` 必在 `CAMPAIGN_LEVELS`，坐标 0..1 越界软告警）+ `CHAPTER_MAPS`/`getChapterMap` 注册；`chN.json` 存节点归一化坐标/`path`/`decor`，**只引 levelId**，与关卡数值分离 |
| `scenes/LevelPrepScene.ts` | **关卡准备界面**（CAMPAIGN_DESIGN §10 P1）：故事摘要面板（`brief`，`breakWords:true` 支持 CJK 自动换行，面板高度自适应文字行数）+ 关卡目标条（`objective?: ObjectiveSpec`，金色 accent 条，支持 survive/boss/destroy_base/timed_defense/leak_limit/escort 6 种，中英德均译）+ 单位卡牌 2 列网格（6 张卡排 3 行×2 列，垂直占用减半，合成按钮 + T3/T6/T9 特质标签保留）+ 体力条 + 开打按钮。`LevelPrepCallbacks.objective` 由 `createAppCore.goLevelPrep` 从 `LevelDefinition.objective` 透传。 |
| `scenes/CampaignMapScene.ts` | **战役笔记本**（PvE 正门，CAMPAIGN_DESIGN §12）：两类页——目录页（6 章卡片 + 进度/星数 + 锁章胶带遮罩）/ 章节页（节点按 `maps/chN.json` 归一化坐标摆放，`SketchPen` 铅笔虚线路径串联，已通关金圈星章 / 当前关蓝圈脉冲 / 未解锁淡铅笔轮廓 / decor 涂鸦）。进场落目录页→自动翻到「当前可打关」那章；翻页 = 横向 slide+fade（`update(dt)` 驱动，0.42s），左右箭头切章（下一章须通关方亮）；整章通关盖「第 N 章 · 通关」红章。章节页顶栏标题（「第 N 章 · 场地名」）下方补一行淡色叙事者归属「涛的笔记本 / Anna 的笔记本」（奇数章=涛/偶数章=Anna，对应 CAMPAIGN_STORY.md 框架表；`buildHeader` 的可选 `subtitleStr` 参数，TOC 页不传）。全程序绘制，零美术资产；回调 `CampaignMapCallbacks` 与旧扁平列表版同构。解锁/落点判断全走 `game/campaign/progress.ts`（不再内联）；纯绘制辅助（`drawNode`/`drawTrail`/`drawDecor`/`drawTape`/`drawClearStamp`，零/近零 `this.` 依赖）已抽到 `scenes/CampaignMapScene/drawing.ts`（form①），类文件缩至 450 行 |
| `game/campaign/progress.ts` | 战役进度纯逻辑（PIXI 无关，可单测）：`isLevelUnlocked`（前一关通关才解锁）/ `currentChapter`（第一个未通关关所在章）/ `currentLevelIdInChapter`（该章首个解锁未通关关=脉冲当前关）/ `parseLevelId`。节点 levelId 与 `CAMPAIGN_LEVEL_ORDER` 1:1 → 所见即所玩 |
| `game/meta/campaignRewards.ts` | **复合评分轴**（`STAR_SCORING.md`）：`computeStars(starThresholds, ctx)` + `buildStarContext` + `deriveParTicks` + `computeStarScore`。hp/speed/leak 三子分按关型加权成综合分 S，对照 `starThresholds`（S×100 门槛）。**通关保底 1★**（基地未打爆），基地打爆=0★（不入账/不解锁）。客户端 + 裁判 + `difficultySim` 同口径（ctx 由 `snapshotSummary()`+`snapshotStats()` 组装）。速度子分从 `waves` 推 par，解决旧 HP% 轴「通关即 3★」无梯度 |

## 测试 harness（test/harness/）

> 测试分层总览（单元 / UI 冒烟 / 渲染泄漏 / E2E）+ headless PIXI 适配 + 上线前浏览器冒烟方案：见 [`client-testing.md`](client-testing.md)。

| 文件 | 说明 |
|---|---|
| `pixiHeadless.ts` | UI 冒烟（`test:ui`）的 PIXI DOM adapter 桩：纯 JS canvas/context + 全局 `Image`/`HTMLImageElement`/`HTMLCanvasElement` 桩，让真实场景在 Node 里构建 PIXI 树、量文字，**不创建 Renderer**（不碰 WebGL）。配合 `vitest.ui.config.ts` 的 `stubBinaryAssets` 插件（`*.png`/`*.tao` import → 1×1 PNG data URI）跑通含完整 GameRenderer 的对战场景。 |
| `HeadlessPlatform.ts` | 无渲染 IPlatform（E2E + headless 单测共用）。**⚠️ 默认预埋 `tutorial_done:true`**（via `nw_save_v1` JSON flags）：`goLobby()` 有 FTUE 一次性门控（`firstLobbyHandled`），首次进大厅若 `tutorial_done` 未置位会跳转 `goTutorial()` → screen='game'，绕过 lobby。headless 测试不跑 FTUE，故默认预埋；若需测 FTUE 路径可 `opts.storage={nw_save_v1:'{}'}` 覆盖。 |
| `HeadlessAppViews.ts` | AppViews 空实现，记录当前 screen + callbacks；`driveToEnd()` / `driveReplayToEnd()` 无 ticker 驱动引擎到结束；`showGame()` 用 `createLocalMatch` 建本地引擎（`screen='game'`），`showGameNet()` 注入服务端引擎（`screen='gameNet'`）。 |
