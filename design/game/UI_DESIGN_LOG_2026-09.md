# 客户端 UI — 变更记录 2026-09

> 从 [`UI_DESIGN_LOG_2026-08.md`](UI_DESIGN_LOG_2026-08.md) 滚出（2026-09-15，原文件 1597 行）。**小节编号沿用原文**，源码里的 `UI_DESIGN.md §N` 引用仍然有效。
> 当前状态的规格看 [`UI_DESIGN.md`](UI_DESIGN.md) 和 [`UI_DESIGN_SCENES.md`](UI_DESIGN_SCENES.md)；本文只记「某天改了什么、为什么」。
> 在先记录见 [`UI_DESIGN_LOG_2026-08.md`](UI_DESIGN_LOG_2026-08.md)。**新增小节请追加到本文末尾。**
>
> **文件名里的日期是这一册开始的时间，不是它覆盖的范围。** 滚册的触发条件是行数（ADR-067 的 500 行），切口挑在语义接缝上，所以前一册末尾会有晚于本册名的条目。

---

## 48. 竖屏批量修：一套自动巡检 + 它查出来的七处重叠（2026-09-11）

**起因**：日常在电脑上开发＝横屏，竖屏问题只能靠人肉在手机上一屏一屏截图报过来，于是「横屏的大部分修完了，竖屏还是到处叠在一起」。

**先做工具**：`client/test/browser/portraitLayout.spec.ts` + `lib/layoutAudit.ts`（说明见 [`claudedocs/client-testing.md`](../../claudedocs/client-testing.md)「竖屏几何巡检」）。真 Chromium 走 **21 个场景 × 3 个竖屏尺寸**（390×844 / 360×640 / 768×1024），在页面里遍历真实 PIXI 显示树判 6 类问题，并给每站落一张 PNG。**这一层不可省**：headless `test:ui` 的 `measureText` 桩是「字符数 × 7px、跟字号无关」，而竖屏问题清一色是宽度不够导致的折行/溢出——在那层结构性复现不了（`titlesPortraitOverlap.ui.ts` 头注释早就写过同一件事）。

**首轮查出 7 处，全部已修**：

| # | 症状 | 真因 |
|---|---|---|
| 1 | 成就页 `Campaign`/`Battle`/`Progression` 三个 tab 文字互相压、`Progression` 整个跑出屏幕 | `HubTabs.drawHubTabs` 自己手搓 `[图标][gap][文字]` 居中，**从不看格子宽度**；改走 `drawButtonLabel`（它会整组缩放，太窄时直接丢图标保字号） |
| 2 | 设置页 `Rename (500 coins)` / `Delete Account` / `Replay tutorial` 文字冲出按钮，`Rename` 一路压到右边的 `Master` 滑杆 | **`drawButtonLabel` 把缩放算了两次**：`text.width` 是跟着 `scale` 走的 getter，`text.scale.set(fit)` 之后又 `text.width * fit` 去算居中偏移，于是标签被按「比实际窄」摆放＝整体右移出框。全游戏所有「太长要缩」的按钮都中过这一枪（含世界频道的发言按钮），一处修完全部好 |
| 3 | 大厅 WORLD 卡副标题「Clear Chapter 1 to unlock」横穿到右侧拍卖入口上 | `drawPillar` 的标题/副标题没有宽度钳制（同文件的 hero 副标题早就有一份，卡片这份漏了）；补 `fitToCard` |
| 4 | 设置页「数据节省」说明缩成 4 CSS px，完全看不清 | 长句强行**单行**缩放到 0.46w → 比例 ~0.2。改成按 0.76w **折行**（它本来就在开关行下方，横向不受开关挤压），字号锁 `FS.tiny` |
| 5 | 关卡准备页剧情段落压住「体力 120/120 · 消耗 10」 | 简报面板高度只按自己折行后的文字长，不知道下面还有底部锚定的体力行/开始按钮；改成传入 `maxBottom`，沿字号表逐档降到能放下为止，最小档还放不下才裁切 |
| 6 | 主城页耐久绿条画穿标题「Home City」 | ①`CityScene` 没把右侧簇宽度告诉 `drawSceneHeader`（有 `rightReserve` 参数但没人用）；②`drawSceneHeader` 的 `rightReserve` **只参与缩放不参与定位**——标题仍按整条 bar 居中，于是照样伸进预留区；③耐久簇宽度 = `headerH × 1.4`，竖屏 header 很高，簇吃掉 478/1080 设计 px，加上返回胶囊 443，留给标题的带宽是**负数**（`fit` 那行 `bandW > 0` 的短路让它干脆不缩了）。三条都修：簇不得超过 bar 宽的 34%、标题按预留区左移、带宽再窄也留 12% 的下限 |
| 7 | 防御编辑器竖屏基本不可用：14 个兵种卡挤在一行、字全糊成一团，`Build` 行标签跑到 x=-9 屏外 | 调色板按 `宽度 / n` 一行铺完，竖屏每格 72 设计 px 而「Shield Bearer」要 ~110。改成**按最长标签实测宽度决定列数**再换行（横屏 1920 仍是一行，不变），标签走 `drawButtonLabel`；行标签在没有左侧留白时改画在行内 |

**同时给巡检器定的两条精度规则**（否则它会变成噪音源）：
- **框是「画在文字下面、且能整个装下它」的那个盒子**。取「最紧的那个」会把装饰当框——大厅主按钮里那对交叉铅笔就是一张比按钮小的 sprite，按最紧取法，按钮上每个字都算溢出。
- **只有画在文字之后的实心矩形算「盖住」**。`Graphics` 的 bounds 不等于它涂到的像素：头像那圈 rim 是一张透明 PNG sprite、描边 `Graphics` 也只画边，把它们当遮盖物，头像里的字母全成了「被盖住」。放弃 z 序（想顺带抓「条画在标题下面」那种）会让每个普通按钮都中招，实测噪音从 9 条涨到 46 条，已回退。
- 引导气泡这类**本来就该盖在别人身上**的层，由产品侧显式标记：`GuideOverlay` 的根容器 `name = 'overlay:guide'`，巡检器只在同一层内互比。

**收尾状态**：三个尺寸全绿（`npm run test:portrait`）。**尚未处理**：竖屏下 `FS.micro`（11 设计 px）在 390 宽的手机上只有 ~4 CSS px——防御编辑器页脚、主城资源行那类小字确实小到读不了，但这是字号表和竖屏缩放比的系统性问题，不是重叠，另案。

## 49. 竖屏第二轮：可读性下限、主城版面、巡检扩面到 32 站 × 6 尺寸（2026-09-11）

接着 §48 那条「另案」办。四件事一次做完：**① 字号可读性下限**（§48 收尾那条）、**② 主城竖屏版面**（巡检看不见的那种不对劲）、**③ 巡检扩面**（战斗 HUD / 结算页 / 各种弹窗以前一站都没走到）、**④ 同一套巡检用到横屏**。

### 49.1 可读性下限：设计 px 不是屏幕 px（`render/fontScale.ts`）

**根因一句话**：竖屏设计宽度钉死 1080，390 宽的 iPhone 于是整屏按 **0.36×** 渲染，360 宽的安卓 0.33×。`FS.micro` 的 11 设计 px 落到屏上就是 **4 CSS px**。这不是「某处字号选小了」，是**字号表用的单位和玩家眼睛用的单位之间差了 2.8 倍**，所以修法也不该是逐处把 `FS.micro` 换成 `FS.tiny`。

**做法**：`FS` 的十个 token 全部改成 getter，读的时候抬到一条**由当前设计缩放比反推的下限**上：

```
floor(scale) = clamp(向上吸附到档位(MIN_LEGIBLE_CSS_PX / scale), micro, FLOOR_CAP)
MIN_LEGIBLE_CSS_PX = 7    FLOOR_CAP = FS.bodyLg (20)
```

| 视口 | 缩放 | 下限 | 效果 |
|---|---|---|---|
| 390×844 竖 | 0.361 | **20** | micro/tiny/small/body 全抬到 20（7.2 CSS px）；label 以上不动 |
| 360×640 竖 | 0.333 | **20**（撞上限） | 6.7 CSS px —— 见下方「没买到什么」 |
| 768×1024 竖 | 0.533 | **16** | micro/tiny 抬到 16（8.5 CSS px） |
| 844×390 横 | 0.361 | **20** | 手机横屏和竖屏是同一道算术 |
| 1366×768 横 | 0.711 | 11（= 不抬） | 桌面开发环境一个像素不动 |

三个刻意的设计点：

- **改 `FS` 而不是改文字工厂**。下限如果加在 `txt()` 里，字变大了、但它旁边那行 `y += FS.small + 6` 的排版算术**还按旧数**——正是行距压穿的配方。改成 getter，两边读到的是同一个数，~590 个调用点一个都不用动。
- **下限本身是个档位**（向上吸附），不是一个随缩放漂移的浮点数。被抬起来的 `micro` 和场景本来就写 `small` 的地方长得一模一样。
- **封顶在 `bodyLg`**。要让 390 宽手机上的小字真到 9–10 CSS px，下限得是 ~25 设计 px，那已经**高过 `FS.label`**：从小字到分区标题会全部塌成一个字号，整套竖屏 UI 得围着它重排一遍。封顶把返工圈在「本来就短的标签」上。
- **只看缩放比、不看朝向**。手机横过来照样 0.36×，同一套下限；桌面横屏问都不用问。

**没买到什么（老实话）**：360 宽的机器被封顶挡在 6.7 CSS px，还是低于 7 的目标；`body` 以上的档位在手机上依旧偏小（`FS.body` 18 → 6.5 CSS px，抬到 20 也才 7.2）。**真正的解法是竖屏的设计宽度**（1080 对 390 的屏太宽），那是另一件大得多的事，不在本轮。

**门禁**：巡检器的 `tiny` 判据改成**「有效字号 < 该视口的 `fontFloorDesignPx(scale)`」**，两边共用同一个函数（spec 直接 `import` `src/render/fontScale`，它零依赖）。

判据同时从「量 ink 盒高」换成「读字号本身」，因为盒高根本量不准：**烘焙标签（`cachedTxt`/`numTxt` 的 Sprite）报的是裁紧的字形盒（≈ 字号的 0.96 倍），活的 `PIXI.Text` 报的是行盒（≈1.35 倍）**——同一条「盒高」门槛要么放过被缩到一半的字，要么冤枉正常的字。活 Text 读 `style.fontSize`，烘焙标签读 `render/fastText.ts` 新盖的 `fsPx`（那个模块本来就为这套巡检往 `name` 上盖 `txt:` 前缀，多盖一个字号是同一笔账）。再乘上它头上累计的局部缩放 = 有效字号。`tiny` 条目的 `b` 字段现在直接写诊断：`font=24 scale=0.68`——「太小了」本身不可行动，「谁把它缩的」才是。

### 49.2 主城竖屏版面（→ `SLG_CITY_DESIGN.md` §8.15）

中段 1099 设计 px 的空白（47% 屏高）+「填满所有队伍」贴着 Team 5。新增 `CityScene/gridMetrics.ts`：竖屏下卡高由带高反推、列数取「卡最接近正方形」的那个（12 格 → 3 列 ×4 行 314×415），卡内偏移全部改成卡高的比例；队伍带头部加 18 px 留白、按钮加宽到 268。详见该节。

### 49.3 巡检扩面：22 → 32 站，含战斗 HUD、结算页和五个弹窗

旧表只走到「大厅一跳」和少数两跳。扩的依据就是报告自己记的 `cbKeys`（每站把 `state.<screen>Cb` 的键列出来——那就是导航图），新增：

- **回调可达的**：`mail` / `battlePass`（挂在抽卡屏和商店屏的 bag 上，不在大厅）/ `feedback`、`family`、`sect`（后三个这账号确实没有，按 `gated` 记为跳过）。
- **战斗**：`onStartGame('AI')` → `game`。这是唯一一站离开菜单外壳——战斗 HUD 直接由 `ILayout` 排版，是竖屏唯一能以「菜单场景不会出现的方式」坏掉的地方。
- **结算页**：`game` 的 bag 只有 `onGameEnd` 和 `onExitToLobby`，于是**直接把渲染器本来会递给它的那份收尾数据递给它**（`onGameEnd(0, [stats, stats])`，`PlayerStats` 照抄 `server/engine/src/types/runtime.ts` 的字段）。打完一局 AI 要几分钟，而这一站要审的是「一屏数字的排版」——数字哪来的不影响。
- **弹窗：回调够不着，只能点。** 弹窗是场景内部的命中区打开的，永远不会是一个 screen，也就没有任何回调可调。新增 `tapLabel(page, 文字)`：在真实显示树里按文字找到那个标签、点它的中心——真事件、真 InputManager、真命中表，跟玩家的手指走同一条路。于是有了 5 站：`cardRoster+detail`、`equipment+craft`、`gacha+draw`、`city+buildDetail`、`city+trainModal`。`Stop.as` 给这类站单独的报告名（`state.screen` 还是底下那个场景，两站会互相覆盖报告和截图）。
  > 点的尽量是 **UI 字符串**而不是内容名（用 `Power` 这个统计标签进英雄详情，而不是「李闯」），免得表依赖新号发哪几张卡。

### 49.4 六个尺寸：横屏走的是同一套

`VIEWPORTS` 加两行手机横屏/平板横屏 + 一行 1366×768 桌面。为此把设计矩形从常量改成**按视口算**（`designBox`：两种朝向都把短边钉在 1080、长边跟着长宽比走，竖屏不短于 1920，横屏在 1920–2592 之间），`tiny` 门槛同样按视口算。桌面那行的作用是「地板」——它的 0.71× 根本不触发可读性下限，所以它报出来的任何东西都是纯排版 bug，不是缩放假象。

跑完六个尺寸约 25 分钟（单个 `--grep <名字>` 约 3 分钟），per-test 超时从 5 分钟提到 10 分钟。

### 49.5 下限带出来的 7 处返工

抬字号必然撑破一些盒子。全部是巡检报出来的，按「为什么坏」分两类：

**A. 字号变大、盒子/行距没跟着（4 处，纯下限的锅）**

| 位置 | 症状 | 修法 |
|---|---|---|
| 主城建筑详情弹窗 | 「→ Lv.2 / 消耗 / 时长」三行互相压穿 | 行距从字面量 16 改 `max(16, FS.tiny×1.25)`，面板高同源；横屏取到 16，一像素不动 |
| 主城训练弹窗 | 队列状态行 456 设计 px > 340 的面板，跑出屏幕 | 折行，且**先量后定面板高**（面板先画，行数悄悄变 2 会从底下漏出去） |
| 英雄详情弹窗 | 「攻城: 0（每 60 兵 8）」越过面板右边 | 折行到 `statMaxW`（等级星本来就按这个宽度排） |
| 英雄列表卡名 | 名字被缩到 0.84（16.8 设计 px）还顶到卡边 | 竖屏的目标卡宽 300 → **480**（= 2 列）。反直觉但对：竖屏卡里那张肖像框宽度是**卡高**推出来的定值 177，卡宽多出来的每一像素都进信息栏，而信息栏才是要装下名字的那个 |

**B. 「装不下就整组缩」把下限吃掉（3 处，老问题被门禁照出来）**

`group.scale.set(可用宽 / 需要宽)` 这个写法在下限面前必然破功：它把刚保证过的字号乘上一个任意浮点。新增 `fitFont(size, need, avail)`——**改字号而不是缩已经画好的组**，沿档位表往下走、停在下限（UI 字体是等宽的，宽度对字号线性，所以 `size × avail/need` 就是该用的字号，不用迭代）。

| 位置 | 症状 | 修法 |
|---|---|---|
| 图鉴「类型 · 费用」行 | 24px 被缩到 0.68 = 16.4 | 三级降级再兜底：先 `fitFont` 降档 → 再**丢图标**（图标本就是词前面的提示，不是词的替代）→ 最后才缩 |
| 图鉴属性 chips | 缩到 0.80 = 19.3 | 竖屏图鉴改**一行一张卡**（原来两列，信息栏只剩 245 设计 px；插图是「边长 = 卡高」的正方形，卡宽减半全砍在信息栏上）。横屏仍两列 |
| 世界地图兵力读数 | 「5000/5000」缩到 0.71 = 19.7 | 两格从 50/50 改 **62/38**（「5000/5000」九个字符 vs 「11」两个），并用 `fitFont` 定字号 |

另有一处不是下限的锅、而是扩面照出来的老账：**战令每一行的「×N」和「已领取/未解锁」画在一起**——奖励组按整卡居中、状态标签钉右下角，两者互不知情。改成状态标签先画、把自己的宽度报给奖励组当预留（同 §48 第 6 条主城标题栏那招）。

**C. 按钮标签这一族（`ui/widgets/buttonLabel.ts`，全游戏 96 个调用点的唯一出口）**

它本来就有一条「装不下就丢图标」的规矩（`minFit`，默认 0.82 —— 2026-09-05 那轮 96 处按钮前置图标全量接线时的结论：**「放不下就丢图标」才是能无脑铺开的前提**）。下限来了以后这条规矩有两个洞，各由一个视口照出来：

- **阈值是猜的，下限是量的**：平板上防御编辑器 70 px 宽的页脚按钮需要缩到 **0.89**——过了 0.82 的槛，于是图标留着、`Clear` 被缩到 14.2 设计 px，低于该视口 16 的下限。判据补成 `fit < minFit || fit * size < 下限`：丢掉图标后标签原尺寸就放得下。
- **竖排（`stack`）那条路根本没有降级**：大厅侧边栏是正方形格子、图标在上文字在下，字号又是格子推出来的（0.30×），于是 `Feedback` 八个字符在**任何视口**下都是格宽的 **1.44 倍**——这条路一直在缩它，桌面上缩到 0.61（28px → 17，够读）就是它本来的样子。补成竖排的同族降级：**缩完低于下限时只画图标**——判据是下限而不是「放不下」，所以桌面/平板一个像素不动，只有 0.36× 的手机横屏换成图标；图标本来就是目的地自己的那枚（batch 5/6 美术），比一个糊成灰条的词值钱，徽标层照常标状态。
  > 中途写错过一版：判据写成「放不下就只画图标」，而这条路**每个视口都放不下**，等于把桌面大厅侧栏的五个词全删了。门禁抓不到这种（少画文字永远不会触发文字类判据），是对着「桌面上这条改动应该是 no-op」这个预期反推出来的。

三处 `fitFont` 的调用（上一节 B）也一并改成**只在「缩下去会破下限」时才降档**，不是无脑先降。两种降级不可互换，而且既有那条是刻意的：桌面窗口的下限就是原始的 `FS.micro`，任何缩放都破不了，于是这条改动在桌面**一个像素都不动**。

**D. `FS` 改成 getter 之后新出现的一类 bug：模块级常量会把下限冻住**

`const TEXT_STYLE = { fontSize: FS.tiny }` 这种写法现在是错的——模块初始化发生在 **import 时**，比 `ScalingManager` 算出缩放比还早，于是那份拷贝永远钉在未抬升的表上。全仓库有四处（`HUDView.ts` 两个样式常量、`WorldMapPanels/spec.ts` 两个字号常量），其中 `PANEL_BTN_FONT` 是真的在竖屏上少了 2 px（另外三处取的档位本来就在封顶之上，冻了也看不出来——**这类才是隐患**）。四处全改成函数，规矩写进 `fontScale.ts` 的 `FS` 注释里。函数体内的局部拷贝没问题：场景是在缩放比确定之后才构建的。

### 49.6 验证 / 没做的

- `tsc --noEmit -p tsconfig.test.json` + `tsconfig.fulllink.json` 干净；`npm run lint` 0 error；`webpack build:web` 过。
- 默认套件 + `test:ui` 全绿；新增 `test/render/fontLegibilityFloor.test.ts`（7 例，钉住五个真实视口的下限、封顶那条、退化缩放的兜底、`snapFont` 也带下限）、`test/cityGridMetrics.test.ts`（6 例）。改了 `test/ui/cardCodexPortraitWidthAndText.ui.ts`（竖屏一列、横屏仍两列各一例）。
- `npm run test:portrait` 六个尺寸 × 32 站全绿（单次全量 ~26 分钟）。中间态值得记一笔：**下限改完的第一轮，六个尺寸只剩 3 条 finding，且是同一族**（平板/平板横屏的 `Clear`、手机横屏的 `Feedback`），也就是上面的 C 两个洞——这轮的门禁确实能把「还差哪一类」收敛成一句话。
- **视觉核对**：报告自带每站 PNG（`client/portrait-report/<视口>/<站>.png`，dpr 2），主城/图鉴/英雄列表/战令/世界地图逐张看过。
- **没做的**：真机（本轮全在 Chromium）；微信小游戏入口（Playwright 到不了）；上面 §49.1 说的竖屏设计宽度那件大事。

## 50. 竖屏第三轮：给巡检喂真实数据 + 三语 + 微信入口盘点（2026-09-11 晚，**进行中**）

> **状态：2026-09-12 续做。** 工具侧全部落地并真跑过：结算数据的录制器现在自己打完一局（§50.7①）、
> seed 的数值全部对回了服务端常量（§50.8）、站点表最后一处到不了的站也修好了。
> **单视口已出结果（§50.9，90 条 finding），全量十行矩阵与逐张读图仍未完成，修复也还没开始。**
> 分支 `feat/portrait-seed`（worktree `.claude/worktrees/portrait-seed`）。

### 50.1 前两轮真正的盲区不是站少，是**站里没东西**

§48 走 21 站、§49 扩到 32 站 × 6 尺寸，都全绿收尾。但把 `phone-390x844` 的 32 张 PNG
逐张看下来，**绝大多数渲染的是空状态**：排行榜 1677 个号全是 elo 1000 / `Unranked`（名字还有一半
是 `#830489724` 这种没设 displayName 的公开 id），拍卖行 12 个标签、好友 10 个、活动 2 个，
邮箱进不去，家族/宗门报 gated 直接跳过。

**空表不会撑坏版面。** 版面是被内容撑坏的——四位数、满员列表、超长名字、带附件的长正文。所以这轮
的第一件事不是再加站，是**给已有的站喂满内容再走一遍**。

### 50.2 seed 为什么必须由 spec 在登录之后调用

`client/test/browser/lib/seed.ts` + `seedFixtures.ts`：直接往 docker 栈的 mongo 写，
`docker exec -i nw-local-mongo sh -c 'cat > /tmp/... && mongosh --file ...'`。

- **不走 UI**：40 人家族要 40 个真玩家、Top-100 要 100 个上榜号，UI 根本产不出来；能产的也要按站按
  视口各花几分钟。
- **不能预先写死**：socialsvc 只存 accountId，显示名是读的时候实时问 metaserver 的；家族/宗门的
  每一处权限判断都比 `leaderId`。所以成员必须是**真账号**、族长必须是**浏览器当次登录的那个号**
  ——而那是 `registerAndEnterLobby` 几秒钟前刚注册的随机号。**预先写好的 seed 不可能知道它。**
- **两段**：账号侧（卡/装备/邮件/好友/家族/拍卖/排行榜）在登录后立刻跑；世界侧（五队满编、
  行军/占领/驻扎/被围攻、宗门、世界频道）必须等 `joinWorld` 建出 `PlayerWorldDoc` 之后——所以
  spec 先走一趟世界地图，回大厅，再喂第二段。

**踩到的**：`mongosh` 直接吃 stdin 是 REPL——提示符 `rs0 [direct: primary] db>` 会跟脚本输出印在
同一行，而且逐行求值，多行 `function`/`if` 全看它的续行判断。改成 `sh -c 'cat > 文件 && mongosh
--file'`，多花一个进程换回正常脚本语义。结果行靠 `@@NWSEED@@` 前缀认，不靠行号。

**踩到的（二）**：`TileDoc._id` 已经是全限定的 `worldId:x:y`，`mainBaseTile` 就是一个 tile id，
不是 `x,y`。第一版按 `x,y` 拼、再自己前缀 worldId，写出来的行军/占领全落在不存在的格子上。

**内容的取值原则**：每个值都取**真实的最坏情况**——名字取到 `MAX_DISPLAY_NAME_LEN`(24) / 家族名取到
`ORG_NAME_WIDTH_MAX`(12 显示宽 = 6 个全角字)、金币七位、家族满 40 人、邮件 24 封（含 80 字上限的
标题 + 1200 字正文 + 四位数附件）、拍卖 44 条（跨页，混固定价/竞价）、排行榜 120 人四位数分档。
CJK 名字刻意占多数：全角一个字两个显示宽，是这个字段物理上最宽的可能。

### 50.3 站点表的三处真错（都是这轮才看出来的）

1. **`friends.png` 一直是三站互相覆盖的结果。** `goMail()` 和世界地图的聊天按钮都是
   `goFriends({defaultTab})`（`app/nav/social.ts`），三个入口都报 `screen: 'friends'`，于是共用
   一个报告槽和一个截图文件——**这个场景三分之二的表面从来没被审过**。已拆成
   `friends` / `friends+mail` / `friends+world` / `friends+mailRead`。
2. **feedback 根本不是一个 screen。** `onOpenFeedback` → `requestFeedbackDialog()`（`net/log.ts`）
   → app.ts 注册的 sink 把 `FeedbackDialog` 挂到 `app.stage`。巡检等了十秒的「切屏」永远不会来，
   于是记成「这个账号没有这个功能」。新增 `stay` 跳法（调完不等切屏）+ `reloadAfter`（大厅之上的
   浮层，`backToLobby` 拆不掉，只能刷新）。
3. **family / sect 是输在时序上，不是输在数据上。** `openFamilyHub()/openSectHub()` 在
   `loadSLGStatus` 解出分片 id 之前一律返回 false，而那个加载只在玩家切到对应 tab 时才跑。
   `callCb` 改成**返回 promise 就 await**，站点表里先跳一手 `{fn:'loadSLGStatus', stay:true}`。

### 50.4 三语：locale 是视口矩阵的一列，不是一个乘数

`initI18n` 在**任何场景构建之前**读 `localStorage['nw_locale']`，所以只能用
`context.addInitScript` 在首次导航前塞，不能事后去设置页点。此前每轮跑的都是 Chromium 自己的
`en-US` —— 也就是说**德语和中文从来没被门禁跑过**。

三语 × 六视口 = 75 分钟，太贵。改成**矩阵加四行**：德语和中文只上两台手机（390×844 / 360×640）。
德语是逐词最长的那门（撑破按钮的是它），中文是全角、且没有空格可供折行（是另一类失败）；
四个更宽的视口在英语下本来就有余量，多出来的字符吃得下。

连带的必然改动：`{tap: '文字'}` 这种站点在三语下会找不到标签，全部改成
**`{tap: <TranslationKey>}`**，由 spec 自己按该视口的 locale 查字典（直接 import
`src/i18n/locales/*`，纯数据、零运行时依赖），带 `{cost}` 占位符的取 `{` 之前的字面前缀。
另加一个 `{tapText}` 形式，只允许用于**本套件自己写进数据库的字符串**（那封 `Kampfbericht #1001`
邮件），因为它不依赖「这个号恰好发到哪几张卡」。

### 50.5 结算页：录一遍真的，而不是编一份像的

`result` 站原先喂的是手写 stats，`killsByType`/`castsByType` 是两个空对象 —— 那是屏幕上**最宽最高
的两块**，空的等于这一站审的是玩家永远看不到的一个画面。

改成两站：
- **`result`** 喂 `endStatsFixture.ts`，由 `captureEndStats.spec.ts`（`NW_CAPTURE=1` 才跑，平时
  skip）**真打完一局 AI** 录下来。巡检自己不打，是因为那要按视口付十次分钟级的代价，而这一站审的
  数字在比赛结束那一刻就定死了。
- **`result+extreme`** 保留一份合成的极值（七位伤害、五种兵种的击杀分布）。**录制的那份是真实的，
  真实就意味着它不极端**，两者不能互相替代。

### 50.6 微信入口：先纠正一个前提，再说能做什么

**`entries/wechat-e2e.ts` 不是 `web-e2e` 的孪生，也没有 `__nwE2E`。** 它的文件头这么自称，但它
实际做的只有一件事：构造 `WechatAudioBus`、在 SFX 总线上挂表、逐个 cue 量交付峰值，写一份 JSON 到
`USER_DATA_PATH`。**它不调 `startApp`、不建 PIXI、不挂任何 state/app 出口**，`wechat-probe.ts` 同理
（它只采宿主表面）。所以「把同一张 STOPS 表接上去」目前**没有可接的接口**。

**`claudedocs/client-testing.md` 第 284 行指的那条路（minium / 小程序自动化 SDK）是已经被证伪的死
路**，证据就写在 `wechat-e2e.ts` 自己的文件头里（2026-08-31 实测）：`miniprogram-automator` 能连上
socket，但每一个 `evaluate` / `callWxMethod` 都**永久挂起**——小游戏没有 appservice 供这些命令抵达。
这一条要从那份文档里改掉，别让下一个人再去试一次。

**因此真正可行的形状是「探针入口」，不是「外部自动化」**：一个 `build:wechat-*` 入口，在包内自己
启动游戏、自己走站、自己跑 `layoutAudit`、把报告写进 `USER_DATA_PATH`（开发者工具模拟器下那是本机
真实目录，会话能直接读）。这正是现有两个探针入口的形状。要做的前置：把 `instrumentViews` 从
`entries/web-e2e.ts` 抽成共享模块、把 `layoutAudit.ts` 从 `test/` 挪进能被打包的位置、给包内的走站
逻辑一份不依赖 Playwright 的实现。**本轮没做。**

**本轮做了的是那件更要紧、也更便宜的**：`src/render/textMetricsProbe.ts`——量
`ctx.measureText` 在这个运行时上到底给什么。理由是全仓库每个文字样式都写
`fontFamily: 'monospace'`，而 §49 的两条结论都压在它身上：`fitFont` 一次除法算出「装得下的字号」
只在「等宽字体宽度随字号线性」时成立，可读性下限则是一句关于「一个字最后有多大」的断言。小游戏
运行时没有 DOM、canvas 来自 `wx.createCanvas()`、`'monospace'` 由手机自己解析（很多安卓机对 CJK
给不出等宽面）。**同一个函数**跑两边——`wechat-probe.ts` 里喂 `wx.createCanvas()`，
`test/browser/textMetrics.spec.ts` 里喂 `document.createElement('canvas')`——因为要的是**差值**，
两个数必须由同一份源码产出。Chrome 那侧断言的是两条真正被依赖的性质（每字advance 与字号成正比、
`M` 和 `i` 等宽），不钉具体像素（那是 CI 镜像装了什么字体的性质，会为错误的理由变红）。

### 50.7 接手清单（2026-09-12 处理，逐条结果）

1. **`endStatsFixture.ts` 已录到** —— 但「无输入的 AI 局会不会自己打完」这个问题的答案是
   **「会，但要 17 分钟，而当时的上限写的是 15 分钟」**：PvP 局的保底终止是引擎自己的和棋计时器
   `FORCE_DRAW_THRESHOLD_TICKS` = 30600 tick / 30 Hz = **17 分钟**，那次跑的 15 分钟上限**低于它唯一
   可能到达的终态**。顺带纠正上一轮留下的第二个前提：**投降不是退路** —— 确认对话框的确认键调的是
   `onExitToLobby`（`GameRenderer/input.ts`），vs-AI 局里它直接回大厅，既不产出 stats 也永远到不了
   ResultScene，那条路上没有东西可录。
   而且就算等满 17 分钟也是**录错了东西**：AI 只打 Top 一侧（`AISystem.ts`），闲着的 Bottom 就是
   「没出兵、没击杀、没施法」，`killsByType`/`castsByType` 会和手写版一样是两个空对象——而那正是这份
   fixture 存在的理由。**所以录制器改成自己打**：走真实输入路径（点手牌 → 点格子，即
   `startTapSelect` → `commitCardPlay` 那条 tap-select 手势），坐标由
   `views.manager.current.renderer.core` 上的 `handView.slotCenter()` / `layout.gridToScreen()` 算出、
   再经 `container.toGlobal()` 换成页面坐标。**它打得很烂（第一张付得起的牌、第一条空着的道），这没关系**
   ——这一站要的是两侧都有真实的兵种分布，不是一盘好棋。结果：**35 手、2 分钟打完**，玩家侧赢，
   两边 `killsByType` 都是真的（含 `mara`/`max`/`lena` 这些英雄卡）。
2. **单视口接线跑通**。第一次跑当场炸在 `E11000 duplicate key: seedcard0000` —— seed 的固定 `_id`
   是纯计数器，而它的清理是**按当前账号**删的，所以第二个被 seed 过的账号必然撞第一个的文档。
   **那次 dry-run 是在干净库上做的，而干净库正是这个 bug 唯一看不见的条件。** 已改成所有固定 id 带
   账号短标签（`seedFixtures.tagOf`）。
3. **48 格地的事**：`tiles` 稀疏不是「多数格子没有文档」，而是**中立格根本不存在文档**（`proceduralTile`
   现算，见 `db/worldDocs.ts`）——那 9 格正好是主城自己的 3×3 footprint（anchor + 8 个 `baseRing`）。
   所以第一版不但一格新地没占，还往 ring 格上盖了 `garrison`（那种格子按设计只有归属和保护，没有驻军）。
   已改成跳过 3×3、把剩下 40 格按 `settleOccupation` 的字段逐个 upsert，并让 `seedWorld` 在落地数
   低于 24 时**直接抛错**——上一轮就是因为它一声不吭才让「占了 9 格」当了一整轮的既成事实。
4. 全量十行矩阵：见 50.10。第一次跑十行死了三次，三次都不是排版问题，而是**「单视口过了」和
   「十行都过」之间隔着的那些东西**：
   - `marches` 上有 `(worldId, ownerId, teamId)` **唯一索引**（一个 owner 的一支队只能有一条活着的
     行军）。seed 那条「被围攻」用的是 `botIds[0]` + `t1`，上一个账号留下的那条还在 → 六行死在
     `seedWorld`。看着像偶发，其实只是账号落在了哪个分片。改成**按索引键删**，不是按 `_id` 删。
   - 我自己加的那道「落地不足 24 格就抛错」的守卫**守错了东西**：它守的是邻里有多空，而世界是共享的、
     越跑越挤，于是它自己把整轮判红。改成**读回来验**（写完再 `countDocuments` 一次），守的是文档
     形状，不是这台机器的历史。顺带：别人家的普通领地直接接管（那些号跑完就废了），只避开城池和
     别人的主城 footprint。
   - **docker 栈的 mongo 从 2026-09-12 起开了认证**（compose 里 `MONGO_INITDB_ROOT_*`，每个服务连
     自己的库）。seed 一直匿名连着能用，**不是因为允许，而是因为「数据卷里还没有用户」时 Mongo 给
     localhost 例外**；那天第一次 `up` 建出用户之后，所有匿名读立刻 `requires authentication`。
     seed 改成带 `-u root -p localdev-root --authenticationDatabase admin`（可用 `NW_MONGO_USER/PASS`
     覆盖）——**跨四个库写，所以只能用 root：每个服务用户都被限定在一个库里。**
5. `claudedocs/client-testing.md` 已更新（几何巡检一节补了 seed / 三语 / 站点表三处修正；minium
   那句按 §50.6 改成了「已被证伪，别再试」）。

### 50.8 最贵的一课：**seed 的数值必须是游戏自己能产生的数值**

`seedFixtures.ts` 的文件头第一句写的就是「每个值都取真实的最坏情况，而最坏情况必须是真的」。
2026-09-12 单视口第一次真跑下来 105 条 finding，**逐条对回服务端常量之后，其中 60 条是在量这份
fixture、不是在量排版**：

| 字段 | seed 写的 | 游戏的上限 | 权威 |
|---|---|---|---|
| 装备词条 id | `p_crit` / `p_def` | 这两个 id 不存在（未知 id 回落成 `p_def +2673` 原样打印） | `SUB_AFFIX_POOL` |
| 装备词条数 | 固定 4 条 | 0/1/2 条，由稀有度定 | `CRAFT_SUB_AFFIX_COUNT` |
| 主词条值 | 存 8630 | **存 base（8/10/6），屏幕自己乘 `enhanceMultiplier`**，+9 也就是两位数 | `MAIN_AFFIX_BY_SLOT` × `ENHANCE_LEVEL_MULTIPLIER` |
| 卡等级 | 60 | 9 | `MAX_CARD_LEVEL` |
| 单卡兵力 | 24800 | 600（李闯）/ 300 | `cardTroopCap` |
| 兵力池 / 上限 | 1482600 / 1600000 | 20000 | `troopCapFor({drillYard:10})` |
| 建筑等级 | 到 20 | 10 | `BUILDING_MAX_LEVEL = DESK_MAX_LEVEL` |
| 资源 | 842 万 | 60 万 | `RESOURCE_CAP × (1 + cabinet×0.20)` |
| 每小时产出 | 12.8 万 | 每格 `100 × level` | `RESOURCE_YIELD_BASE` |
| 地块驻军 | 48200 | 500 起（`npcGarrison` = 120×level 托底） | `GARRISON_PER_TILE` |
| 行军兵力 | 148200 | 20000 | `satchelCarryCapFor` |
| 邮件标题 | `.slice(0, 40)`，而源串本身只有 36 字 | **80 字**（全角即 160 显示宽） | `MAIL_SUBJECT_MAX` |

**这类 finding 比没有 finding 更坏。** 它不是噪音——它长得和真 bug 一模一样，带着坐标和字号，会把人
送去「修」一个玩家永远看不到的字符串；装备页那 60 条 `unreadable` 全是 `fitFont` 在为宽出四五个字符
的假串缩字号。全部按上表改回真上限之后，**同一个视口从 105 条降到 90 条，而剩下的 90 条条条能读**
（`Troops 1500/1500`、`Crit Damage +30%`、`Health +50%`）。

**规矩**：往 seed 里写一个数之前，先找到定义它上限的那个常量，并把常量名写在注释里。找不到上限的字段
（家族繁荣度一类）可以估，但要在注释里说明它是估的。

### 50.9 phone-390x844 真跑出来的东西（2026-09-12，seed 修正后）

35 站全部到达（`skipped: []`），90 条 finding，六族：

1. **主城队伍条**（`city` / `city+buildDetail` / `city+trainModal` 各 5 条 offscreen + 5 条 overflow）：
   `Heroes 5   Troops 1500/1500` 折成两行、36 px 高塞进 20 px 高的条里，且底边落在 y=824+36 > 844
   ——**直接跑出画布**。五支队伍五条，是这轮最整齐的一族。
2. **队名 × 状态重叠 26%**（`city` 5 条）：`破晓先锋营` 和它下面的 `Marching`/`Injured 30m`。
3. **训练队列三行互相重叠 21%**（`city+trainModal` 2 条）：三个并行槽位的行距小于行高。
4. **装备词条被缩到读不了**（`equipment` 45 条）：`Crit Damage +30%` 的有效字号 11.8 px
   （font=20 × scale 0.59），`Health +50%` 17.2 px——列宽不够，`fitFont` 一路缩下去。
   **这是真的**：这些字符串就是游戏能产生的最宽词条行。
5. **好友申请行：长名字压住 Accept/Decline**（`friends` 5 条）：`Maximiliane-Vandenberghe`（24 字，
   `MAX_DISPLAY_NAME_LEN` 上限）同时压到两个按钮。
6. **邮件标题溢出行框**（`friends+mail` 2 条）+ **反馈弹窗正文溢出对话框**（`feedback` 1 条）。

**顺带修掉的巡检自身缺陷**：`friends+mailRead` 之前一直被记成「到不了」。原因是 **tap 型跳法在导航跳法
之后是「屏一变就点」**，而邮件列表的行是服务端填的——点的时候一行都还没有。改成**轮询到 6 秒**
（`TAP_WAIT_MS`）。证据很刺眼：它要找的那个标签，就明明白白印在**前一站的截图**里。

### 50.10 全量十行矩阵（2026-09-12，686 条）

十个视口全部 36 站到齐，`skipped: []`、`blank: []`。**一条都还没修**，下面是分布：

| 视口 | 条数 | 主要来源 |
|---|---|---|
| phone-390x844-de | 130 | 装备 45 / 拍卖 15 / 主城 17 / 训练弹窗 14 / 打造 9 |
| narrow-360x640-de | 112 | 装备 36 / 拍卖 15 / 主城 16 / 训练弹窗 14 |
| phone-390x844 | 90 | 装备 45 / 主城 15 / 训练弹窗 12 |
| narrow-360x640 | 79 | 装备 36 / 主城 15 |
| phone-390x844-zh | 61 | 主城 15 / 装备 17 |
| narrow-360x640-zh | 58 | 主城 15 / 装备 14 |
| tablet-768x1024 | 55 | 装备 28 / 主城 10 |
| tablet-1024x768 | 35 | **卡牌列表 32**（横屏独有） |
| landscape-844x390 | 34 | **卡牌列表 30**（横屏独有） |
| desktop-1366x768 | 32 | **卡牌列表 30**（横屏独有） |

三条结构性的读法：

1. **德语是最狠的一列，而且它开的是新面**。拍卖（15）、战令（5）、称号（2）、打造（8–9）、
   防守编辑器（2–3）、结算（1）、战役地图（1）——这些站在英语下全绿。§50.4 把德语只挂在两台手机上
   是对的赌注：它一个视口就多报了 40 条，且全在别的语言碰不到的地方。
2. **横屏三个视口只报一件事：卡牌列表 30–32 条 `tiny`**，而竖屏一条都没有。也就是说
   `cardRoster` 的缩字号发生在**宽而矮**的形状里，和竖屏那些窄列问题不是同一个 bug。
   `desktop-1366x768` 这一行本来是「地板」（0.71× 不触发任何下限），它报出来就说明这是真排版问题。
3. **`friends+mail` 的两条在十个视口里条条都有**，包括桌面。邮件行装不下一个合法的 80 字标题，
   和屏幕大小无关。

修复顺序（按「一处改动能消掉多少条」×「玩家多久看见一次」排）：主城队伍条（5 个竖屏视口 × 15，
且**真的跑出画布**）→ 装备词条行（约 200 条）→ 卡牌列表横屏 → 好友申请行 → 邮件标题 → 德语那批。

### 50.11 修了什么（2026-09-12，686 → 50）

九处改动，全部在 `tsc` + 3637 单测 + 2640 UI 测 + `webpack build:web` 下过。**每一处都先看过截图**
（`client/portrait-report/<视口>/<站>.png`，dpr 2），不是只读报告数字。

| # | 处 | 症状 | 真因 | 改法 |
|---|---|---|---|---|
| 1 | 主城队伍条 | 五队的「Heroes 5 Troops 1500/1500」折四行、**掉出画布**；队名压住 `Marching` | 五张并排 = 每张 197 设计 px，减去内边距和 76 px 的领队头像只剩 **93 px 文字列**，而 §49 的下限把这里每个字号都抬到了 20 —— 一行装不下四个全角字 | 竖屏改成**两行 3+2**（列宽翻倍到 242），卡内行距从写死的 26 改成 `FS.body × 1.3`，底部那行按**实测高度**贴底而不是按固定 34 px 预留 |
| 2 | 装备词条行 | 45 条 `unreadable`，`Crit Damage +30%` 有效字号 11.8 px；改成折行后第三条又被按钮带**盖住** | `line.scale.set(colW / line.width)` —— `fitFont` 存在就是为了替掉这个写法 | **反过来算**：先量最宽的那条词条，再由它决定图标能有多大（`boxByWidth`），图标让出的宽度正好是词条列缺的宽度。图标缩小后在原来的带里**居中**，留白读作边距而不是一个洞 |
| 3 | 好友申请行 | 24 字名字同时压住 Accept 和 Decline | 名字没有任何宽度上限；而这一行的字号是按**行高**算的，竖屏设计框是高度跟随的（390×844 → 2337 设计 px），于是一行 210 px 高、名字 67 px | 先算按钮几何，再用 `fitFont` 把名字夹在按钮之前 |
| 4 | 邮件标题 | **十个视口条条溢出**，桌面也不例外 | `MAIL_SUBJECT_MAX` 是 80 字，全角即 160 显示宽，没有任何一行装得下 | 新增 `txtFit()`：**先降字号（停在下限），真不行再截断加省略号**。列表行给预览，全文在阅读页 |
| 5 | 训练队列 | 三个并行槽位互相压 21% | 行距写死 16，而下限把 `FS.tiny` 抬到 20（行高 ~26） | 行距和面板高度都改成按 `FS.tiny` 算 |
| 6 | 卡牌列表「已出战」徽标 | 横屏三个视口各 30–32 条，有效字号 16 px | 又一个 `tag.scale.set(rightW / tag.width)` | `txtFit` |
| 7 | 反馈弹窗 | 正文「溢出」一个不是它的框 | **误报**：它的背景是 0.45 透明遮罩，而审计只把**不透明**矩形算作遮挡，于是大厅的面板留在了比较集合里 | 根容器打 `overlay:feedback`（和引导层同一个机制）——层内互比，内部排版照判不误 |
| 8 | seed 兵力 | 队伍条印出 `Troops 1500/1300`（带的比装得下的多） | 按 defId 一刀切给满编值，可那 25 张卡里前几张是账号自己的 **1 级新手卡** | 连 `level` 一起读，按 `cardTroopCap` 逐卡算 |

**一般化的两条**：
- **`scale.set(可用/需要)` 是这一轮六处里的三处**。它把下限刚刚保证的字号乘上一个任意浮点数，结果落在
  哪儿全看那个比值。凡是看到这个写法，改 `fitFont`（选一个更小的档位、停在下限）或 `txtFit`（再不行就截断）。
- **写死的行距在有下限之后全是错的**。`y += 26` / `y += 16` / 「底下预留 34 px」这三处都是同一个 bug：
  它们是按下限抬升之前的字号量出来的。行距要么按 `FS.x × 1.3` 算，要么按实测 `height` 贴。

### 50.12 还没修的：50 条，**全部是德语**

十个视口里八个清零（英语两台手机 + 中文两台手机 + 平板竖屏 + 平板横屏 + 手机横屏 + 桌面）。剩下
`narrow-360x640-de` 33 条、`phone-390x844-de` 17 条。

> **拍板（2026-09-12，用户）：德语放不下时的处理顺序是「先折行，再缩写」。**
> 1. **能折行就折行**，字号留在可读性下限上，按钮长高一点没关系；
> 2. **折了还是放不下，就把德语串换成缩写**（改 `i18n/locales/de.ts` 的值，不改 key）——
>    **每颗按钮都有图标，图标才是认路的东西，字只是确认**。
>
> 两条推论，写死在这里免得下一轮又漂回去：
> - **不许再缩字号**（`scale.set` / 缩到下限以下）——那是这一轮刚清掉的反模式，见 §50.11；
> - **按钮不许用省略号截断**。`txtFit` 的截断分支是给**列表行**的（邮件标题那种：预览在行里、全文在点开的
>   页面里）；一颗写着「Schmied…」的按钮没有「点开看全文」这一说，该缩写就光明正大地缩写。
>
> 连带要做的改动：`drawButtonLabel` 目前**根本没有折行这条路**，按钮标签一律单行。要按上面第 1 条办，
> 得给它加一条「两行」分支（并让按钮高度容得下），同时把 §50.11 末尾记的那个洞补上——
> **字号已经等于下限时 `fitFont` 返回原值、`floorFit` 返回 false，后面照样 `scale.set`**：
> 那个分支现在应该改成「试折行 → 还是不行就停在下限（由缩写去解决）」，而不是继续缩。

四族，每族都需要一次真正的版面判断而不是一个夹子：

1. **拍卖倒计时压买入按钮（两个德语视口各 15，占剩余的六成）**。`0T 12Std 59Min 23Sek` 在右栏折两行，最后一行伸进右下角那颗
   96 px 的按钮。夹宽度没用：从文字列左沿到按钮左沿只有 ~75 设计 px，而这串在下限上要 240 px。
   **真答案是这张卡的右栏对德语太窄**——倒计时得自己占一行，或者按钮换位置。
2. **按钮标签被缩到下限以下（`narrow-360x640-de`：打造 8、防守编辑器 2、主城加速 1；390 宽的那台刚好够，一条没有）**。`drawButtonLabel` 已经很小心地处理了
   下限，但它有个洞：**当字号已经等于下限、再缩一档也还是下限时，`floorFit` 返回 false，后面照样
   `scale.set`**。`Schmieden` 就是这样落到 0.71 的。可选的三条路（加宽按钮 / 折行 / 接受溢出）都要按
   钮逐个判断，不能一刀切——所以这里先记下病因，不硬改。
3. **重叠**：称号页的 `Ranglistenprofi` 压 `Angelegt`（两个视口各 1）。战令那 5 条（`Gesperrt` 压 `×25`）在训练弹窗行距修好之后自己消失了——同一族的写死行距。
4. **溢出框**：`NOCHMAL KÄMPFEN`（结算）、`Sekten durchsuchen`（宗门搜索）、`(zum Entfernen tippen)`
   （称号提示）、训练队列的德语条目、战役地图 1 条 —— 都是按钮/框按英文宽度定的，只在 360 宽的那台上炸。

**修完之后，德语仍然是唯一还红着的一列**：英语和中文的同一台手机是 0，德语是 17。开的也全是别的语言
碰不到的面（拍卖、称号、打造、防守编辑器、结算、宗门）。§50.4 把德语只挂两台手机上的赌注是赢的——
它一个人扛住了整轮剩余的 100%。

### 50.13 德语那一列清零（2026-09-12，73 → 0）

> **先更正 §50.12 的一个数。** 那节写的「剩 50 条：narrow 33 + phone 17」里，33 是对的，17 不是。
> 重跑基线量到的是 **phone-390x844-de 40 条**（拍卖 15 / 打造 9 / 战令 5 / 防守编辑器 3 / 主城 2 /
> 训练弹窗 2 / 称号 2 / 宗门 1 / 结算 1），也就是**总数 73**。§50.12 只把「拍卖 + 称号」两族记进了
> 390 那一行，其余七族当时归给了 360——可 §50.10 自己的表里 390 明明写着「打造 9」。
> **教训和 §50.8 是同一条**：分布数字要么从报告里现算，要么就别写进文档；凭印象归族，下一轮接手的人
> 会照着一个错的清单去找活干。

两个德语视口现在都是 **0 条 / 36 站全到**（`skipped: []`、`blank: []`），十个视口全绿。

#### 按拍板的顺序，三个答案各自用在哪

**① 加宽**（有空地就先花空地——比折行还便宜，ruling 没写但它是 ① 的前一格）

| 处 | 原来 | 现在 |
|---|---|---|
| 打造按钮 `EquipmentScene/craft.ts` | 写死 `min(104, …)`，而格子右栏有 222 设计 px 空着 | 按标签量宽（`Schmieden` + 铁砧 = 142） |
| 防守编辑器 Save/Clear `DefenseEditorScene/render.ts` | 写死 70，而底栏有 ~900 px 空着 | 按两个标签里最宽的那个量（`Speichern` = 103） |

**② 折行**（`drawButtonLabel` 新增的两行分支）

| 处 | 德语串 | 按钮 |
|---|---|---|
| 主城加速 `CityScene/render.ts` | `Beschleunigen (90 Münzen)` | 45 → 58 高 |
| 宗门「浏览宗门」`SectScene/render.ts` | `Sekten durchsuchen` | 72 → 84 高（72 差 4 px 装不下两行 `FS.heading`） |
| 结算主 CTA `ResultScene/builders.ts` | `NOCHMAL KÄMPFEN` | 高度本来就够，改的是它终于走了共享控件 |

**③ 缩写**（改 `de.ts` 的值，key 不动）——只有两处，都是**折行本身就是病因**的场合：

- `auction.timeLeft`：`{d}T {h}Std {m}Min {s}Sek` → `{d}T {h}h {m}m {s}s`。市场卡右栏在竖屏三列
  网格里只有 167 设计 px（≈14 个等宽字），20 个字符必然折成两行，而**第二行正好落在右下角那颗买入
  按钮上**。这一族占了 73 条里的 30 条。缩写后与英语同形，而英语这一格一直是绿的。
- `titles.tapUnequip`：`(zum Entfernen tippen)` → `(zum Ablegen)`。称号卡 184 px 宽，原串 252 px，
  两边都溢出；这行下面没有「点开看全文」，所以不能截断。

#### `drawButtonLabel`：补上那个洞，并且补的是**两个**洞

§50.12 记的病因是「字号已经等于下限时 `fitFont` 返回原值、`floorFit` 返回 false，后面照样 `scale.set`」。
真正改完是两条：

1. **两行分支**。触发条件是「**一行装不下且会破下限**」——精确地说是 `min(1, fitW/text.width) * size <
   下限`，用的是「丢掉图标之后一行还能怎样」，不是组的 `fit`。因为丢图标本来就是这个函数已有的答案
   （`minFit`），而且它更便宜。这个门槛意味着**今天能读的一个按钮都不会换形状**：八个已经清零的视口
   里这个判断恒为假。两行还要求盒子装得下（`2 × 行高 ≤ h - 2`），装不下就退回去——这就是上面
   「宗门 72 → 84」那一行的由来。
2. **`scale.set` 一律夹在下限上**（solo 分支和组分支各一处）。夹住之后放不下就**溢出**，不再缩。
   这是故意的：溢出会被巡检报成 `overflow`，把修法推回调用点（加宽）或推回翻译（缩写）；缩到 14 px
   只会留下一团看着像「刻意的小字」的灰影。

`splitButtonLabel` 按空格取**最平衡**的两段（按字符数，因为 UI 全是等宽字，跟 `fitFont` 同一个前提）。
没有空格就返回 `null`——德语复合词（`Schmieden`）和所有 CJK 串都走这条，对它们「折行」根本不在选项里，
这也正是 ③ 存在的理由。

#### 另外四处：`scale.set` 之外的同族错误

- **场景标题 `SceneHeader.ts`**：`titleNode.scale.set(fit)`。标题字号跟着**栏高**算（`backSize`），
  可用带宽跟着**栏宽**算，竖屏这两头反向拉——390×844 的设计画布高 2337，于是要 84 设计 px 的标题
  去挤一条被右侧簇切到 ~130 的带子，乘出 0.20，即 17 px（`Heimatstadt`、`Hauptstadt-Verteidigung`）。
  改成 `fitFont` 选档、停在下限；**下限之后让出宽度的是图标不是字**（同 §50.11 #2 的取舍），图标小到
  读不出图形就整个丢掉。
- **战令奖励行 `BattlePassScene/cell.ts`**：`bandW = Math.max(ic, …)` —— 这个 `max` 在状态标签很宽时
  **把带子外的宽度又还给了组**。`Gesperrt` 在 465 宽的格子里占 282，`×20` 那组就压进去 36 px。改成
  先按带子夹字号（`fitFont`，停在下限），**再由数字剩下的宽度决定图标能有多大**，不够就不画图标。
- **称号卡 `TitlesScene.ts`**：徽标按「离卡片底边固定偏移」放，完全不知道上面那行名字折了几行；
  而同一张卡的提示行**已经**有 `contentBottom` 保护（2026-08-11 加的），所以只有徽标撞。改成
  **先量字、后定奖章**：底部块（徽标+提示）作为一个整体向下让，奖章框从 0.44 压到「标签剩下多少」
  （不低于 0.28，再低就不像奖章了）。
- **训练队列行 `CityScene/trainModal.ts`**：同一个面板里，状态行早就「先量后定高」，队列行却没有。
  德语 `Bildet aus: 5000 · noch 1:07:17` 是 320 px 对 320 px 的面板，直接跑出去。改成同样先量再累加进
  `contentH`。

#### 战役地图：这一条不是夹子能解的，是**排版本身错了**

`Kapitel 2 · Trainingsgelände` 压住右上角的 `Kapitel` / `Ausrüstung` 两颗快捷键。第一版只给标题加了
带宽夹（`fitFont` + 按 `rightX` 截右沿）——**跑完仍然压着**，因为算下来带子只有 181 px，而这串到下限
也要 341 px：夹子只能让它从「大字压上去」变成「小字压上去」。**逐张读图才看出来的**，报告已经是 0 条
（字变小之后重叠面积掉到阈值以下）。

真答案：这条栏在竖屏是 230 设计 px 高，章节页本来就当两行用（标题在 0.40、笔记本主人在 0.72），
**把两颗快捷键挪到主人那一行**，标题这一行就整条空出来，需要的 545 px 直接够用，一点都不用缩。
没有副标题的目录页（快捷键只有一颗、标题也短）维持原样。

#### 两处**巡检看不见、只有读图才发现**的

1. **拍卖倒计时被买入按钮切掉一行像素**。缩写之后它变成一行，但落点仍在按钮带里 10 px；按钮的不透明
   填充盖在字的最后一排像素上。`covered` 的面积占比够不到阈值，所以**九个视口的绿都没拦住它**——
   缩写后的德语和英语（`0T 9h 59m 24s` / `0d 9h 59m 24s`）等长，也就是说**英语一直是同样被切的**，
   只是没人去看。修法是 `AUC_CELL_H` 180 → 200：
   格子是上对齐的，按钮之上的一切原地不动，20 px 正好是量出来的差额。
2. **主城建造队列的表头和条目粘成一串**：`Bauwarteschlange` 16 个字符，写死的 `cx0 + 195` 起点被它
   吃掉 12 px，屏幕上读作 `BauwarteschlangeStadtmauer Lv.10 · …`。改成按表头**实测宽度**起排。

#### 而抬高 `AUC_CELL_H` 又炸出了第四条——**一条一直在那儿的 cull bug**

十行矩阵跑完，`narrow-360x640` / `tablet-768x1024` / `landscape-844x390` 三行**英语**红了 9 条：
最后一行卡片的价格和倒计时压在底部那颗「+ 上架物品」按钮上。

拍卖网格**没有 mask，只有画不画的 cull**，条件是 `y + AUC_CELL_H >= listY && y <= listY + availH`
——「**顶边在视口里就整张画**」。也就是说底部那一行本来就会往视口外画最多一整个卡片高，只是
**行边界正好落在哪儿**决定了它咬不咬人；把格子高抬 20 px 就够把它推过去了。顶边那个条件同理：
往上滚到一半的那行会整张画出来，盖住上面的分类条。

改成和打造/背包网格同一个形状（`gridLayer` + `clip` Graphics + 换 `core.bodyLayer`）。原来那条
「不设 mask，所以一行要么整张画要么不画」的注释**从来就不成立**，已经改掉。

**这条的教训**：一个看起来只是「+20 px」的常量改动，会把**依赖巧合的几何**暴露出来。别把红当成
「新改动写错了」——先问「它是不是本来就错，只是以前没被踩到」。这次是后者，而且修法比回退好。

**这一节最该记住的一条**：`npm run test:portrait` 全绿只是「没有超过阈值的重叠」，不是「这一屏是对的」。
这一轮有三条真缺陷（战役地图标题、拍卖倒计时、主城队列表头）在报告上都是 0，全部来自逐张读 PNG。
§50.9 的那句「逐张读图」不是流程洁癖，是这套门禁的**必要补集**。

## 51. 微信侧：把同一张 STOPS 表接进包内探针入口（2026-09-12）

§50.6 把这件事的形状定死了，但没做：**「包内探针」，不是「外部自动化」**。minium 那条路 2026-08-31
已实测证伪（socket 连得上，每个 `evaluate` 永久挂起，小游戏没有 appservice），所以唯一可行的是
一个 `build:wechat-*` 入口，在包内自己启动游戏、自己走站、自己跑 `layoutAudit`、把报告写进
`USER_DATA_PATH`（开发者工具模拟器下是本机真实目录，会话直接读）。这一节把它做完了。

### 51.1 三个前置，按「共享数据、不共享机制」切

| 搬到哪 | 是什么 | 两边怎么用 |
|---|---|---|
| `src/testing/instrumentViews.ts` | `__nwE2E` 句柄（原来长在 `entries/web-e2e.ts` 里） | 两个入口装同一份；web-e2e 只在上面多挂 `bake` / `textMetrics` |
| `src/testing/layoutAudit.ts` | 判 6 类几何问题的那个纯函数（原 `test/browser/lib/`） | Playwright 交给 `page.evaluate`；微信包直接调 |
| `src/testing/layoutStops.ts` | **36 站的表**（原来写死在 spec 里）+ `endStatsFixture` | 两边 import 同一张 |
| `src/testing/layoutWalk.ts` | **包内走站**，新写的 | 只有微信侧用 |

**走站逻辑是第二份实现，不是共享的**，这是有意的：两边真正不同的恰好是三件机制——怎么送一次点击、
有没有 reload、报告怎么出去——其余全是数据。表是两者之间的契约，往表里加一站，两边都会走。

两处必须点名的改动：

- `__nwE2E` 挂在 **`globalThis`** 上，不是 `window`。小游戏运行时没有自己的 `window`，而
  `layoutAudit` 的函数体要被 `page.evaluate` 序列化、在两个运行时里各跑一遍，所以那个名字必须两边
  都认。
- **点击**：浏览器侧点真实屏幕像素、由平台适配器换算；包内没有适配器事件可发，于是两半手工做——
  `ScalingManager.toDesignSpace` 就是每个适配器用的那个变换，`InputManager._emit*` 就是它们喂的那个
  漏斗。够不到的是 PixiJS 自己的事件系统（`eventMode`/`pointertap` 的舞台级弹窗），但 `STOPS` 里
  没有一站需要它——每个 tap hop 打的都是场景内部的命中区。

### 51.2 入口：`entries/wechat-layout.ts`（`npm run build:wechat-layout`，永不发布）

与 `entries/wechat.ts` 逐行同构（`installHost` 第一个 import、`@pixi/unsafe-eval` 紧随、
assetIO/transport/audioBus 三行照装），**只换一个方法**：`getAuthCredential()` 返回 device 凭证而不是
`wx.login` code。理由是 `app/nav/auth.ts` 的 `resolveEntry()` 把 `kind:'wx'` 当「已识别」直接进大厅——
这个平台上根本没有 LoginScene，而巡检需要一个干净的、已知的号，唯一的门就是 `loginCb.onRegister`，
和浏览器巡检同一扇门。

两处配套：`webpack.config.js` 给这个 target **默认烘本地栈地址**（`http://localhost:8088`，
`NW_*` 仍可覆盖）——和 `playwright.portrait.config.ts` 同一个理由：地址错了不会响亮地失败，只会注册
不上，然后三分钟后每一站都报 gated。另一处是 DevTools 的「不校验合法域名」要勾上（`localhost` 不在
request 合法域名里）——**这一条没有写进仓库**：`project.private.config.json` 是共享设置，为一个探针
把它长期关掉不值得。

**截图**走 `canvas.toTempFilePathSync({fileType:'png'})` + `fs.copyFileSync`；前一行的
`app.renderer.render(app.stage)` 是承重的（PIXI 的 `preserveDrawingBuffer:false`，出了这一帧就不保证
读得回来）。PixiJS 自己的 `renderer.extract.base64` 是**退路而不是首选**——实测在这个运行时抛
`ImageData is not defined`（`Extract.canvas` 要造一个交给 `putImageData`）。

### 51.3 跑出来的三件事（一件是修好的，两件是新挖出来的）

#### ① `AbortController is not defined` —— 微信包里的 REST 从来没通过

第一次真跑，第一件事就炸：注册返回 `auth.err.network`，`detail` 是 `AbortController is not defined`。
全文见 `ASSET_PACKAGING_LOG.md` §22。修法是 `platform/wechat/abortShim.ts`（~40 行）。
改前 10/36 站，改后 **35/36**。

#### ② `state.screen` 会说谎，而且有两种说法

**`instrumentViews` 在 `show*` 方法**被调用时**写 `state.screen`，于是有两条路让这个名字和屏幕脱节：

- **弹出 overlay 再弹回**，中间不跑任何 `show*`。CityScene 是盖在活着的 WorldMapScene 上的
  （`app/nav/world.ts` 的 `onBack: returnFromCityToMap`），退出来之后地图在屏幕上、名字永远停在
  `'city'`。浏览器侧一直有这个毛病，只是它 `page.reload()` 一下就回大厅了，从没人注意到；小游戏
  没有 reload（`wx.reLaunch` 是小程序 API），所以第一次真跑**它之后的 16 站全丢了**。
  包内的替代品是 `forceLobby()`：倒着遍历记录过的每个 `*Cb`，谁的 `onBack` 能回大厅就用谁——
  并且**记进报告**（`forcedLobby`，本轮 7 次），因为每一次都是一个「名字失真」的位置。
- **`show*` 被调用了，可那个场景根本没上舞台。** 这一种更阴——见下一条。

因此报告里每一行现在都带一个 `mounted` 字段，直接读 `SceneManager` 的 `current` / `overlayScene` /
`transition.phase`，而不是读 `state.screen`。**`city :: WorldMapScene +CityScene` 一眼就说清了第一种。**

#### ③ 未解：一族「场景在树上，画面上没有」

**报告里每一行的 `mounted` 就是为这一族加的**，因为只有它能把这件事说清楚。四类，全部在两轮复跑里
逐字相同，且**在浏览器同一站都是正常的**：

| 站 | `state.screen` | `mounted`（实读 SceneManager） | PNG 画的是 | 浏览器同站 |
|---|---|---|---|---|
| `result` / `result+extreme` | `result`（`cbKeys` 是 ResultScene 的） | **GameScene** | 战斗 | 真结算页 |
| `battlePass` | `battlePass`（`cbKeys` 是 BattlePassScene 的） | **GachaScene** | 抽卡页 | 真战令页 |
| `city` / `defenseEditor` / `city+trainModal` | 对 | `WorldMapScene +CityScene`／`+DefenseEditorScene` | **只有世界地图** | 真主城 / 真编辑器 |
| `equipment+craft` | `equipment` | `EquipmentScene` | **背包页，没切到锻造** | 锻造页 |

前两行是「`show*` 调了，`manager.current` 还是上一个场景」；第三行是「overlay 确实挂上了，但一个像素
都没画」；第四行是「标签找到了、点了，页面没反应」。

**同一族里有一个反例，而它可能就是线索**：`city+buildDetail` 那一站 CityScene **画出来了**
（22 → 67 个标签，PNG 是真主城）——同一个 overlay，同一次跑，隔几站就正常。所以不是「微信上
CityScene 画不出来」，是**某种时序**。

**一条还没验的线索**（写在这里是为了下一个人不用重新想，**不是结论**）：ADR-085 把 `WorldMapScene`
改成了 `paint:'reactive'`，而 `RenderPolicy` 每帧读的是 `SceneManager.paintMode`。「往一个 reactive
场景上压一层新 overlay」是否会因为没人宣告脏而始终不重绘，值得先查——但它解释不了 `result` /
`battlePass` 那两行（那两个是 `goto`，不是 overlay）。

已经排除的（别重复走）：

- **不是异常被吞**——`callCb` 现在把 throw 记进报告，两轮 `errors` 都是 `[]`；
  `showResult`/`showBattlePass` 也只是一句同步的 `manager.goto(timedBuild(...))`，中间没有 await、
  没有 try/catch，`timedBuild` 自己也不 catch。
- **不是渐变没走完**——`mounted` 里的 `fade:none`。
- **不是截图陈旧**——`mounted` 是审计同一刻实时读的，和 PNG 互相印证。
  （顺带更正一个我自己的错误推断：第一轮我以为 `family.png` 截到了上一站，其实那就是 FamilyScene
  的「还没加入家族」态——浏览器同一场景的壳长得一样。**「截图陈旧」至今没有证据，别当结论用**；
  入口里那两个 `requestAnimationFrame` 是预防，不是修了一个测到的 bug。）

**成因未知。这里只记测到的事实。**

顺带两条同族观察，都指向同一件事——**微信侧的 REST 现在是「部分通」而不是「全通」**：
`achievements` 那张 PNG 上有一条「网络连接失败」toast，`friends+world` 那张写着「世界频道加载失败」，
而同一次跑注册是成功的。

### 51.4 本轮的结果与它的边界

模拟器（390×844，`resolution: 1`，设计缩放 0.361，locale `zh`）：**35/36 站，0 条 finding，
0 blank，0 error**，36 张 PNG 全部落盘并**逐张读过**。唯一没走到的 `friends+mailRead` 是对的——
它点的是**浏览器 seed 写进去的**那封邮件标题。

**逐张读完的结论**：除上面 ③ 那三处（都不是排版问题）之外，没有发现新的排版缺陷。

⚠️ **两侧的 PNG 不能并排目测比大小。** 微信 `devicePixelRatio = 1`，而浏览器巡检强制
`deviceScaleFactor: 2`——同一台 390×844 设备，一边导出 390×844、一边导出 780×1688，缩到同一宽度看，
微信那张的字必然显得糊、显得小。我第一轮就是这么误判了「拍卖顶部标签条在微信上小得读不了」。
**可比的只有数字**：两边 `designScale` 都是 0.361，`tiny` 门禁用的是同一个 `fontFloorDesignPx`，
两边都 0 条。为了把这条坐实，同日补跑了浏览器侧的 `phone-390x844-zh` 一行（0 条），
两边同站的 PNG 逐张对比下来版面构成一致。

其余三条边界，别把这份报告当浏览器矩阵的替代品：

1. **没有 seed。** 浏览器侧靠 `docker exec` 直写 mongo 把账号灌满（§50.1：空表撑不坏版面），包内够不
   到数据库。所以这里走的是**全新空号**，靠内容撑开的那些站（拍卖、排行、家族、打造、训练）都是薄的。
   `gacha+draw` 那一站没变化就是这个原因——账号 0 金币，单抽本来就抽不动。
   **但别把每一站的「没变化」都推给空号**：`equipment+craft` 只是切一个标签页，不需要任何资源，
   它没切过去（见 ③ 第四行）。五个 tap 站里 `cardRoster+detail` 和 `city+buildDetail` 是确实生效的，
   所以「包内合成点击」这条路本身是通的，只是**不是每一站都通**。
2. **一个形状。** 浏览器侧的力量在十个视口；模拟器是 DevTools 当前选的那台机器。换机型再跑一遍。
3. **一个语言。** `WechatPlatform.supportedLocales` 是 `['zh']`。

**并且照例**：0 条 finding 不等于这 36 屏是对的（§50.11 那三条真缺陷都是在报告 0 的情况下逐张读 PNG
挖出来的）。本轮 ③ 那三条，全部是读 PNG + 读 `mounted` 读出来的，门禁一条都没报。

## 52. 签到月历竖屏：格子被宽度钉死、富余喂了行距；顺带补上「图标从来没被门禁量过」这个洞（2026-09-14）

**报告原话**：「这个页面，横屏时看着没什么，竖屏时就感觉格子里的东西看不清了。你先分析原因，因为其他页面可能也有这个问题。」

### 52.1 先把归因摆正：不是朝向问题，是视口问题

把两张截图的尺寸代进 `ScalingManager` / `PortraitLayout` / `LandscapeLayout` 算一遍，结论跟直觉相反——
**两边的格子在设计空间里几乎一样大**，差的全在设计 px → 屏幕 px 那一步：

| 视口 | 设计矩形 | 缩放 | 格子（设计 px） | 日期数字 | 奖励「+N」 | 奖励图标 | 里程碑角标 |
|---|---|---|---|---|---|---|---|
| 报告里的横屏窗口 1861×855 | 2351×1080 | **0.792** | 280×129 | 33.2 | 25.3 | 26.1 | 15.8 |
| 报告里的竖屏窗口 422×615 | 1080×1920 | **0.320** | 165×132 | **13.5** | **10.3** | **10.9** | **6.4** |
| iPhone 13 竖 390×844 | 1080×2337 | 0.361 | 165×132 | 15.2 | 11.6 | 12.3 | 7.2 |
| **手机横屏 844×390** | 2337×1080 | 0.361 | 278×129 | **15.2** | **11.6** | **11.9** | **7.2** |
| 桌面横屏 1366×768 | 1921×1080 | 0.711 | 223×129 | 29.9 | 22.8 | 23.5 | 14.2 |

（后四列是 CSS px。）最后两行是这张表的重点：**手机横屏和竖屏一样糊**，桌面横屏才是好看的那个。
「横屏没问题」只是因为报告者的横屏窗口是 1861 宽的桌面窗口（0.792×），竖屏窗口是个 422 宽的小窗（0.320×）。
真机上横竖一样。根因就是 §49.1 记过、至今没做的那件事：竖屏设计宽度钉死 1080。

**§49.1 的可读性下限在这一页一点忙都帮不上**，这点值得单独说，因为它解释了「为什么巡检全绿而页面不能看」：

- 下限抬的是**低于 `fontFloorDesignPx(scale)`（手机上 20 设计 px）的字号**。签到格里的字号是
  `snapFont(ch × 0.32 / 0.24 / 0.16)` = **42 / 32 / 20**，全在下限之上，**一个都没被抬**。
  下限管的是「某个 token 选小了」，这里是「整套尺度对这个视口太小」——不是一回事。
- 唯一压在下限上的是里程碑金币角标（20），而下限自己**封顶在 `bodyLg`**，于是它落在 6.4–7.2 CSS px，
  低于下限自己承诺的 7。这正是 §49.1 结尾「没买到什么」那段说的东西，在这一页原样复现。
- **下限只管文字，完全不管图标。** 奖励图标 `ch×0.26`、角标金币 `ch×0.16`、绿勾 `ch×0.42` 都是直接按
  格高算的 sprite，没有任何下限。截图里真正认不出来的就是这两个 glyph——它们还是手绘笔触，
  笔画宽度跟着一起缩，10 CSS px 上就是一团墨点。

### 52.2 本页真正能改的那一刀：富余高度喂错了地方

竖屏的 `cellH = Math.min(areaH * 0.78 / ROWS, cellW * 0.8)` 里**赢的一直是右边那项**（143 vs 184），
也就是格子被**宽度**钉死；§10.10 加的 `spread` 逻辑把剩下的高度**全摊成了行距**——
390×844 上每行 159 设计 px 的间距对 143 的格子，**网格纵向 48% 是空白**。

但格内一切都是 `ch` 的比例（数字 0.32、奖励 0.26/0.24、角标 0.16、绿勾 0.42），
**格高是唯一能够到它们的旋钮**，所以这部分花在行距上等于白扔。

改法（`client/src/scenes/DailyScene/panels/checkin.ts`）：**先算高度、纵横比只当封顶**——
`gridTop` / `gridAvailH` 提到 `cellH` 之前算，竖屏 `cellH = min((gridAvailH − rowGap×(ROWS−1)) / ROWS, cellW × PORTRAIT_MAX_ASPECT)`，
剩下的才摊成行距。横屏分支（`min(areaH×0.78/ROWS, cellW×0.8)` + 固定 `h×0.006`）**逐字节不动**。

`PORTRAIT_MAX_ASPECT = 1.25` 不是拍脑袋：字号梯子的顶是 `FS.display`（60 设计 px），
而日期数字是 `ch×0.32`，所以 `ch` 过了 ~190 数字就不再长、旁边的「+N」却还在长，
`snapFont` 会把两者都吸到 60 上——**格内层级当场倒过来**。1.25 让 `ch` 落在 206，正好卡在那之下。

效果（390×844，CSS px）：格子 132 → **206 设计 px（+56%）**，行距占比 48% → 19%：

| | 日期数字 | 「+N」 | 奖励图标 | 里程碑角标 |
|---|---|---|---|---|
| 旧 | 15.2 | 11.6 | 12.3 | 7.2 |
| 新 | **21.7** | **15.2** | **19.1** | **11.6** |

**横屏没跟着改**，虽然它也有约 16% 的富余：横屏格子涨 16% 之后日期数字仍然吸附在 42 这一档（不动），
只有图标能从 11.9 涨到 14.1——收益小，而代价是动一个报告者明确说「看着没什么」的版面。记在 52.5 的清单里。

### 52.3 taller cell 立刻撞出来的东西，以及抓到它的是新门禁

新格子一上去，**里程碑格的两位数日期直接压在自己的 `+40` 上**（14/21/30 三格，重叠 33%）。
成因干净利落：角标钉在格子**右上角**、按 `ch` 算大小；数字**居中**、也按 `ch` 算大小；
**决定它们会不会碰的那个维度——格宽——在两边的公式里都不出现**。
横屏格子 2.2:1，宽度富余，永远碰不上；竖屏新格子 0.8:1，当场就碰。

改法：竖屏给角标**留出顶部一条**（`NUM_TOP_FRAC = { landscape: 0.06, portrait: 0.22 }`）。
**30 格一律留**，不只给带角标的那 4 格——30 个数字在同一条基线上，才还读得出是个网格；
taller cell 正好付得起这条（数字底边 ~0.61 ch，奖励图标顶边 0.66 ch）。

这条重叠是**真浏览器巡检报出来的，不是肉眼看出来的**——我自己看 390×844 的 PNG 时没注意到。
值得记一笔：几何改动的第一轮回归，往往就在你刚改的那几行的正下方。

### 52.4 门禁：`iconTag` + `layoutAudit` 的 `icon` 一类

图标之前**从来没被量过**：art 解码之后一个 icon 就是普通 `Sprite` 装在普通 `Container` 里，
走显示树时跟任何别的图片没有区别，所以 §49.1 那套下限只够到文字。

- `client/src/render/iconTag.ts`（新）——`tagIcon(node, id, size)` 往 `name` 上盖 `icon:` 前缀、
  往节点上盖 `iconPx`，跟 `fastText.ts` 的 `tagged()`（`txt:` / `fsPx`）是同一套路子、同一个理由。
  盖的是**调用方要的尺寸**而不是 bounds：art 是 contain-fit 进方框的，非正方的图 bounds 比框矮。
- 三个叶子构造器全部盖章：`buildRasterTabIcon` / `buildInkIcon` / `buildMaterialIcon`；
  `buildIcon` 再用 **kind 覆盖 url**（见下面第二个坑）。
- `layoutAudit.ts` 加 `icon` 一类：有效尺寸 = `iconPx × 局部缩放`，门槛 `minIconDesignPx`
  **就等于文字那条下限**，`auditOptionsFor` 里同源。

**门槛为什么不敢定高**：要抓到本页 34 设计 px 的奖励图标，门槛得是文字下限的 1.7 倍，
那会把全游戏大部分图标一起报出来——因为它们小的原因都是同一个（52.1 的 0.36×）。
**永远红的门禁不是门禁。** 所以这条只抓那件本来一个人都没在看的窄事：
**图标画得比它旁边的字还小**。老实说：**它抓不到这次报告的那个 bug**，它抓到的是别的。

两个必须点名的坑，都是自己踩出来的：

1. **`buildMaterialIcon` 不能盖在 sprite 本体上。** `sprite.width = size` 的实现是
   `scale = size / texture.width`，这个图集是 128px 帧、画 20px，于是世界缩放 = **0.16**——
   而门禁正是读世界缩放来分辨「有人把它缩了」和「它本来就这么大」。盖在 sprite 上，
   全游戏的材料 chip 都会被报成「被缩到 1/6」（光 `equipment+craft` 一站 23 条假 finding）。
   改成跟另外两个构造器一样**盖在 wrapper 上**，wrapper 自己的 scale 是 1，数才是门禁以为的那个数。
2. **finding 的名字是 webpack 内容哈希。** 两个叶子构造器只看得见 art 路径，而
   `assetModuleFilename: 'static/[contenthash][ext]'` 在 dev 也生效，于是报出来是
   `tiny icon "8ee6d0c38b3ae9553c2a"` ——什么都没说。`buildIcon` 手里有 kind，就在那里覆盖。

**门禁真报出来的三处（都是原有问题，本轮一并修了）**：`lock` 18 设计 px（equipment / cardRoster，
6.5 CSS px）、`achievements` 的金币 19、`stats` 的播放 19。前者是写死的字面量 18 —— 而 18 正好是
`FS.body` 的基准值，改成读 `FS.body` 之后**桌面一个像素不动**、手机被下限抬到 20，并且跟它旁边
一直就是 `FS.body` 的「×N」角标对齐了。后两者是 `rowH * k` 这种比例尺寸，新增
`fontScale.ts#iconFloorPx(px)` —— **`snapFont` 之于比例字号，就是它之于比例图标**：只抬到下限，
不吸附到字号档位（图标没有落在排版档位上的道理）。抬的那个下限跟门禁用的是同一个
`fontFloorDesignPx`，两边同源。

**豁免一类：重复的 pip（`untagIcon`）。** 门禁的前提是「图形没有退路」——字糊了还是个已知长度、
已知位置的词形，图糊了就只剩一团墨。等级星**正好相反**：意义在**一排有几颗**，
三个金点照样读成三。按文字下限卡每一颗，一个手机视口报 **231 条**。
所以 `render/levelStars.ts` 的每颗星 `untagIcon`。**这是对门禁的豁免、不是对问题的豁免**：
12–15 设计 px 的星在手机上确实只有 ~5 CSS px，而且**光调大常数没用**——
每个调用方都传了 `maxW`，行变宽就被原样缩回去，它要的是宽度不是常数。一并记进 52.5。

### 52.5 验证 / 没做的

- **看过**：本机真 Chrome（500×615，`designScale` 0.320，跟报告截图同一个缩放）改前/改后各拍一次对比；
  巡检自带的 `client/portrait-report/phone-390x844/daily.png`（dpr 2）改前/改后各读一张——
  第一张上那三处数字压角标清清楚楚，第二张干净。
- **数值核对**：横竖屏 7 个视口把新旧公式都算了一遍（格高、行距占比、四个 glyph 的 CSS px、网格是否还装得下）。
- `tsc --noEmit -p tsconfig.test.json` + `tsconfig.fulllink.json` 干净；`npm run lint` 0 error；`build:web` 过。
- 新增 `client/test/ui/dailySceneCheckinCellHeight.ui.ts`（3 例）：竖屏格高（读日期字号，旧公式是 42）、
  竖屏数字必须在角标下方且 30 格同基线、横屏 y/字号/数字与角标的相对位置跨绝对尺寸逐字节不变。
  **断言里没有一个量文字宽度**——headless 适配器的 `measureText` 是 `length × 7` 且无视字号，
  宽度类的重叠断言在那里测的是 harness 不是版面；真宽度归真浏览器巡检管。
  既有 `dailySceneCheckinRowSpread.ui.ts`(5) / `dailySceneCheckinFocus.ui.ts`(12) 无回归。
- **门禁复跑**：`npm run test:portrait --grep "(phone-390x844|landscape-844x390)$"`。改前那一轮是它报出了上面 52.3 那三处重叠 + 231 条 pip + 23 条 material 假 finding；收尾那一轮两个视口 36 站全绿。过程中碰到两次跟本轮无关的工具拖抽（`Execution context was destroyed` / `__nwE2E.app is missing`），单独重跑即绿。

**没做的（按值得做的顺序）**：

1. **竖屏设计宽度**（§49.1 那件大事）。本节所有数字的分母都是它。没动。
2. **等级星在手机上 ~5 CSS px**。`buildLevelStars` 的 12–15 设计 px + 调用方的 `maxW` 收缩，
   三个场景（equipment / cardRoster / auction）要连带重排才动得了。已从门禁豁免，问题仍在。
3. **横屏格子还有 ~16% 富余**（52.2 末尾）：字号梯子吃掉大半收益，暂不动。
4. **`tiny` 这一类没有加 CSS px 判据**。想过，做不了：下限自己封顶在 `bodyLg`，
   360 宽机器上本来就够不到 7 CSS px，加了这条门禁在那一行**永久红**。等第 1 项。

---

## 53. 状态标签：文字换图标，按量出来的宽度而不是朝向（2026-09-15）

**起因**：用户截了一张竖屏「每日任务」的图，圈出每张卡右边的「In progress」，提议 **横屏宽、文字带描述；竖屏窄、只留图标**。

方向对，但两处要改：

### 53.1 判据不是朝向，是量出来的宽度

「位置窄」有三个成因，朝向只是最吵的那个：竖屏、德语（逐词最长）、中文（全宽且无断点）。
`if (this.landscape)` 只挡第一个。这条教训仓库里已经写过两遍——
`DailyScene/panels/index.ts` 领奖按钮的注释（「按实测文字宽度算，对朝向和语言都成立」）、
`BattlePassScene/cell.ts` 的 `Gesperrt` 在 465 宽格子里吃掉 282 px（§50.12，**横屏也会犯**）。

于是新增 [`ui/widgets/statusTag.ts`](../../client/src/ui/widgets/statusTag.ts)，和 `buttonLabel.ts` 互为镜像：

| | 是什么 | 挤不下时丢什么 |
|---|---|---|
| `drawButtonLabel` | 对「点下去会怎样」的承诺，**词是载荷** | 丢**图标**，词保持原字号（`minFit`） |
| `drawStatusTag` | 行本身已经用底色/边框/位置半说出来的**事实** | 丢**词**，留字形 |

两者都**不缩到可读下限以下**；`drawStatusTag` 干脆完全不缩：字形按 `ICON_RATIO × fontSize` 画，
比它替换掉的那行字更大，所以**只要原来的词过得了巡检的 `tiny` 门禁，降级后的字形就过得了 `icon` 门禁**
（`testing/layoutAudit.ts`：「读不清的字还是个词形，读不清的图标只是一团墨」）。
返回值是实占宽度，调用方拿它算自己那一侧还剩多少（每日任务卡的折行宽、战令格的奖励带）。

**只给有公认字形的状态用**：done/claimed = `check`，locked = `lock`。
拍卖行那七态（Sold / Cancelled / Expired / Leading / Outbid / Won / Lost）**不换**——没有一套约定俗成的图标，换了是猜谜。

### 53.2 每日任务这条，比「换图标」还该更狠：那行字是零信息

三个日常任务是二值的（`shared/src/retention.ts` `DAILY_TASKS`，各 1 点、做一次就完），
所以「In progress」=「还没做」=「卡片没变绿」。换成沙漏只是用更少像素说同一句废话。

**改法**：完成 → `✓ Done`（`drawStatusTag`）；未完成 → **什么都不画**。
连带把 label 的 `wordWrapWidth` 从写死的 `cardW * 0.6` 改成「卡宽减去标签实占」——
那条 60% 上限当初就是为了躲开右边的状态字，现在未完成的卡上已经没有东西要躲。
`daily.tasks.pending` 三语一并删掉（`i18n-no-dead-keys.test.ts` 会盯）。

### 53.3 重复的格子 ≠ 一次性的行：战令走纯字形

一轮改完先截了图，战令那一屏**更吵了**：Lv.1 时 40 格里 39 格 locked，
于是屏幕上印了 39 遍 `[锁] Locked`——比原来光一个词的版本墨还多，而且没多说任何事。

所以分成两种情形，判据是**这个格子重复几遍**：

- **一次性的行**（成就档位、充值里程碑、邮件领取横幅）→ `drawStatusTag`，有地方就带词。词只出现一次，值那个宽度。
- **重复的格子**（战令 40 格）→ **纯字形**，直接 `buildIcon`，不经过这个 widget。
  顺带把 §50.12 给那格打的两次德语补丁彻底解决：`Gesperrt` 占 282 px，一把锁占一个字形宽，
  奖励带拿回去的宽度立刻看得见（`×60` 不再被挤）。

`pass_required` 本来就是纯字形，这轮只是把三种字形的尺寸统一成同一个 `tagH`，
省得一列格子在两种锁尺寸之间跳。`battlepass.claimed` / `battlepass.locked` 三语删掉——
再也不画了，留着就是 dead copy。

### 53.4 顺手修的：成就领取按钮在横屏手机上被切两头

`achievement.claim`（"Claim +200"）的按钮宽是写死的 `rowH * 1.9`。
横屏手机（844×390）行矮 → `bh` 小 → 字号小（**设计 px**），但整屏在 0.36x 上渲染，
`drawButtonLabel` 撞到可读下限就停止缩小、任由文字溢出（它自己的 §50.12 规则）——于是金色框两头都被切掉。
改成按实测 label 宽定宽、`rowH * 1.9` 只当下限，跟 DailyScene 领奖按钮同一个修法。

**这个缺陷巡检从来没报过，因为巡检的号从来没有过一个可领的档位**——见下条。

### 53.5 巡检漏的洞：一个场景只走默认那个 tab

用户那张图之所以能活到被截图，是因为**竖屏巡检根本没走到那个 tab**。
`layoutStops.ts` 的 `daily` 站只进场，落在哪个 tab 由 `DailyScene.pickInitialTab` 从存档推（新号 → 签到页）。
Daily 另外三个 tab、拍卖行的 `mine`/`bids` 两个 tab 全没审过。

补了四站（用 `Hop` 的 `tap` 形式，点 tab 自己的标题，跟玩家一样）：
`daily+tasks`、`daily+weekly`、`auction+mine`、`auction+bids`。

**同时给 seed 补状态**，否则新站只能审到空壳：

- `save.retention.daily/weekly` 摆成**半完成**（三任务完成一个、三档周奖励领掉一档且下一档已达成）——
  带状态标签的恰恰是 done/claimed 那一半形态，全新号一个都渲染不到。
- `save.achievements` 给 `ach.campaign.chapters` 领掉档位 I（号本来就通关了第一章，档位是真达成的）。
- `auctionBids` 集合（My Bids 读的是这张参与流水，不是 `topBid`）——不喂它，那个 tab 在 44 条挂单的市场上渲染空状态。

### 53.6 怎么核的

没有后端。`TARGET=web-e2e` 的 `__nwE2E.views.showXxx(stub)` **直接挂场景**，
桩里塞进想看的状态，一个服务都不用起（同 `worktree-session-cannot-start-dev-server` 那条记忆）。
三个视口 × 六屏：`390×844`、`844×390`、`360×640` 德语。

**一条诚实的观察**：这六屏在三语三尺寸下，`[字形][词]` 这一对**都塞得下**——降级分支一次都没触发。
也就是说本轮真正改观的是 53.2（删掉零信息的词）和 53.3（重复格子只留字形），
而 53.1 那套降级是**给还没发生的字符串和盒子留的保险**。
保险没人走过就等于没有，所以补了 `test/ui/statusTag.ui.ts` 把三条契约钉住：
塞得下 → 字形+词；塞不下 → 丢词不丢字形；盒子比字形还窄 → 照画不缩（那是巡检该报的版面 bug，不是该悄悄糊掉的东西）。

### 53.7 门禁（2026-09-15 补，四份）

§53 的三条判断全都是「看着不对」得出的，而看着不对的东西下一个人会好心改回去，所以每条都钉住了。
**四份都做过 mutation check**：把对应的代码改回旧样子，确认它们真的会红。

| 钉什么 | 在哪 |
|---|---|
| 降级本身：塞得下 → 字形+词；塞不下 → **丢词不丢字形**；盒子比字形还窄 → 照画不缩 | `test/ui/statusTag.ui.ts` |
| 每日任务：未完成的卡**一个字一个图标都不画**（断言整块面板的文字预算），完成的卡恰好一个 `check`；折行宽由标签实占反推（未完成的卡拿到整张卡宽） | `test/ui/dailyTasksStatusTag.ui.ts` |
| 战令格：claimed/locked/pass_required **只有字形没有词**，claimable **只有词没有字形**；奖励组右边缘 ≤ 状态字形左边缘（就是 §50.12 那个德语撞车，现在在任何语言下都不该发生） | `test/ui/battlePassCellState.ui.ts` |
| 成就领取按钮 ≥ `[gift][label]` + padding，两个朝向都测；短文案仍走老的固定宽（下限不是替换） | `test/ui/achievementClaimButtonWidth.ui.ts` |

另外补了一份**跟本轮改动无关、但被本轮补站暴露出来**的表级门禁 `test/layoutStops.test.ts`（纯 node）：

- **每一站的报告名唯一**（`as ?? screen`）。重名 = 两站共用一个报告槽和一张 PNG，后跑的覆盖先跑的——
  社交页三个入口就是这么让三分之二的面积无声无息没被审过的（§50 那段注释记着）。本轮一次加四站，
  正是最容易撞名的时候，而**撞了不会报错**。
- **`tap` hop 的字面前缀在三语下都非空**。`tapLabel` 匹配的是 `raw.split('{')[0]`，
  一个以占位符开头的文案会截成空串，而 `indexOf('')` 对树上每个标签都返回 0——
  走法不会失败，它会去点最后碰到的那个东西。

`drawCell` 是纯函数，战令那份直接调它，不用起场景。

## 54. 巡检补齐剩下三处「只走默认 tab」：成就分类、Shop 组、看广告（2026-09-15）

§53.5 补了 `daily` 和 `auction` 各两个 tab，同时留了三处没补。这一轮补完，
并且顺手纠正了站点表里**两个名字说谎的站**。

### 54.1 成就墙：永远只审到 pve，而 pve 只有一条成就

`AchievementScene.fetch()` 落在**第一个非空分类**，也就是 `pve`。
而 `ACHIEVEMENTS`（`server/shared/src/achievements.ts`）里 pve 只有 `ach.campaign.chapters` 一条——
巡检一直在审一张卡，然后说这一屏干净了。pvp 有三条，是这个场景唯一堆得够高的分类。

补两站：`achievements+pvp`、`achievements+progression`（点分类 tab 自己的标题）。

**`collection` 故意不补**：那个分类一条定义都没有，`categories()` 直接把 tab 藏掉，
补了也只能永远记成 skipped。留了注释说明将来要补时的两个坑——
它的标题跟生涯底栏的 `collection.title` 同字（英/德都同字），
而 `tapLabel` 取的是**画得最晚的那个匹配**、生涯底栏恰恰画在最后，所以那天得改用 `tapText` 或改文案。

### 54.2 Shop 组：看起来是一个场景的 tab 条，其实是四个场景

`[Shop | 充值 | 抽卡 | 战令 | 累充]` 这条 tab 条由四个场景各画一遍（`app/nav/shop/nav.ts`）。
大厅的两个入口分别落在**抽卡场景**（`onOpenShop` → `goGacha`）和 **ShopScene 的充值 tab**
（`onOpenRecharge` → `goShop(…, 'coins')`）——于是：

- **ShopScene 的默认 tab**（月卡/年卡/新手礼包/皮肤的商品网格）**没有任何一站走到过**；
- **RechargeScene**（累计充值阶梯）同上。

补两站 `shop` / `recharge`，都经组内 peer 回调走（`openShop` / `openRecharge`），
**不点 tab 标签**：德语里 `shop.coinsTab` 和 `recharge.title` 都是 "Aufladen"，
点标签只会命中画得晚的那一个。

顺带纠正两个名字：`{screen:'shop', via:['onOpenShop']}` 实际落在 gacha（报告里一直叫 `gacha`），
标成 `gacha`；`{screen:'recharge', via:['onOpenRecharge']}` 实际是 ShopScene 的 coins tab，
改报告名 `shop+coins`——它此前占着 `shop` 这个名字，这正是「ShopScene 默认 tab 看着被审过、其实从没走过」的由来。

### 54.3 看广告 tab：一个发到 iOS、却在任何可驱动的 build 上都不存在的页面

`hasRewardedAd()` 为假时 DailyScene **整个不画**这个 tab——这是产品规则
（[[hide-dont-mock-unavailable-features]]：没有真广告后端就藏入口，绝不给玩家看假的），不是疏漏。
后果是：plain web 没有 `window.NWAds` bridge、微信广告位 id 还没配、CrazyGames SDK 在测试源加载不了，
**这个 tab 在任何一个巡检能驱动的 build 上都不存在**。

所以 spec 在 boot 前塞一个 bridge 桩（`lib/nwE2E.ts` 的 `installNativeAdsStub`），
只为让 tab 存在、能被量——跟 `lib/seed.ts` 直写 mongo 是同一笔交易，理由也一样：
**量的是这一屏的版面，不是它背后的接入**。桩里的 `showRewarded` 永远 reject，
所以它只能让一个 tab 可见，发不出任何奖励。包内微信巡检没有这个桩，这一站在那边 `gated` 跳过。

**一个顺带的好处**：Daily 的底栏从三个 tab 变成四个，巡检从此量的是 iOS 那个更挤的形状。

### 54.4 seed：让每个成就 tab 一屏内出齐三态

`save.achievements` 之前只给了一条 `claimedTiers: [1]`，但 `save.stats` **一个都没写**——
于是「已领取」是在统计值为 0 的档位上领的，可领（金色 Claim 按钮）那一态**一次都没渲染过**。
这轮补了五个统计值 + 四条 claimedTiers，取值都对得上号里本来就有的数据
（`pvp.wins` 取的就是 `save.pvp.wins` 那个 1284，`campaign.chaptersCleared` 取 3，号确实通关了第一章）：

- pve：chapters 3 → I 已领 / II 可领 / III(9) 进行中
- pvp：archer 640 → I 已领 / II 可领 / III 进行中；guard 2450 → 三档全领（做完的那种卡）；pvp.wins → 三个 Claim 按钮并排
- progression：meteor 128 → I 已领 / II 可领 / III(400) 进行中

### 54.5 跑出来的：门禁全绿，但逐张读 PNG 看到四件门禁看不见的事

`npm run test:portrait`，**45 站**（原 40 站 + 本轮 5 站），跑了三个视口：

| 视口 | 站数 | skipped | blank | findings |
|---|---|---|---|---|
| phone-390x844 | 45 | 0 | 0 | **0** |
| landscape-844x390 | 45 | 0 | 0 | **0** |
| phone-390x844-de | 45 | 0 | 0 | 11（**都不在本轮新增的站上**，见下） |

五个新站在三个视口上都到达并渲染出了内容（labels：`shop` 28/23/28、`shop+coins` 30/25/30、
`recharge` 27/28/27、`achievements+pvp` 41、`achievements+progression` 19、`daily+ads` 9）。

**逐张读 PNG 看到的、门禁一条都没报的四件事**（这轮不改，只记录）：

1. `shop`（竖屏）：年卡的 **"Save $10" 徽章压在 365 票根插画上**。`ShopScene/shop.ts` 里那条注释
   说的正是这个问题——当时把 `$10.00` 缩成 `$10` 修过一次；在 390 宽的竖屏上它又压回去了，
   因为徽章是卡片右上的**固定覆盖层、不随卡宽缩放**。横屏卡更宽，同一个徽章是让开的。
   门禁看不见：另一边是**插画不是标签**，label-vs-label 和「被后画的实心矩形盖住」两条都不适用。
2. `daily+ads`（竖屏英语）："Watch Ad" 的最后一个字母**紧贴绿色按钮右边框**，右侧零内边距
   （图标+文字整块左对齐）。英语刚好没溢出所以不算 `overflow`；德语反而安全——
   `drawButtonLabel` 丢掉图标并缩字号后两边都留出了余量。
3. `achievements+pvp`（竖屏）：第三张卡的底边**被生涯底栏盖住**——成就列表既不滚动、
   也没给 `bottomNavH` 留高度。门禁看不见：底栏是自己画的层，巡检只在同层内互比。
4. **大厅金币芯片显示 0**，而 seed 写的是 9,876,543。`wallet.coins` 是 commercial 权威、
   会被镜像回来盖掉 seed 的值。**这是既有回归，不是这轮引入的**：主检出里 2026-09-14 那份
   `report-phone-390x844.json` 配套的 `lobby.png` 已经是 0。后果是 seed 头注释里
   「七位金币是全 UI 复用最多的那个数字」那句**现在是假的**——所有带金币芯片的站量的都是一位数。

**德语那 11 条全部落在今天早些时候（§53.5）新增的两站上**，不是本轮的站：

- `daily+tasks`：德语的「+5 Münzen abholen」领取按钮**按标签自适应宽度**，宽到把它左边的
  「1 / 3」进度计数整个吃掉（计数先画、按钮后画 → 判为 `covered`）。
- `auction+bids`：倒计时「0T 1h 58m 32s」和「Überboten」徽章**逐行压在一起**（10 行，frac 0.74–0.85）。

两条都是同一个形状：**德语串变长，行里两个元素抢同一段宽度**——
按 [[scale-set-defeats-font-floor-2026-09-12]] 记的拍板顺序（加宽 → 折行 → 缩 `de.ts` 的值，按钮永不截断）处理。
§53.5 那四站当时是用 `showXxx(stub)` 三个视口核的、没跑过德语这一行，所以直到这轮才暴露。

## 55. 德语那一行终于跑了：两站各一处「同一行里两个元素抢同一段宽度」（2026-09-15）

§54.5 把德语的 11 条记成了「不是本轮的站，下次处理」。这轮处理完。

前置事实值得单独记一遍，因为它解释了为什么这两处能活到今天：§53.5 新增的四站，当时是用
`__nwE2E.views.showXxx(stub)` 在三个视口核的（§53.6），**德语那一行从来没跑过**。
跑一次，11 条。两条都是 [[scale-set-defeats-font-floor-2026-09-12]] 里那个拍板顺序的适用场景
（加宽 → 折行 → 缩 `de.ts` 的值，按钮永不截断），但两处**卡在顺序的不同一步上**，所以修法也不同。

### 55.1 每日任务：领取按钮按自己的标签自适应宽度，却不知道同一行左边还站着进度计数

**现象**：`1 / 3` 被判 `covered 40%`，盖住它的是那个 296×116 的灰色领取按钮。

**根因**不是「按钮太宽」，是**它的宽度公式里没有这一行的另一半**。2026-08-10 那次修的是反向的毛病
（按钮按 `cardW*0.45` 死宽、装不下自己的标签），修法是给它加一个按标签实测宽度算的**破顶值**——
破顶值只认标签，不认行。德语 `+5 Münzen abholen` 一来，按钮一路长到左边缘压在计数上；
计数先画、按钮后画，于是判 `covered` 而不是 `overlap`。

真正撑爆的是内边距：`btnPad = btnH * 0.5`，而 `btnH = cardH*0.85`、`cardH = areaH*0.22`——
**竖屏下这是屏幕高度的一个比例，却被拿去当水平内边距花**。296 CSS px 里有 58 是它。

**修法**（`DailyScene/panels/index.ts`）：给破顶值加一个**行的上限**，
`btnW = max(cardW*0.45, min(roomW, 标签 + 图标 + btnPad))`，`roomW = cardW − 计数实宽 − cardW*0.05`。

两个界限**故意分在 clamp 的两侧**：`cardW*0.45` 这个下限留在 `min` 外面——一行窄到连它都放不下，
那是巡检应该喊出来的版面 bug，不是靠继续缩来藏掉的；而上限之内怎么退让是
`drawButtonLabel` 本来就在做的事（缩到可读下限 → 丢图标 → 折两行，§50.12）。
实测德语在新上限内仍是一行满字号，没走到任何一级退让。

**爆炸半径**：`roomW` 在 390 宽竖屏上约 265 CSS px，而英语 `Claim +5 coins` 这一组连内边距要 261、
中文更短——**上限只对德语生效**，另外两语的按钮一个像素都没动（英语那张 PNG 逐张比过）。
不过 261 vs 265 的余量本身很薄，值得记一笔：这一行早就是满的，只是德语先撞上而已。

### 55.2 我的竞拍：德语「Mein Gebot: 706500」折行，把倒计时顶到结果徽章那一行

**现象**：10 条 `overlap`，frac 0.74–0.85，倒计时和 `Überboten` **逐字压在一起**。

**先把这一列的尺子量清楚**（这轮所有判断都挂在这三个数上）：竖屏 1080 设计宽，三列网格 →
格子 341.33 宽、200 高，图片 130，**信息栏 167.33 设计 px**。三语在竖屏手机上，
这几行的字号 token（`FS.tiny`/`small`/`body`）**全部被抬到可读性下限的 20 设计 px**，所以一个数管三行。
半角字形宽 **11.0 设计 px**，是从本轮报告里反推的实测值，不是估的：
13 字符的倒计时 53.806 CSS、14 字符的 57.778 CSS，差 3.972 CSS ÷ 0.3611 = 11.0；
同一份报告里另外两个标签（含一个粗体）用 `11n + 6` 逐像素吻合（6 是 `pixiText` 烤进画布的 padding）。
**一行 15 个字符。**

**根因**：英语 `My bid: 1122900` 正好 15 个字符——**一个字符的余量都没有**；
德语 `Mein Gebot: 706500` 是 18，折成两行。信息栏是自上而下长的，倒计时是**最后一行**，
右下角的结果徽章钉死在格子底部，所以多出来的这一行直接把倒计时推到徽章那一行上。
10 条正好是 10 个「被反超」的格子；**领跑的格子没有「我的出价」这一行**，所以它们是干净的——
截图里那两张 `Führend` 卡就是对照组。顺带说明 §50.13 把 `AUC_CELL_H` 从 180 抬到 200 买回来的
那 20 px 是**一行**，德语把它花掉了。

**按拍板顺序走，前两步都不可用**：

- **加宽**：信息栏宽度由三列网格定；要让德语这一行放下，图片得从 130 砍到 79，
  等于把「卡片格」降级成列表行。改成两列则是把四个视口的密度一起改掉——这轮不是干这个的。
- **折行**：这一列的折行**正是病因**。`listCell.ts` 的头注释 2026-09-12 就写死了这条
  （「第二行会落在按钮上，是这个格子唯一吸收不了的东西」），当时只把它挂在 `auction.timeLeft` 上。

- **于是缩 `de.ts` 的值**：`'Mein Gebot'` → `'Meins'`（`Meins: 9999999` = 14 字符）。
  在位读起来不含糊：紧邻的上一行就是 `Aktuelles Gebot: …`，这一行是「我的那个」。

**顺手带出的一条**：中文 `'我的出价'` 是同一个病——全角字形按两个半角算，
`我的出价: 9999999` = 187 设计 px，同样折行。改成 `'我的'`（上一行就是「当前出价」）。
**这条不是巡检看到的**，是写下面那份门禁、拿同一把尺子量三语时量出来的。

### 55.3 门禁（四份，都做过 mutation check）

| 钉什么 | 在哪 |
|---|---|
| 信息栏那三行（我的出价 / 买断 / 倒计时）在**最宽币值（七位）下都是一行**，三语 | `test/auctionCellInfoWidth.test.ts`（纯 node） |
| **166 确实是任何视口能产生的最窄信息栏**（下面 55.3.1） | 同上 |
| **格子真的按 `aucInfoColumnW()` 折行**——门禁和渲染器量的是同一列 | `test/ui/auctionScene.ui.ts`（My Bids 段） |
| 领取按钮：长标签仍然长过 `cardW*0.45` 下限，**但左边缘绝不越过 `3 / 3` 的右边缘** | `test/ui/dailySceneTasksClaimButtonWidth.ui.ts` 末条 |

- 信息栏宽度**从真常量算出来**，而不是抄一份会漂的算术：`listCell.ts` 里散着的
  `pad = 14` / `imgSize` 上限 `130` / 图文间距 `16` 提成 `AUC_CELL_PAD`/`AUC_CELL_IMG_MAX`/`AUC_CELL_IMG_GAP`，
  列数与格宽提成 `aucGrid()`、信息栏宽提成 `aucInfoColumnW()`（都在 `AuctionScene/types.ts`，
  该文件本来就没有任何运行时 import，纯 node 拿得动）。`list.ts`/`listCell.ts` 改成吃这几个。
  **mutation check**：把两个旧值放回去 → de 209、zh 187，两条都红。
- **但「提出来」还不等于「两边真的在用同一个」。** 格子完全可以继续用自己那份
  `x + cellW - pad - ax`，门禁照样绿——只是从此在量一列**已经不存在的东西**。
  所以 `auctionScene.ui.ts` 从渲染器那一侧把话说死：My Bids 的格子渲染出来之后，
  带 `wordWrap` 的那五行（名称 / 当前出价 / 我的出价 / 买断 / 倒计时）的 `wordWrapWidth`
  必须**等于 `aucInfoColumnW(aucGrid(w).cellW)`**。倒计时是活的时长、渲染和断言之间会跳秒，
  所以它靠**数个数**认（这一列正好五行），顺带也能抓到「某一行悄悄丢了 wordWrap」。
  断的是**宽度不是像素**：headless harness 的 `measureText` 是恒定 7px/字符，
  复现不了「哪门语言会折行」，而宽度恰恰是两边必须一致、也真的会漂的那个量。
  **mutation check**：把格子的 `rightW` 改回自己算（少 8 px）→ 159.33 ≠ 167.33，红。
- 领取按钮那份原先钉的是「一个不合理的长标签也要把按钮撑到放得下」——**那正是吃掉计数的那条行为**。
  改成上下两句一起说：长过下限 ✔、越过计数 ✘。
  **mutation check**：去掉 `min(roomW, …)` → 按钮左边缘 105.5 < 计数右边缘 148.6，红。

#### 55.3.1 写门禁时才发现：**竖屏手机并不是最窄的那一列**

上面 55.2 通篇按「167.33 是最紧的形状」在算，因为巡检十个视口里最窄的就是它。**这是巧合，不是性质。**

`cellW` 的最小值出现在**列数的边界上**——网格刚好差一点放不下下一列，于是每格退回
`AUC_CELL_W_TARGET` 本身。此时信息栏 = 340 − 28 − 130 − 16 = **166**，比手机的 167.33 还窄 1.33。
而且它**够得着**：`contentW` = 1784（横屏设计宽 2000，约 1852×1000 的窗口）就是这个数。

所以门禁的预算改成按 166 算，不是按 167.33。三语实测：**英语 165 / 德语 154 / 中文 143**——
英语在 166 里花掉 165，**余量一个像素**。这句话比「英语 15 of 15」更该被记住：
这一列早就满了，德语只是第一个撞上的。

判据是**扫出来的不是推出来的**，因为这一列对 `contentW` 是锯齿状、不单调：
扫的范围是两套 layout 能交给网格的全部 `contentW`——竖屏恒为 1080 设计宽，
横屏是 `designW − sidebarNavW`（`designW` ∈ [1920, 2592]，即 1704–2376）。
`sidebarNavW` 的 `round(h*0.2)` 在测里**故意抄了一份**（`HubTabs` 会把 PIXI 拽进来，
这套件是纯 node），跟 `layoutStops.test.ts` 的 `prefix()`、`lib/auditBox.ts` 的设计盒规则同一处理、同一理由。

### 55.4 这轮**没改**的两条

#### 55.4.1 中文倒计时在「还剩一天以上」时同样会折行

`'{d}天{h}时{m}分{s}秒'` 有四个全角单位字，`2天23时59分59秒` = 187 设计 px > 167.33。
挂单固定跑 72h（`AUCTION_DURATION_SEC`），所以线上 `d` 能到 2；
而**巡检的 seed 最长只挂 21 小时**（`seedFixtures.ts` 的 `buildAuctions`：`expireAt = now + (2 + i%20)h`），
于是这一格**从来没有被渲染出来过**，十个视口全绿也说明不了它是对的。

怎么缩一个三天的中文倒计时是文案决定，不是这轮该顺手替产品拍的，所以没动：
`auctionCellInfoWidth.test.ts` 的倒计时断言只覆盖 en/de，**这个洞写在那条断言的注释里**，
不是靠调参数让它绿。

#### 55.4.2 `narrow-360x640-de` 的 `daily+weekly`：德语活跃点标签折成三行，第三行「te」落在奖励行上

**这是跑出来的，不是想出来的。** 补完门禁后顺手把**另一条德语行**（`narrow-360x640-de`）也跑了一遍——
它同样从没跑过 §53.5/§54 新增的那批站。45 站里 `daily+tasks` 干净（说明 55.1 那个上限在两个竖屏视口都成立），
但多出**一条不属于本轮的 finding**：

`renderWeekly` 的 `daily.weekly.pointsProgress` 在德语下是 `9 / 9 Aktivitätspunkte`，
被 `cardW * 0.55` 的宽度帽折成**三行**（`breakWords` 把 `Aktivitätspunkte` 拆成 `Aktivitätspunk` / `te`），
第三行正好压在下面那一行的奖励图标 + `+20` 上（overlap 0.28）。
三张卡都这样，只有第一张被报出来——另外两张的奖励是单件装备/卡（`singleItem`，不画 `+N`），
于是没有「标签对标签」可判，但那个 `te` 照样坐在图标上。

**不是本轮引入的**：`renderWeekly` 在本轮 diff 里一行没动，德语那两条改的是拍卖的键。
病因也和 55.1/55.2 同族（德语复合词 + 一个按比例写死的宽度帽），但**修法是第三种**
（这张卡的标签是唯一一行、下面就是奖励行，所以要么给标签更多宽度、要么让奖励行按标签实际高度往下走），
属于另一件事，等拍板。

### 55.5 跑出来的

| 视口 | 站数 | skipped | blank | findings |
|---|---|---|---|---|
| phone-390x844-de | 45 | 0 | 0 | **0**（修前 11） |
| phone-390x844 | 45 | 0 | 0 | **0**（不变） |
| landscape-844x390 | 45 | 0 | 0 | **0**（不变；上一次跑这行时挂在 `Execution context was destroyed` 的导航 flake 上，重跑即过，不是 finding） |

两张 PNG 逐张读过：`daily+tasks` 的 `1 / 3` 完整露出、按钮仍是一行满字号；
`auction+bids` 12 个格子里，倒计时都回到徽章上面一行，被反超和领跑两种卡现在是同一个版面。

补跑的第四行（**不在原任务范围内**，是补门禁时顺手带的）：

| 视口 | 站数 | skipped | blank | findings |
|---|---|---|---|---|
| narrow-360x640-de | 45 | 0 | 0 | 1（`daily+weekly`，见 55.4.2；**本轮两站在这个视口上都是干净的**） |

---

## 56. 设置页收尾两件小事：几何读数退回原生壳、法律链接按量出来的宽度并排（2026-09-17）

**起因**：用户对着 1920×911 的浏览器截图圈了两处——底部那行 `inner 1920x911 | screen 1920x1080 | dpr 1 | env 0/0/0/0 | vv 1920x911@0 | browser` 该去掉了；右下角 `法律条款` 那一小块「太挤了，要不要排一行」。

### 56.1 读数：不是删掉，是退回它唯一有用的地方

那两行是 ADR-088 加的，而 ADR-088 的真机验收**至今没做**（现有 TestFlight 包仍是 `contentInset:'always'`，见记忆里那条开放项），所以整条删掉等于把验收口径也删了。

判据现成就在代码里：`viewportVerdict()` 对非原生壳一律返回 `browser`——**它在浏览器里本来就不下结论**，因为标签页/地址栏吃掉视口是合法的。也就是说这一行对网页玩家既看不懂、又什么都证明不了。于是 `drawViewportDiagnostics` 改成 `if (!geom || !geom.nativeShell) return;`：

- 浏览器 / 微信（微信本来就不实现 `getViewportGeometry`）→ 一行不画；
- Capacitor 壳 → 一字不改，`inset-eaten` → `env-reported` 那套验收照旧。

### 56.2 链接：一行还是两行，让宽度说了算

`法律条款` 底下原本是固定两行（`· 隐私政策` / `· 用户协议`），紧挨着上面的「重看新手教学」按钮和下面那两行读数，横屏里三者挤在 0.845h–1.0h 这段 ~130 设计 px 里。读数撤走之后这段空出来一截，才有得排。

**没有无条件排成一行**：右栏是 `0.56w`…`0.94w`，横屏 ~730 设计 px（三种语言都放得下），竖屏只有 ~410，而德语 `· Datenschutzerklärung · Nutzungsbedingungen` 要 ~620。所以按**量出来的宽度**决定（同 §53.1 的判据）：放得下就并排一行，放不下就维持原来的两行，并且两种排法各有自己的纵向分寸——并排那份把省下的一行还给「按钮↔标题」和「标题↔链接」两个间距（横屏实测 24/16/22 px 的留白），堆叠那份保持原有分数不动，因为它在原生壳里下面还得让出读数的两行。

点击热区跟着改：堆叠时仍然是「至少 0.3w 宽」（一个刚好等于 `Terms` 字形宽度的热区在手机上点不中），并排时只能占自己的字宽 + 半个间距，否则两个热区互相吞——`用户协议` 会打开隐私政策。

### 56.3 一个必须先解决的门禁问题：headless 的 `measureText` 会把分支钉死

`test/ui` 的 PIXI 桩（`test/harness/pixiHeadless.ts`）的 `measureText` 是**字符数 × 7px、跟字号无关**（§48 开头就记过这件事）。于是任何「按测出来的宽度分支」的布局，在 UI 层里每个字符串都只有真实宽度的三分之一，分支被永远钉在「放得下」那一侧——德语竖屏那条根本测不到。

修法是给宽度一个**由字符串本身推出来的下界**：`render/pixiText.ts` 新增 `monospaceWidth(text, fontSize)`（等宽字体下按格子数算：拉丁 `MONO_CELL.latin` em、全角 1 em），布局取 `Math.max(text.width, monospaceWidth(...))`——真浏览器里测量值当家，headless 里估算值兜底，两边的分支都真的会走。

### 56.4 怎么核的

真 Chrome（`localhost:9390`，worktree 自己的 dev server）逐个看过：横屏 1920×911 中/英/德三种语言都并排一行、`法律条款` 与按钮之间有明显留白、底部读数消失；把 `innerWidth/innerHeight` 改写成 480×900 再派发 `resize` 得到竖屏排布——中文并排、英语与德语堆叠两行，与宽度判据一致。并排那行左右两个链接各点一次，分别打开 `/privacy.html` 与 `/terms.html`。

门禁：`test/ui/settingsLegalLinks.ui.ts`（新增「宽就并排/窄就堆叠」「并排时点谁是谁」三例，重叠断言改成「两轴任一不相交」）、`test/ui/settingsViewportDiagnostics.ui.ts`（新增「浏览器里一行不画」，原有八例全部仍以原生壳几何跑）。`test:ui` 272 文件 / 2697 例全绿，`tsc --noEmit` 干净。

### 56.5 补门禁（2026-09-17，用户追问"有测试可以加吗"）

§56 落地时只有场景层的用例，三处没人守：`monospaceWidth` 自己、它那两个常数、以及"一行还是两行都别出栏"。补完三份，每份都做了变异验证。

**① `test/monospaceWidth.test.ts`（13 例，纯函数层）**：拉丁 0.6→按常数计的格子宽、CJK 全角、线性、空串、各类全角边界（假名、CJK 标点、全角拉丁、谚文）、以及**代理对只算一格**。最后这条顺带修了个真 bug：原来的字符类只覆盖 BMP，`𠀋` 这种扩展区汉字会被算成**两个拉丁格**（1.2 em），改成带 `u` 标志 + `\u{20000}-\u{3FFFD}`。

**② `test/browser/textMetrics.spec.ts` 新增一例：`MONO_CELL` 必须夹住真实字距。** 这一例当场推翻了 §56.3 写下的数字——**Chrome 在本机把 `'monospace'` 解析成 Consolas，字距是 0.5498 em，不是 0.6**。也就是说那个"下界"在唯一被验过的运行时上**高估了 9%**：`Math.max` 里估算值会盖过真实测量，浏览器就会拿猜的数排版而不是拿它量得到的宽度。常数下调到 **0.54**（DejaVu Sans Mono / Menlo 约 0.602，0.54 是这三者的地板），断言两头都卡：不得高于实测（否则不是下界），也不得低于实测的 1/1.25（否则就退化成 `chars*7` 那种"三分之一"的假估计）。

**这一例本身是被另一个 bug 挡住才跑出来的**：`textMetrics.spec.ts` 原先用 `page.evaluate(probeTextMetrics.toString())` 把探针序列化进页面，而探针闭包引用模块级的 `PROBE_STRINGS`/`PROBE_SIZES` 和两个 helper —— 页面里全 undefined，**整个 spec 一直是红的、没人知道**（opt-in 的 `test:browser`，CI 那条又长期 `continue-on-error`）。正确形状早就写在 `entries/web-e2e.ts` 的 `handle.textMetrics` 上，spec 从没跟上。改成走 `__nwE2E.textMetrics()` 之后两例都绿。教训记进 [`client-testing.md`](../../claudedocs/client-testing.md)。

**③ `settingsLegalLinks.ui.ts` 再加 6 例（3 语言 × 横竖）：无论落在哪个分支，字形和点击热区都不许越过 0.94w 那条栏边。** 这补的是横屏德语——竖屏巡检（`portraitLayout.spec.ts`）的德语行全是竖屏，横屏那几个视口清一色 `locale: 'en'`，所以最长的那对标签在最窄的那一栏里，谁也没量过。

**变异验证**：`oneRow` 钉死成 `true` → 5 例红（含新加的 en/de 竖屏出栏）；把 `Math.max(text.width, monospaceWidth(...))` 退回只读 `text.width` → 3 例红（德语竖屏当场排成一行）。

---

## 57. 反馈面板：发完就关，确认交给 toast（2026-09-17）

**起因**：用户看着反馈面板的截图问——「玩家发送完反馈后，这个页面直接关闭，然后一个弹窗提示感谢您的反馈，用户点确认关掉，是不是更好？」

**答一半做一半**。关面板：对，比原来好。原行为是 2026-08-04 落地时的有意决定（见 [`UI_DESIGN_SCENES.md`](UI_DESIGN_SCENES.md) §4.1.1），
成功后留在原地、清空输入框、在按钮上方写一行绿色「已收到，谢谢」，理由是「反馈没有一张工单的模型，可以连着发第二条」。
这个理由不划算：连发是极少数情况（重开面板只要一次点击），代价却落在常见情况上——面板原样杵着，唯一变化是一行小字，
玩家视线在按钮上，看不见就会再点一次 Send，而且要离开还得再点一次 Close，一件事两次点击。

**「感谢弹窗 + 确定按钮」没做**：它说的话和 toast 一字不差，却多一层遮罩和一次点击。
确认按钮的意义是「需要你做个决定」或者「这条信息重要到必须挡住你」，「我们收到了」两样都不占。

**改动**（三处，都是往既有形状上靠）：

| 位置 | 改了什么 |
|---|---|
| `client/src/ui/dialogs/FeedbackDialog.ts` | `submit()` 成功分支：清空输入框 + 写绿字 → `closeInput()` + `cb.onClose()`；`submitting` 只在 catch 里复位（成功后留 `true`，挡住关闭前的第二次点击）——与 `AppealDialog.submit()` 逐行同形 |
| `client/src/app.ts` | 反馈 sink 的 `onSubmit` 由裸转发改成包装：`await core.submitFeedback!(text)` 之后 `showToastMessage(t('feedback.sent'), 'success')`，与上面 appeal sink 里 `appeal.submitted` 那行写法相同 |
| `i18n/locales/{en,zh,de}.ts` | `feedback.sent` 从「已收到，谢谢！」加重为「感谢反馈，我们已经收到，会尽量处理。」（EN/DE 同步）——它现在是屏幕上唯一的确认 |

**失败路径故意不对称**：仍然不关面板、红字提示、保留已输入文字。玩家刚打完一段话、提交又失败，正是最不能丢那段话的时刻。

**文案长度**核对过：新串 EN 55 / DE 57 字符，与既有 toast 同一量级（`reconnect.gone` 53、德语 `common.syncFailed` 66），
`GlobalToast.show()` 本来就带 `wordWrap + breakWords` 的宽度帽（2026-08-03 那条修的就是它），窄屏折行不会冲出画面。

**测试**：

- 新增 `client/test/ui/feedbackSubmitOutcome.ui.ts`（12 例）：横竖屏各测「提交 trim 后的文字 + `onClose` 恰好一次」「不留内联确认（statusLabel 为空）」「顺手关掉平台输入框」「失败留在原地 + 红字」「失败后重试不从空白开始」，另加在途二次点击只发一次、纯空白不发网络请求。**临时把旧成功分支贴回去验证过 9/12 先红**。
- 新增 `client/test/feedbackSuccessToast.test.ts`（3 例）：源码扫描 `app.ts`——toast 必须在 `await` 之后（先弹就是在确认一件可能还会失败的事）、键是 `feedback.sent`、与 appeal 那条保持同一写法。弹窗自己不知道 toast 的存在，而 `app.ts` 没有端到端单测入口（同 `appTickerDialogWiring.test.ts` 的理由）。
- 改 `client/test/ui/dialogModalInputGate.ui.ts` 两例：原先断言「成功后面板仍开着」，按新行为改写。2026-08-10 那条回归（关闭键把大厅导航走了）意义不变，而且成功路径现在根本没有那次 Close 点击。

**验证**：`npm run typecheck`、`npx vitest run`（3778 例）、`npm run test:ui`（2705 例）、`npm run build:web` 全过；
真 Chrome 跑了一遍（worktree dev server 9190 → 本地 8088 的 docker 后端，已登录账号 LimeMutt）：输入 → Send → 面板消失 → 大厅上浮出绿色 toast，文字白底绿条读得清。

## 58. 选队弹窗：队伍行的「兵力 / 体力」从词换成图标（2026-09-17）

**起因**：用户圈出占地选队弹窗里 `Team 1 · Troops 2525 · Stamina 100` 那一行问——「这些信息，team、troop、stamina 用图标显示吧」。

**他圈的那一行本来就是坏的，不只是啰嗦**。`btnH` 是 84，四列时 `btnW` 是 210，标签按 `FS.title`（32）折行——
那串字在英文下是**三行**，装得下两行，所以第三行被切掉了（截图里 `Stamina` 的 `100` 只剩上半截）。
更要紧的是：五行里 `Troops`/`Stamina` 两个词**一字不差地重复了五遍**，真正有差别的恰恰是被切掉的数字。
所以换图标不是省地方，是把地方从重复的词挪给唯一有信息的那部分。

**做法**：给弹窗按钮加第二行——`ModalButton.stats`，一串 `[字形][数字]` chip，画在标签下方，整块（标签 + chip 行）在按钮里居中。

| 位置 | 改了什么 |
|---|---|
| `WorldMapPanels/modalLine.ts` | 新增 `ModalButtonStat`（`{icon, text}`）与 `ModalButton.stats?`。`text` 只放数字，量的名字由字形承担 |
| `WorldMapPanels/core.ts` | `buildStatRow()` 把一行 chip 铺成**一个容器**（整组缩放，同 `buttonLabel.ts` 的约定），`showModal` 把它排在标签下方；`btnH` 在有 stats 的弹窗里**加高** `statBlockH`，`labelBoxH()` 让前置字形门禁仍按标签真实可用高度判断 |
| `worldmap/net/march.ts` | 选队行的 label 退回**只剩队伍名**，兵力/体力走 `stats`：`unit` + 数字、`flame` + 数字（当天先借 `hourglassMd`，同日换成自己的火苗，见下方追加） |
| `WorldMapPanels/statRow.ts`（新） | chip 行的几何 + `buildStatRow`。core.ts 加上 chip 行后到 504 行、撞上 500 行门禁，按拆分优先级第一档（form① 自由函数）拆出去，回到 457 行。`statFont()`/`statBlockH()` 是**函数不是常量**——`FS.*` 是 getter，模块级常量会被钉在未抬升的字号表上 |

**字形是借的，不是新画的**（零新美术）：`unit` 在这张地图上已经是「兵力」（地块驻军行、派兵弹窗的可用兵力），
`hourglassMd` 已经是「按墙上时钟恢复的量」（占领倒计时、城池每小时恢复）。**这条是使用边界**：
只有玩家在同一屏已经见过的字形才能替换掉词，否则那不叫图标化，叫猜谜（与 §53 状态标签同一条判据）。
前置的 `swords` 保留——它说的是「这是一支能出征的队伍」，和 chip 里的量不是一回事。

**两个数被实拍推着改了两次**：

- chip 字号先用 `FS.small`，在 1568 宽的窗口里量到约 10 CSS px——名字 32px、数字 10px，
  而数字才是玩家要横向比较的东西。改 `FS.label`（24），仍明显从属于名字。
- 加大之后两行（38 + 6 + 30 = 74）挤在 84 的按钮里，名字和数字各距手绘边框 4px 上下。
  于是**按钮加高**而不是把字缩回去——按钮长高一点没关系（§50.12 的既有拍板），弹窗自己按 `btnH` 算高度，跟着长。

**同日追加：体力这枚字形是借的，已约好还**。用户看完就问「体力的图标一直都是漏斗吗」——沙漏在这一屏上已经是**三层「时间」**（HUD 的加速 buff 剩余、弹窗的占领/围攻倒计时、商城加速道具三档），而体力是「还能下几次令」的预算，同一字形让 `100` 读成时长。（保护盾不是沙漏，是批 10 的伞；用户记的方向对、道具记错了一个。）库内 109 枚里没有能借的：闪电=`atkspd`、弹簧=`binding`、心=`hp`、箭=武器语言（批 8 给 `range` 写过定论）、齿轮=`settings`、钥匙=`key`、油灯=`umbrella`、液体=`ink`、脚印=`footsteps`。**用户拍板出一张新图：火苗（`flame`）**，并把判定线放宽为「在最小尺寸分不清无所谓，大部分情况适用即可」——判断表、prompt、接线与验收口径在 [`tab-icon-art-prompts-batch13.md`](../product/tab-icon-art-prompts-batch13.md)，**同日就出图并接线完成，一版过**（96×128 = 1.33:1，两个火尖 + 切口 + 内凹底沿连 26px 都还在，比这条放宽的判定线要求的更好）。另存档了一个非字形方案（体力真身是离散的 6 次，回 `◼◼◼◼◻◻` 比任何字形都准），未推进。

**测试**：新增 `client/test/ui/worldMapModalBtnStats.ui.ts`（4 例，headless PIXI）：两个数字画在标签下方且在按钮内、按传入顺序、各自左边有自己的字形；
标签 + chip 整块居中（标签让出正中）；没有 stats 的按钮**还在正中**（老版面一字不动）；chip 行超出列宽时整组缩放而不是溢出（用 11 位数字迫出缩放，并按缩放后的间距反推非空测）。
「chip 行的高度记在标签预算上」这条**没法在这个 harness 里钉**——它的 stub 2D context 对任何字号都报每行约 10px，
84 的按钮永远用不完高度，去掉预算这条也会绿；写在文件头当已知缺口，改由真字体实拍覆盖。
另改两处既有选队测试（`worldMapOccupyTeamPicker` / `worldMapTeamStaminaPicker`）：它们原来在 label 里找 `Troops 2160`/`Stamina 100`，现在读 `stats` 里对应字形的数字。

**验证**：`npm run typecheck`、`npx vitest run`（3791 例）、`npm run test:ui`（2719 例）、`npm run check:filelength`、`npm run build:web` 全过。
真 Chrome 看了三个尺寸（dev server 9190 → 本地 8088）：桌面 1568×744、竖屏 390×844 dpr2、横屏手机 844×390。
五行全部单行放下、无裁切，数字与两枚字形在 dpr2 竖屏上清楚可读；横屏手机上字号下限把 chip 抬到接近名字大小，正是想要的降级方向。
**弹窗是用 hash 路由的临时 harness 起的**（`#pickerdemo`，用真 `WorldMapPanels` + 手搓 ctx 调真 `showModal`，提交前删掉）——
本机 docker 账号（LimeMutt）在世界地图上一支队伍都没有，走真实入口只会弹「尚无队伍」。

---

## 59. 同意门的标题在德语下越过卡片两边（2026-09-21）

**起因**：用户报的一处 pre-existing 问题——`ConsentDialog` 的标题用固定的 `snapFont(unit * 0.07)` 画，
从来没跟卡片宽度核过。德语 `Datenschutz & Datennutzung` 在桌面横屏窗口（1920×911 → 2276×1080 设计矩形）里
量到 **858 设计 px，卡片只有 821**，左右各溢出约 18px；中英文在同一字号下放得下，所以只有德语截图能发现。
两种模式（`'accept-only'` / 新的 `'choice'`）表现完全一样——标题字号本来就不看模式。

**修法**：在 `measure()` 里把标题按卡片的文字栏收进去，而不是画的时候再缩。

| 位置 | 改了什么 |
|---|---|
| `ConsentDialog.measure()` | 新增 `innerW = cardW * 0.84`（**正文一直用的那一栏**，现在标题和正文共用一个量度）；标题先按 `unit * 0.07` 铸一次，量出来超栏就 `fitFont` 沿共享字号表**降一档重铸**，正文的 `wordWrapWidth` 也改读同一个 `innerW` |

三条边界值得写下来：

- **降字号，不是 `scale.set()`**。跟按钮（`widgets/buttonLabel.ts`）同一条：任意缩放因子会把字落在字号表外、在手机上掉到易读下限以下；`fitFont` 只在表上走，且以下限收口。
- **不用 `txtFit`**。它兜底是省略号——对服务器给的邮件标题是对的，对同意门的标题不是：`Datenschutz & Datennut…` 本身就是个 bug。
  没有哪个语言接近下限；真到了那天，**大声溢出**才会把修法推到翻译那边（`buttonLabel.ts` 同一判断）。
- **宽度取 `Math.max(title.width, monospaceWidth(...))`**，即 §56.3 那条下界。没有它，这个分支在 `test/ui` 里根本走不到
  （桩 `measureText` 不看字号，标题只有真实宽度的三分之一，永远停在「放得下」那侧），
  于是**断言写了也是绿的、跟代码干没干活无关**——本轮一开始就是这么写的，靠变异验证才撞出来。

**门禁**：`test/ui/consentDialogWrap.ui.ts` 新增一组 24 例（3 语言 × 4 视口 × 2 模式），
断言标题的「真实宽度」既不越过卡片外沿、也不越过正文那一栏，且**没有被截成省略号**（比对完整译文）。
视口用的是设计空间的数（2276×1080 等），跟真机映射一致。
**变异验证两次**：去掉整个 fit → 8 例红（德语 4 个视口 × 2 模式）；只去掉 `monospaceWidth` 那半边下界 → 同样 8 例红。

**怎么核的**：真 Chrome（worktree 自己的 dev server，端口 9390）桌面横屏 1568×744 截图帧下，中/英/德三种语言各看一遍，
德语先把修复退回去复现了原始溢出（量到 591 截图 px 的标题挤在 564 的卡片里，跟报告一致），再装回去确认收进卡片、上下留白对称。
英文与中文一字未动——它们本来就没触发收缩，这正是想要的。
**竖屏没有实拍**：本机 Chrome 窗口是最大化的，`resize_window` 改不动它（`innerWidth` 一直是 1920），
竖屏那两个视口只有上面那组断言覆盖。
`tsc --noEmit`、`npm run build:web`、`test:ui` 274 文件 / 2757 例全绿。

### 59.1 补门禁（2026-09-21，用户追问「有测试可以加吗」）

§59 只守住了标题那一颗节点。先把整张卡量了一遍（3 语言 × 4 视口，逐个非折行节点打真实宽度）——
**没有第二处溢出**，两处链接、四个按钮标签、AgeGate 的三种标题在三种语言下全部放得下。所以这三条是**守**，不是抓 bug。

**① 现有 24 例从「标题」放宽到「卡片上每一颗单行节点」。** 标题只是五颗里的一颗：两条链接至今仍是裸的
`snapFont(cardH * 0.042)`、没有任何宽度约束，按钮标签则靠 `drawButtonLabel` 自己兜。链接**另外**按正文那一栏收
（它们和标题一样是居中的文字，不像按钮标签有自己的盒子兜底），判据用 `· ` 前缀认——那是 `addLink` 唯一的标记。
这条正是 2026-08-25 那次重绘留下的教训：**一个结构性改动让一个用例红了，要顺手看还绿着的邻居**。

**② 两条「fit 不许做的事」。** 上界单独存在是可以靠「每种语言都画到下限」满足的，所以补：
- **只重铸、不缩放**：`title.scale.x === 1`、字号仍落在共享表上、不低于 `currentFontFloor()`。
  `scale.set(可用/需要)` 是这套字号下限要防的那个反射动作（§50.12）。
- **放得下的语言一个像素都不许动**：算出自然字号，自然字号放得下就必须**等于**它，放不下才允许更小、且更小之后要真的放得下。
  这条只在**一趟排版**时有意义（卡片太高触发第二趟会把 `unit` 整体缩掉），而「正文还在它的自然字号上」正好就是这个条件，
  所以判据写成读正文字号，不写视口白名单。

**③ 同一张卡的复制品 `AgeGateDialog` 补一组宽度门禁（3 语言 × 3 视口，ask/confirm/blocked 三态）。**
它连 `snapFont(Math.round(unit * 0.07))` 都是照抄的，而 `ageGate.ui.ts` 从来没在第二种语言下构造过这张卡：
文件头承诺的「it must FIT」一直只是**竖着**的（行不叠、不出屏），唯一横向的那条读 `getBounds()`——
在这个 harness 里是 `chars * 7`，跟字号无关，永远停在「放得下」。confirm 那态是唯一带插值数字的字符串，用真实点击走进去。

**变异验证五次，都红**：① 整个 fit 去掉 → 8 例；② 只去掉 `monospaceWidth` 那半边 → 8 例；
③ 把重铸换成 `title.scale.set(...)` → 10 例；④ 把 fit 的目标宽度砍半（放得下的也缩）→ 2 例（zh/en）；
⑤ 德语 `ageGate.blockedTitle` 换成一句更长的 → AgeGate 3 例。
**另有两次变异是无效的，值得记**：把标题比例从 `0.07` 提到 `0.12` 什么也没变——`snapFont` 封顶在最大档 60；
`fitFont(size, need, avail)` 在 `need <= avail` 时原样返回，所以只放宽 `if` 条件也不会让它缩。
**变异没红先别怪测试，先确认那条变异真的改变了产物**。

`tsc --noEmit`、`npm run build:web`、`test:ui` 274 文件 / **2772 例**（+15）全绿。本轮不含可见改动，故无实拍。

## 60. 输码键盘：横屏两行铺开，竖屏改成拨号盘（2026-09-21）

**起因**：用户拿着一张**改纯数字之前**的截图问「房间码改成纯数字了，这里也要跟着改」，并要求
「横屏稀疏地排两行即可，竖屏你自己测量」。字符集那一半当天早些时候已经随 §「房间码字符集」落地了
（`0123456789`，键盘 3×7 → 2×5），所以本轮真正要做的是**布局**——而一量就发现两个朝向各自的毛病。

**量到的（都用设计矩形的数，真机映射一致）**：

| 视口 | 设计矩形 | 改前 | 问题 |
|---|---|---|---|
| 横屏 1568×744 | 2276×1080 | 键 190×190，键盘 1086 宽 | 键盘只占动作行（1912）的 **57%**，两行小方块缩在页面中间 |
| 竖屏 390×844 | 1080×2337 | 键 168×168，动作行收在 1537 | 屏底空出 **800 设计 px ≈ 三分之一屏**，内容全挤在上半页 |

两处是同一个根因的两面：键格强制正方形、**按宽度和竖直预算里小的那个取值**，而动作行紧跟在键盘下面。
横屏的限制来自高（键被压小、宽度富余没人要），竖屏的限制来自宽（键被压小、高度富余没人要）。

**修法**（`codeEntryLayout`，`client/src/scenes/RoomScene/views.ts`）：

| 改了什么 | 为什么 |
|---|---|
| **三行共用 `0.84w` 一条宽度预算**（输码框行也是，改前是 `w*0.10` 一格，与另外两行无关） | 输码框 → 键盘 → 动作行读起来是一列，而不是三块各自为政的东西 |
| **动作行钉在屏底** `aY = h - 0.04h - aH`，键盘吃掉它与输码框之间的整条带 | 竖屏底部那三分之一屏是这么空出来的 |
| **列数看朝向**：横屏 5，竖屏 3（`perRow = w > h ? 5 : 3`） | 竖屏设计矩形宽恒 1080、高 1920–2337，5 列怎么调都填不满竖向；3 列正好 |
| **键宽高解耦**：宽吃到 `0.84w`，高吃竖直预算，上限 1.8:1、且不许比宽还高 | 横屏由此铺开成 355×202，与动作行齐平；1.8 的帽子是给 2.6:1 窗口准备的（否则键被拉成信箱） |
| 竖屏键序改 `1234567890`，末行单独一个 `0` **居中**（正对 `8`） | 3 列数字格的世界通用读法是电话拨号盘，不是 0 打头的计算器 |

改后：横屏 1568×744 键 355×202、键盘 1911 宽（动作行 1912）；竖屏 390×844 键 291×285、键盘 905×1188
正好吃满带宽，屏底余量 93。

**门禁**：`test/ui/roomCodeEntryLayout.ui.ts` 从 5 例/视口涨到 8 例/视口（4 个视口，32 例）。新增的四条守的是这次的决定本身——
动作行必须落在屏底 5% 以内、键盘的宽和高都要占到各自预算的三分之二以上、键不许比宽还高也不许超过 1.8:1、
以及「横屏 [5,2] / 竖屏 [3,4] 且键序分别是 `0123456789` / `1234567890`」。
原先那条「`rows * perRow` 必须正好等于 10」按新形状改写成「装得下 10 个、最多空出不到一列」——
**竖屏本来就有一个空格子，这条老断言留着就会把正确的拨号盘判红**。
`test/ui/scenes.ui.ts` 的 `tapDigit` 不能再按 `CODE_ALPHABET.indexOf` 找命中区了（竖屏键序不是字符集顺序），
改成读 `codeEntryLayout(...).keys`。

**怎么核的**：`npm run start:e2e`（9096）+ 真 Chrome。横屏用窗口自己的 1568×744 截图帧；
竖屏靠改写 `innerWidth`/`innerHeight` getter 再 fire resize（**必须先退回大厅**——非大厅场景把 resize 监听 `stop()` 掉了）
逼到 390×844，两个朝向各实拍一张，竖屏还点了 `1`/`5`/`0` 三个键确认命中区跟着新键序走（框里出现 `150`，没有错位）。
`tsc --noEmit`、`npm run build:web`、`test:ui` 275 文件 / 2808 例、`vitest run` 3860 例全绿
（`net-client.test.ts` 那例「重连时不许开第二个 socket」在全量跑里偶发红，单独连跑三遍全绿，与本轮无关）。
